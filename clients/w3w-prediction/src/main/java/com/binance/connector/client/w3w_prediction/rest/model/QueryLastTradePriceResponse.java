/*
 * Prediction Trading REST API
 * Place and manage prediction market orders, query positions, and transfer funds via the Prediction Trading REST API.
 *
 * The version of the OpenAPI document: 1.0.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package com.binance.connector.client.w3w_prediction.rest.model;

import com.binance.connector.client.w3w_prediction.rest.JSON;
import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import jakarta.validation.constraints.*;
import java.io.IOException;
import java.io.UnsupportedEncodingException;
import java.net.URLEncoder;
import java.nio.charset.StandardCharsets;
import java.util.HashSet;
import java.util.Objects;
import org.hibernate.validator.constraints.*;

/** QueryLastTradePriceResponse */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class QueryLastTradePriceResponse {
    public static final String SERIALIZED_NAME_MARKET_ID = "marketId";

    @SerializedName(SERIALIZED_NAME_MARKET_ID)
    @jakarta.annotation.Nullable
    private Long marketId;

    public static final String SERIALIZED_NAME_LAST_TRADE_PRICE = "lastTradePrice";

    @SerializedName(SERIALIZED_NAME_LAST_TRADE_PRICE)
    @jakarta.annotation.Nullable
    private String lastTradePrice;

    public QueryLastTradePriceResponse() {}

    public QueryLastTradePriceResponse marketId(@jakarta.annotation.Nullable Long marketId) {
        this.marketId = marketId;
        return this;
    }

    /**
     * Get marketId
     *
     * @return marketId
     */
    @jakarta.annotation.Nullable
    public Long getMarketId() {
        return marketId;
    }

    public void setMarketId(@jakarta.annotation.Nullable Long marketId) {
        this.marketId = marketId;
    }

    public QueryLastTradePriceResponse lastTradePrice(
            @jakarta.annotation.Nullable String lastTradePrice) {
        this.lastTradePrice = lastTradePrice;
        return this;
    }

    /**
     * Get lastTradePrice
     *
     * @return lastTradePrice
     */
    @jakarta.annotation.Nullable
    public String getLastTradePrice() {
        return lastTradePrice;
    }

    public void setLastTradePrice(@jakarta.annotation.Nullable String lastTradePrice) {
        this.lastTradePrice = lastTradePrice;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        QueryLastTradePriceResponse queryLastTradePriceResponse = (QueryLastTradePriceResponse) o;
        return Objects.equals(this.marketId, queryLastTradePriceResponse.marketId)
                && Objects.equals(this.lastTradePrice, queryLastTradePriceResponse.lastTradePrice);
    }

    @Override
    public int hashCode() {
        return Objects.hash(marketId, lastTradePrice);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class QueryLastTradePriceResponse {\n");
        sb.append("		marketId: ").append(toIndentedString(marketId)).append("\n");
        sb.append("		lastTradePrice: ").append(toIndentedString(lastTradePrice)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    public String toUrlQueryString() {
        StringBuilder sb = new StringBuilder();

        Object marketIdValue = getMarketId();
        String marketIdValueAsString = "";
        marketIdValueAsString = marketIdValue.toString();
        sb.append("marketId=").append(urlEncode(marketIdValueAsString)).append("");
        Object lastTradePriceValue = getLastTradePrice();
        String lastTradePriceValueAsString = "";
        lastTradePriceValueAsString = lastTradePriceValue.toString();
        sb.append("lastTradePrice=").append(urlEncode(lastTradePriceValueAsString)).append("");
        return sb.toString();
    }

    public static String urlEncode(String s) {
        try {
            return URLEncoder.encode(s, StandardCharsets.UTF_8.name());
        } catch (UnsupportedEncodingException e) {
            throw new RuntimeException(StandardCharsets.UTF_8.name() + " is unsupported", e);
        }
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces (except the first
     * line).
     */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n		");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("marketId");
        openapiFields.add("lastTradePrice");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to
     *     QueryLastTradePriceResponse
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!QueryLastTradePriceResponse.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                "The required field(s) %s in QueryLastTradePriceResponse is not"
                                        + " found in the empty JSON string",
                                QueryLastTradePriceResponse.openapiRequiredFields.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if ((jsonObj.get("lastTradePrice") != null && !jsonObj.get("lastTradePrice").isJsonNull())
                && !jsonObj.get("lastTradePrice").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            "Expected the field `lastTradePrice` to be a primitive type in the JSON"
                                    + " string but got `%s`",
                            jsonObj.get("lastTradePrice").toString()));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!QueryLastTradePriceResponse.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'QueryLastTradePriceResponse' and its
                // subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<QueryLastTradePriceResponse> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(QueryLastTradePriceResponse.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<QueryLastTradePriceResponse>() {
                        @Override
                        public void write(JsonWriter out, QueryLastTradePriceResponse value)
                                throws IOException {
                            JsonElement obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public QueryLastTradePriceResponse read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            // validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of QueryLastTradePriceResponse given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of QueryLastTradePriceResponse
     * @throws IOException if the JSON string is invalid with respect to QueryLastTradePriceResponse
     */
    public static QueryLastTradePriceResponse fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, QueryLastTradePriceResponse.class);
    }

    /**
     * Convert an instance of QueryLastTradePriceResponse to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
