/*
 * Prediction Trading REST API
 * Place and manage prediction market orders, query positions, and transfer funds via the Prediction Trading REST API.
 *
 * The version of the OpenAPI document: 1.0.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package com.binance.connector.client.w3w_prediction.rest.api;

import static org.junit.Assert.assertEquals;

import com.binance.connector.client.common.ApiClient;
import com.binance.connector.client.common.ApiException;
import com.binance.connector.client.common.ApiResponse;
import com.binance.connector.client.common.auth.BinanceAuthenticationFactory;
import com.binance.connector.client.common.auth.SignatureAuthentication;
import com.binance.connector.client.common.configuration.ClientConfiguration;
import com.binance.connector.client.common.configuration.SignatureConfiguration;
import com.binance.connector.client.common.sign.HmacSignatureGenerator;
import com.binance.connector.client.common.sign.SignatureGenerator;
import com.binance.connector.client.w3w_prediction.rest.model.GetMarketDetailResponse;
import com.binance.connector.client.w3w_prediction.rest.model.ListPredictionCategoriesResponse;
import com.binance.connector.client.w3w_prediction.rest.model.ListPredictionMarketsResponse;
import com.binance.connector.client.w3w_prediction.rest.model.MarketSearchResponse;
import com.binance.connector.client.w3w_prediction.rest.model.OrderBy;
import com.binance.connector.client.w3w_prediction.rest.model.QueryLastTradePriceResponse;
import com.binance.connector.client.w3w_prediction.rest.model.QueryOrderBookResponse;
import com.binance.connector.client.w3w_prediction.rest.model.SortBy;
import jakarta.validation.constraints.*;
import okhttp3.Call;
import okhttp3.Request;
import org.bouncycastle.crypto.CryptoException;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.mockito.ArgumentCaptor;
import org.mockito.Mockito;

/** API tests for MarketDataApi */
public class MarketDataApiTest {

    private MarketDataApi api;
    private ApiClient apiClientSpy;
    private SignatureGenerator signatureGeneratorSpy;

    @BeforeEach
    public void initApiClient() throws ApiException {
        ClientConfiguration clientConfiguration = new ClientConfiguration();
        clientConfiguration.setUrl("http://localhost:8080");
        SignatureConfiguration signatureConfiguration = new SignatureConfiguration();
        signatureConfiguration.setApiKey("apiKey");
        signatureConfiguration.setSecretKey("secretKey");
        clientConfiguration.setSignatureConfiguration(signatureConfiguration);

        HmacSignatureGenerator signatureGenerator =
                new HmacSignatureGenerator(signatureConfiguration.getSecretKey());
        signatureGeneratorSpy = Mockito.spy(signatureGenerator);
        SignatureAuthentication signatureAuthentication =
                new SignatureAuthentication(
                        signatureConfiguration.getApiKey(), signatureGeneratorSpy);
        SignatureAuthentication authenticationSpy = Mockito.spy(signatureAuthentication);
        Mockito.doReturn("1736393892000").when(authenticationSpy).buildTimestamp();

        BinanceAuthenticationFactory factoryMock = Mockito.mock(BinanceAuthenticationFactory.class);
        Mockito.doReturn(authenticationSpy)
                .when(factoryMock)
                .getAuthentication(signatureConfiguration);

        ApiClient apiClient = new ApiClient(clientConfiguration, factoryMock);

        apiClientSpy = Mockito.spy(apiClient);
        Mockito.doReturn(new ApiResponse<>(200, null))
                .when(apiClientSpy)
                .execute(Mockito.any(), Mockito.any(java.lang.reflect.Type.class));
        Mockito.doReturn(new ApiResponse<>(200, null)).when(apiClientSpy).execute(Mockito.any());
        Mockito.doReturn("1736393892000").when(apiClientSpy).buildTimestamp();

        api = new MarketDataApi(apiClientSpy);
    }

    /**
     * Get Market Detail
     *
     * <p>Get full details for a specific prediction market topic, including variant data and
     * timeline. Weight(IP): 200
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void getMarketDetailTest() throws ApiException, CryptoException {
        Long marketTopicId = 4229564L;
        ApiResponse<GetMarketDetailResponse> response = api.getMarketDetail(marketTopicId);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());


        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals("marketTopicId=4229564&timestamp=1736393892000", signInputCaptor.getValue());
        assertEquals(
                "47cb3089b54bb547f8a752a5dd2106ceb371a822827a64d5e7d064f8e40eaec1", actualRequest.url().queryParameter("signature"));
        assertEquals("/sapi/v1/w3w/wallet/prediction/market/detail", actualRequest.url().encodedPath());
    }

    /**
     * List Prediction Categories
     *
     * <p>Get all available prediction market categories (L1 and L2). Weight(IP): 200
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void listPredictionCategoriesTest() throws ApiException, CryptoException {
        ApiResponse<ListPredictionCategoriesResponse> response = api.listPredictionCategories();

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());


        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals("timestamp=1736393892000", signInputCaptor.getValue());
        assertEquals(
                "53668e00dc92eb93de0b253c301e9fc0c20042b13db384a0ad94b38688a5a84c",
                actualRequest.url().queryParameter("signature"));
        assertEquals("/sapi/v1/w3w/wallet/prediction/category/list", actualRequest.url().encodedPath());
    }

    /**
     * List Prediction Markets
     *
     * <p>Get a paginated list of prediction market topics, with optional category and sort filters.
     * Weight(IP): 200
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void listPredictionMarketsTest() throws ApiException, CryptoException {
        String l1Category = "crypto";
        String l2Category = "up-down";
        SortBy sortBy = SortBy.RECOMMENDED;
        OrderBy orderBy = OrderBy.ASC;
        Integer offset = 0;
        Integer limit = 20;
        ApiResponse<ListPredictionMarketsResponse> response =
                api.listPredictionMarkets(l1Category, l2Category, sortBy, orderBy, offset, limit);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());


        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals("l1Category=crypto&l2Category=up-down&sortBy=RECOMMENDED&orderBy=ASC&offset=0&limit=20&timestamp=1736393892000", signInputCaptor.getValue());
        assertEquals(
                "83281116b7368d7ac1190e8a85711bd1a3add818ab18440a3c3fe5aa9787ad9a",
                actualRequest.url().queryParameter("signature"));
        assertEquals("/sapi/v1/w3w/wallet/prediction/market/list", actualRequest.url().encodedPath());
    }

    /**
     * Market Search
     *
     * <p>Semantic search for prediction market topics by keyword. Weight(IP): 200
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void marketSearchTest() throws ApiException, CryptoException {
        String query = "BTC price";
        Integer topK = 20;
        ApiResponse<MarketSearchResponse> response = api.marketSearch(query, topK);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());


        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals("query=BTC+price&topK=20&timestamp=1736393892000", signInputCaptor.getValue());
        assertEquals("59c35d4885a5a2c8c2755b8b351cfd15b69e75bb437a5f42732d3971f0e6e8f7", actualRequest.url().queryParameter("signature"));
        assertEquals("/sapi/v1/w3w/wallet/prediction/market/search", actualRequest.url().encodedPath());
    }

    /**
     * Query Last Trade Price
     *
     * <p>Get the most recent trade price for a prediction market. Weight(IP): 200
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void queryLastTradePriceTest() throws ApiException, CryptoException {
        Long marketId = 5567895L;
        ApiResponse<QueryLastTradePriceResponse> response = api.queryLastTradePrice(marketId);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());


        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals("marketId=5567895&timestamp=1736393892000", signInputCaptor.getValue());
        assertEquals(
                "3412167c340fdcbdf845734eaed7971544f4ba69408679692becab658139c036", actualRequest.url().queryParameter("signature"));
        assertEquals("/sapi/v1/w3w/wallet/prediction/order-book/last-trade-price", actualRequest.url().encodedPath());
    }

    /**
     * Query Order Book
     *
     * <p>Get the current order book (bids and asks) for a specific prediction market outcome token.
     * Weight(IP): 200
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void queryOrderBookTest() throws ApiException, CryptoException {
        String vendor = "predict_fun";
        Long marketId = 5567895L;
        String tokenId = "112233";
        ApiResponse<QueryOrderBookResponse> response =
                api.queryOrderBook(vendor, marketId, tokenId);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());


        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals("vendor=predict_fun&marketId=5567895&tokenId=112233&timestamp=1736393892000", signInputCaptor.getValue());
        assertEquals("959176214d54cd8e4eb4a7097b6f1ebe75d9c5101619747bf1e136be60bf4c58", actualRequest.url().queryParameter("signature"));
        assertEquals("/sapi/v1/w3w/wallet/prediction/order-book", actualRequest.url().encodedPath());
    }
}
