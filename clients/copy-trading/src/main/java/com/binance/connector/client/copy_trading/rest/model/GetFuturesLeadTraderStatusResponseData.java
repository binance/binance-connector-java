/*
 * Binance Copy Trading REST API
 * OpenAPI Specification for the Binance Copy Trading REST API
 *
 * The version of the OpenAPI document: 1.0.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package com.binance.connector.client.copy_trading.rest.model;

import com.binance.connector.client.copy_trading.rest.JSON;
import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import jakarta.validation.constraints.*;
import java.io.IOException;
import java.io.UnsupportedEncodingException;
import java.net.URLEncoder;
import java.nio.charset.StandardCharsets;
import java.util.HashSet;
import java.util.Map;
import java.util.Objects;
import java.util.Set;
import org.hibernate.validator.constraints.*;

/** GetFuturesLeadTraderStatusResponseData */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.12.0")
public class GetFuturesLeadTraderStatusResponseData {
    public static final String SERIALIZED_NAME_IS_LEAD_TRADER = "isLeadTrader";

    @SerializedName(SERIALIZED_NAME_IS_LEAD_TRADER)
    @jakarta.annotation.Nullable
    private Boolean isLeadTrader;

    public static final String SERIALIZED_NAME_TIME = "time";

    @SerializedName(SERIALIZED_NAME_TIME)
    @jakarta.annotation.Nullable
    private Long time;

    public GetFuturesLeadTraderStatusResponseData() {}

    public GetFuturesLeadTraderStatusResponseData isLeadTrader(
            @jakarta.annotation.Nullable Boolean isLeadTrader) {
        this.isLeadTrader = isLeadTrader;
        return this;
    }

    /**
     * Get isLeadTrader
     *
     * @return isLeadTrader
     */
    @jakarta.annotation.Nullable
    public Boolean getIsLeadTrader() {
        return isLeadTrader;
    }

    public void setIsLeadTrader(@jakarta.annotation.Nullable Boolean isLeadTrader) {
        this.isLeadTrader = isLeadTrader;
    }

    public GetFuturesLeadTraderStatusResponseData time(@jakarta.annotation.Nullable Long time) {
        this.time = time;
        return this;
    }

    /**
     * Get time
     *
     * @return time
     */
    @jakarta.annotation.Nullable
    public Long getTime() {
        return time;
    }

    public void setTime(@jakarta.annotation.Nullable Long time) {
        this.time = time;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        GetFuturesLeadTraderStatusResponseData getFuturesLeadTraderStatusResponseData =
                (GetFuturesLeadTraderStatusResponseData) o;
        return Objects.equals(
                        this.isLeadTrader, getFuturesLeadTraderStatusResponseData.isLeadTrader)
                && Objects.equals(this.time, getFuturesLeadTraderStatusResponseData.time);
    }

    @Override
    public int hashCode() {
        return Objects.hash(isLeadTrader, time);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class GetFuturesLeadTraderStatusResponseData {\n");
        sb.append("		isLeadTrader: ").append(toIndentedString(isLeadTrader)).append("\n");
        sb.append("		time: ").append(toIndentedString(time)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    public String toUrlQueryString() {
        StringBuilder sb = new StringBuilder();

        Object isLeadTraderValue = getIsLeadTrader();
        String isLeadTraderValueAsString = "";
        isLeadTraderValueAsString = isLeadTraderValue.toString();
        sb.append("isLeadTrader=").append(urlEncode(isLeadTraderValueAsString)).append("");
        Object timeValue = getTime();
        String timeValueAsString = "";
        timeValueAsString = timeValue.toString();
        sb.append("time=").append(urlEncode(timeValueAsString)).append("");
        return sb.toString();
    }

    public static String urlEncode(String s) {
        try {
            return URLEncoder.encode(s, StandardCharsets.UTF_8.name());
        } catch (UnsupportedEncodingException e) {
            throw new RuntimeException(StandardCharsets.UTF_8.name() + " is unsupported", e);
        }
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces (except the first
     * line).
     */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n		");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("isLeadTrader");
        openapiFields.add("time");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to
     *     GetFuturesLeadTraderStatusResponseData
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!GetFuturesLeadTraderStatusResponseData.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                "The required field(s) %s in GetFuturesLeadTraderStatusResponseData"
                                        + " is not found in the empty JSON string",
                                GetFuturesLeadTraderStatusResponseData.openapiRequiredFields
                                        .toString()));
            }
        }

        Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
        // check to see if the JSON string contains additional fields
        for (Map.Entry<String, JsonElement> entry : entries) {
            if (!GetFuturesLeadTraderStatusResponseData.openapiFields.contains(entry.getKey())) {
                throw new IllegalArgumentException(
                        String.format(
                                "The field `%s` in the JSON string is not defined in the"
                                    + " `GetFuturesLeadTraderStatusResponseData` properties. JSON:"
                                    + " %s",
                                entry.getKey(), jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!GetFuturesLeadTraderStatusResponseData.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'GetFuturesLeadTraderStatusResponseData'
                // and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<GetFuturesLeadTraderStatusResponseData> thisAdapter =
                    gson.getDelegateAdapter(
                            this, TypeToken.get(GetFuturesLeadTraderStatusResponseData.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<GetFuturesLeadTraderStatusResponseData>() {
                        @Override
                        public void write(
                                JsonWriter out, GetFuturesLeadTraderStatusResponseData value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public GetFuturesLeadTraderStatusResponseData read(JsonReader in)
                                throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            // validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of GetFuturesLeadTraderStatusResponseData given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of GetFuturesLeadTraderStatusResponseData
     * @throws IOException if the JSON string is invalid with respect to
     *     GetFuturesLeadTraderStatusResponseData
     */
    public static GetFuturesLeadTraderStatusResponseData fromJson(String jsonString)
            throws IOException {
        return JSON.getGson().fromJson(jsonString, GetFuturesLeadTraderStatusResponseData.class);
    }

    /**
     * Convert an instance of GetFuturesLeadTraderStatusResponseData to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
