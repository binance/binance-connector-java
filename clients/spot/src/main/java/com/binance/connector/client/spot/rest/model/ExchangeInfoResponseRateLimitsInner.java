/*
 * Binance Spot REST API
 * OpenAPI Specifications for the Binance Spot REST API  API documents:   - [Github rest-api documentation file](https://github.com/binance/binance-spot-api-docs/blob/master/rest-api.md)   - [General API information for rest-api on website](https://developers.binance.com/docs/binance-spot-api-docs/rest-api/general-api-information)
 *
 * The version of the OpenAPI document: 1.0.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package com.binance.connector.client.spot.rest.model;

import com.binance.connector.client.spot.rest.JSON;
import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import jakarta.validation.constraints.*;
import java.io.IOException;
import java.io.UnsupportedEncodingException;
import java.net.URLEncoder;
import java.nio.charset.StandardCharsets;
import java.util.HashSet;
import java.util.Map;
import java.util.Objects;
import java.util.Set;
import org.hibernate.validator.constraints.*;

/** ExchangeInfoResponseRateLimitsInner */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.12.0")
public class ExchangeInfoResponseRateLimitsInner {
    public static final String SERIALIZED_NAME_RATE_LIMIT_TYPE = "rateLimitType";

    @SerializedName(SERIALIZED_NAME_RATE_LIMIT_TYPE)
    @jakarta.annotation.Nullable
    private String rateLimitType;

    public static final String SERIALIZED_NAME_INTERVAL = "interval";

    @SerializedName(SERIALIZED_NAME_INTERVAL)
    @jakarta.annotation.Nullable
    private String interval;

    public static final String SERIALIZED_NAME_INTERVAL_NUM = "intervalNum";

    @SerializedName(SERIALIZED_NAME_INTERVAL_NUM)
    @jakarta.annotation.Nullable
    private Long intervalNum;

    public static final String SERIALIZED_NAME_LIMIT = "limit";

    @SerializedName(SERIALIZED_NAME_LIMIT)
    @jakarta.annotation.Nullable
    private Long limit;

    public ExchangeInfoResponseRateLimitsInner() {}

    public ExchangeInfoResponseRateLimitsInner rateLimitType(
            @jakarta.annotation.Nullable String rateLimitType) {
        this.rateLimitType = rateLimitType;
        return this;
    }

    /**
     * Get rateLimitType
     *
     * @return rateLimitType
     */
    @jakarta.annotation.Nullable
    public String getRateLimitType() {
        return rateLimitType;
    }

    public void setRateLimitType(@jakarta.annotation.Nullable String rateLimitType) {
        this.rateLimitType = rateLimitType;
    }

    public ExchangeInfoResponseRateLimitsInner interval(
            @jakarta.annotation.Nullable String interval) {
        this.interval = interval;
        return this;
    }

    /**
     * Get interval
     *
     * @return interval
     */
    @jakarta.annotation.Nullable
    public String getInterval() {
        return interval;
    }

    public void setInterval(@jakarta.annotation.Nullable String interval) {
        this.interval = interval;
    }

    public ExchangeInfoResponseRateLimitsInner intervalNum(
            @jakarta.annotation.Nullable Long intervalNum) {
        this.intervalNum = intervalNum;
        return this;
    }

    /**
     * Get intervalNum
     *
     * @return intervalNum
     */
    @jakarta.annotation.Nullable
    public Long getIntervalNum() {
        return intervalNum;
    }

    public void setIntervalNum(@jakarta.annotation.Nullable Long intervalNum) {
        this.intervalNum = intervalNum;
    }

    public ExchangeInfoResponseRateLimitsInner limit(@jakarta.annotation.Nullable Long limit) {
        this.limit = limit;
        return this;
    }

    /**
     * Get limit
     *
     * @return limit
     */
    @jakarta.annotation.Nullable
    public Long getLimit() {
        return limit;
    }

    public void setLimit(@jakarta.annotation.Nullable Long limit) {
        this.limit = limit;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        ExchangeInfoResponseRateLimitsInner exchangeInfoResponseRateLimitsInner =
                (ExchangeInfoResponseRateLimitsInner) o;
        return Objects.equals(this.rateLimitType, exchangeInfoResponseRateLimitsInner.rateLimitType)
                && Objects.equals(this.interval, exchangeInfoResponseRateLimitsInner.interval)
                && Objects.equals(this.intervalNum, exchangeInfoResponseRateLimitsInner.intervalNum)
                && Objects.equals(this.limit, exchangeInfoResponseRateLimitsInner.limit);
    }

    @Override
    public int hashCode() {
        return Objects.hash(rateLimitType, interval, intervalNum, limit);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class ExchangeInfoResponseRateLimitsInner {\n");
        sb.append("		rateLimitType: ").append(toIndentedString(rateLimitType)).append("\n");
        sb.append("		interval: ").append(toIndentedString(interval)).append("\n");
        sb.append("		intervalNum: ").append(toIndentedString(intervalNum)).append("\n");
        sb.append("		limit: ").append(toIndentedString(limit)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    public String toUrlQueryString() {
        StringBuilder sb = new StringBuilder();

        Object rateLimitTypeValue = getRateLimitType();
        String rateLimitTypeValueAsString = "";
        rateLimitTypeValueAsString = rateLimitTypeValue.toString();
        sb.append("rateLimitType=").append(urlEncode(rateLimitTypeValueAsString)).append("");
        Object intervalValue = getInterval();
        String intervalValueAsString = "";
        intervalValueAsString = intervalValue.toString();
        sb.append("interval=").append(urlEncode(intervalValueAsString)).append("");
        Object intervalNumValue = getIntervalNum();
        String intervalNumValueAsString = "";
        intervalNumValueAsString = intervalNumValue.toString();
        sb.append("intervalNum=").append(urlEncode(intervalNumValueAsString)).append("");
        Object limitValue = getLimit();
        String limitValueAsString = "";
        limitValueAsString = limitValue.toString();
        sb.append("limit=").append(urlEncode(limitValueAsString)).append("");
        return sb.toString();
    }

    public static String urlEncode(String s) {
        try {
            return URLEncoder.encode(s, StandardCharsets.UTF_8.name());
        } catch (UnsupportedEncodingException e) {
            throw new RuntimeException(StandardCharsets.UTF_8.name() + " is unsupported", e);
        }
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces (except the first
     * line).
     */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n		");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("rateLimitType");
        openapiFields.add("interval");
        openapiFields.add("intervalNum");
        openapiFields.add("limit");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to
     *     ExchangeInfoResponseRateLimitsInner
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!ExchangeInfoResponseRateLimitsInner.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                "The required field(s) %s in ExchangeInfoResponseRateLimitsInner is"
                                        + " not found in the empty JSON string",
                                ExchangeInfoResponseRateLimitsInner.openapiRequiredFields
                                        .toString()));
            }
        }

        Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
        // check to see if the JSON string contains additional fields
        for (Map.Entry<String, JsonElement> entry : entries) {
            if (!ExchangeInfoResponseRateLimitsInner.openapiFields.contains(entry.getKey())) {
                throw new IllegalArgumentException(
                        String.format(
                                "The field `%s` in the JSON string is not defined in the"
                                    + " `ExchangeInfoResponseRateLimitsInner` properties. JSON: %s",
                                entry.getKey(), jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if ((jsonObj.get("rateLimitType") != null && !jsonObj.get("rateLimitType").isJsonNull())
                && !jsonObj.get("rateLimitType").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            "Expected the field `rateLimitType` to be a primitive type in the JSON"
                                    + " string but got `%s`",
                            jsonObj.get("rateLimitType").toString()));
        }
        if ((jsonObj.get("interval") != null && !jsonObj.get("interval").isJsonNull())
                && !jsonObj.get("interval").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            "Expected the field `interval` to be a primitive type in the JSON"
                                    + " string but got `%s`",
                            jsonObj.get("interval").toString()));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!ExchangeInfoResponseRateLimitsInner.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'ExchangeInfoResponseRateLimitsInner' and
                // its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<ExchangeInfoResponseRateLimitsInner> thisAdapter =
                    gson.getDelegateAdapter(
                            this, TypeToken.get(ExchangeInfoResponseRateLimitsInner.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<ExchangeInfoResponseRateLimitsInner>() {
                        @Override
                        public void write(JsonWriter out, ExchangeInfoResponseRateLimitsInner value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public ExchangeInfoResponseRateLimitsInner read(JsonReader in)
                                throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            // validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of ExchangeInfoResponseRateLimitsInner given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of ExchangeInfoResponseRateLimitsInner
     * @throws IOException if the JSON string is invalid with respect to
     *     ExchangeInfoResponseRateLimitsInner
     */
    public static ExchangeInfoResponseRateLimitsInner fromJson(String jsonString)
            throws IOException {
        return JSON.getGson().fromJson(jsonString, ExchangeInfoResponseRateLimitsInner.class);
    }

    /**
     * Convert an instance of ExchangeInfoResponseRateLimitsInner to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
