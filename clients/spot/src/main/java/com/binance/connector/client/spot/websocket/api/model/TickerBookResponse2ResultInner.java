/*
 * Spot WebSocket API
 * Access market data, manage accounts, and trade on Binance Spot.
 *
 * The version of the OpenAPI document: 1.0.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package com.binance.connector.client.spot.websocket.api.model;

import com.binance.connector.client.common.websocket.dtos.BaseDTO;
import com.binance.connector.client.spot.websocket.api.JSON;
import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import jakarta.validation.constraints.*;
import java.io.IOException;
import java.nio.charset.StandardCharsets;
import java.util.HashSet;
import java.util.Map;
import java.util.Objects;
import java.util.Set;
import java.util.TreeMap;
import java.util.stream.Collectors;
import org.hibernate.validator.constraints.*;

/** TickerBookResponse2ResultInner */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class TickerBookResponse2ResultInner extends BaseDTO {
    public static final String SERIALIZED_NAME_SYMBOL = "symbol";

    @SerializedName(SERIALIZED_NAME_SYMBOL)
    @jakarta.annotation.Nullable
    private String symbol;

    public static final String SERIALIZED_NAME_BID_PRICE = "bidPrice";

    @SerializedName(SERIALIZED_NAME_BID_PRICE)
    @jakarta.annotation.Nullable
    private String bidPrice;

    public static final String SERIALIZED_NAME_BID_QTY = "bidQty";

    @SerializedName(SERIALIZED_NAME_BID_QTY)
    @jakarta.annotation.Nullable
    private String bidQty;

    public static final String SERIALIZED_NAME_ASK_PRICE = "askPrice";

    @SerializedName(SERIALIZED_NAME_ASK_PRICE)
    @jakarta.annotation.Nullable
    private String askPrice;

    public static final String SERIALIZED_NAME_ASK_QTY = "askQty";

    @SerializedName(SERIALIZED_NAME_ASK_QTY)
    @jakarta.annotation.Nullable
    private String askQty;

    public TickerBookResponse2ResultInner() {}

    public TickerBookResponse2ResultInner symbol(@jakarta.annotation.Nullable String symbol) {
        this.symbol = symbol;
        return this;
    }

    /**
     * Get symbol
     *
     * @return symbol
     */
    @jakarta.annotation.Nullable
    public String getSymbol() {
        return symbol;
    }

    public void setSymbol(@jakarta.annotation.Nullable String symbol) {
        this.symbol = symbol;
    }

    public TickerBookResponse2ResultInner bidPrice(@jakarta.annotation.Nullable String bidPrice) {
        this.bidPrice = bidPrice;
        return this;
    }

    /**
     * best bid price.
     *
     * @return bidPrice
     */
    @jakarta.annotation.Nullable
    public String getBidPrice() {
        return bidPrice;
    }

    public void setBidPrice(@jakarta.annotation.Nullable String bidPrice) {
        this.bidPrice = bidPrice;
    }

    public TickerBookResponse2ResultInner bidQty(@jakarta.annotation.Nullable String bidQty) {
        this.bidQty = bidQty;
        return this;
    }

    /**
     * bid/ask qty.
     *
     * @return bidQty
     */
    @jakarta.annotation.Nullable
    public String getBidQty() {
        return bidQty;
    }

    public void setBidQty(@jakarta.annotation.Nullable String bidQty) {
        this.bidQty = bidQty;
    }

    public TickerBookResponse2ResultInner askPrice(@jakarta.annotation.Nullable String askPrice) {
        this.askPrice = askPrice;
        return this;
    }

    /**
     * best ask price.
     *
     * @return askPrice
     */
    @jakarta.annotation.Nullable
    public String getAskPrice() {
        return askPrice;
    }

    public void setAskPrice(@jakarta.annotation.Nullable String askPrice) {
        this.askPrice = askPrice;
    }

    public TickerBookResponse2ResultInner askQty(@jakarta.annotation.Nullable String askQty) {
        this.askQty = askQty;
        return this;
    }

    /**
     * bid/ask qty.
     *
     * @return askQty
     */
    @jakarta.annotation.Nullable
    public String getAskQty() {
        return askQty;
    }

    public void setAskQty(@jakarta.annotation.Nullable String askQty) {
        this.askQty = askQty;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        TickerBookResponse2ResultInner tickerBookResponse2ResultInner =
                (TickerBookResponse2ResultInner) o;
        return Objects.equals(this.symbol, tickerBookResponse2ResultInner.symbol)
                && Objects.equals(this.bidPrice, tickerBookResponse2ResultInner.bidPrice)
                && Objects.equals(this.bidQty, tickerBookResponse2ResultInner.bidQty)
                && Objects.equals(this.askPrice, tickerBookResponse2ResultInner.askPrice)
                && Objects.equals(this.askQty, tickerBookResponse2ResultInner.askQty);
    }

    @Override
    public int hashCode() {
        return Objects.hash(symbol, bidPrice, bidQty, askPrice, askQty);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class TickerBookResponse2ResultInner {\n");
        sb.append("		symbol: ").append(toIndentedString(symbol)).append("\n");
        sb.append("		bidPrice: ").append(toIndentedString(bidPrice)).append("\n");
        sb.append("		bidQty: ").append(toIndentedString(bidQty)).append("\n");
        sb.append("		askPrice: ").append(toIndentedString(askPrice)).append("\n");
        sb.append("		askQty: ").append(toIndentedString(askQty)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    public String toUrlQueryString() {
        StringBuilder sb = new StringBuilder();
        Map<String, String> valMap = new TreeMap<String, String>();
        valMap.put("apiKey", getApiKey());
        String symbolValue = getSymbol();
        if (symbolValue != null) {
            String symbolValueAsString = symbolValue.toString();
            valMap.put("symbol", symbolValueAsString);
        }
        String bidPriceValue = getBidPrice();
        if (bidPriceValue != null) {
            String bidPriceValueAsString = bidPriceValue.toString();
            valMap.put("bidPrice", bidPriceValueAsString);
        }
        String bidQtyValue = getBidQty();
        if (bidQtyValue != null) {
            String bidQtyValueAsString = bidQtyValue.toString();
            valMap.put("bidQty", bidQtyValueAsString);
        }
        String askPriceValue = getAskPrice();
        if (askPriceValue != null) {
            String askPriceValueAsString = askPriceValue.toString();
            valMap.put("askPrice", askPriceValueAsString);
        }
        String askQtyValue = getAskQty();
        if (askQtyValue != null) {
            String askQtyValueAsString = askQtyValue.toString();
            valMap.put("askQty", askQtyValueAsString);
        }

        valMap.put("timestamp", getTimestamp());
        return asciiEncode(
                valMap.keySet().stream()
                        .map(key -> key + "=" + valMap.get(key))
                        .collect(Collectors.joining("&")));
    }

    public Map<String, Object> toMap() {
        Map<String, Object> valMap = new TreeMap<String, Object>();
        valMap.put("apiKey", getApiKey());
        Object symbolValue = getSymbol();
        if (symbolValue != null) {
            valMap.put("symbol", symbolValue);
        }
        Object bidPriceValue = getBidPrice();
        if (bidPriceValue != null) {
            valMap.put("bidPrice", bidPriceValue);
        }
        Object bidQtyValue = getBidQty();
        if (bidQtyValue != null) {
            valMap.put("bidQty", bidQtyValue);
        }
        Object askPriceValue = getAskPrice();
        if (askPriceValue != null) {
            valMap.put("askPrice", askPriceValue);
        }
        Object askQtyValue = getAskQty();
        if (askQtyValue != null) {
            valMap.put("askQty", askQtyValue);
        }

        valMap.put("timestamp", getTimestamp());
        return valMap;
    }

    public static String asciiEncode(String s) {
        return new String(s.getBytes(), StandardCharsets.US_ASCII);
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces (except the first
     * line).
     */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n		");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("symbol");
        openapiFields.add("bidPrice");
        openapiFields.add("bidQty");
        openapiFields.add("askPrice");
        openapiFields.add("askQty");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to
     *     TickerBookResponse2ResultInner
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!TickerBookResponse2ResultInner.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                "The required field(s) %s in TickerBookResponse2ResultInner is not"
                                        + " found in the empty JSON string",
                                TickerBookResponse2ResultInner.openapiRequiredFields.toString()));
            }
        }

        Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
        // check to see if the JSON string contains additional fields
        for (Map.Entry<String, JsonElement> entry : entries) {
            if (!TickerBookResponse2ResultInner.openapiFields.contains(entry.getKey())) {
                throw new IllegalArgumentException(
                        String.format(
                                "The field `%s` in the JSON string is not defined in the"
                                        + " `TickerBookResponse2ResultInner` properties. JSON: %s",
                                entry.getKey(), jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if ((jsonObj.get("symbol") != null && !jsonObj.get("symbol").isJsonNull())
                && !jsonObj.get("symbol").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            "Expected the field `symbol` to be a primitive type in the JSON string"
                                    + " but got `%s`",
                            jsonObj.get("symbol").toString()));
        }
        if ((jsonObj.get("bidPrice") != null && !jsonObj.get("bidPrice").isJsonNull())
                && !jsonObj.get("bidPrice").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            "Expected the field `bidPrice` to be a primitive type in the JSON"
                                    + " string but got `%s`",
                            jsonObj.get("bidPrice").toString()));
        }
        if ((jsonObj.get("bidQty") != null && !jsonObj.get("bidQty").isJsonNull())
                && !jsonObj.get("bidQty").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            "Expected the field `bidQty` to be a primitive type in the JSON string"
                                    + " but got `%s`",
                            jsonObj.get("bidQty").toString()));
        }
        if ((jsonObj.get("askPrice") != null && !jsonObj.get("askPrice").isJsonNull())
                && !jsonObj.get("askPrice").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            "Expected the field `askPrice` to be a primitive type in the JSON"
                                    + " string but got `%s`",
                            jsonObj.get("askPrice").toString()));
        }
        if ((jsonObj.get("askQty") != null && !jsonObj.get("askQty").isJsonNull())
                && !jsonObj.get("askQty").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            "Expected the field `askQty` to be a primitive type in the JSON string"
                                    + " but got `%s`",
                            jsonObj.get("askQty").toString()));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!TickerBookResponse2ResultInner.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'TickerBookResponse2ResultInner' and its
                // subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<TickerBookResponse2ResultInner> thisAdapter =
                    gson.getDelegateAdapter(
                            this, TypeToken.get(TickerBookResponse2ResultInner.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<TickerBookResponse2ResultInner>() {
                        @Override
                        public void write(JsonWriter out, TickerBookResponse2ResultInner value)
                                throws IOException {
                            JsonElement obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public TickerBookResponse2ResultInner read(JsonReader in)
                                throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            // validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of TickerBookResponse2ResultInner given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of TickerBookResponse2ResultInner
     * @throws IOException if the JSON string is invalid with respect to
     *     TickerBookResponse2ResultInner
     */
    public static TickerBookResponse2ResultInner fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, TickerBookResponse2ResultInner.class);
    }

    /**
     * Convert an instance of TickerBookResponse2ResultInner to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
