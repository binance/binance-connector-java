/*
 * Binance Spot REST API
 * OpenAPI Specifications for the Binance Spot REST API  API documents:   - [Github rest-api documentation file](https://github.com/binance/binance-spot-api-docs/blob/master/rest-api.md)   - [General API information for rest-api on website](https://developers.binance.com/docs/binance-spot-api-docs/rest-api/general-api-information)
 *
 * The version of the OpenAPI document: 1.0.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package com.binance.connector.client.spot.rest.model;

import com.binance.connector.client.spot.rest.JSON;
import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import jakarta.validation.Valid;
import jakarta.validation.constraints.*;
import java.io.IOException;
import java.io.UnsupportedEncodingException;
import java.net.URLEncoder;
import java.nio.charset.StandardCharsets;
import java.util.HashSet;
import java.util.Map;
import java.util.Objects;
import java.util.Set;
import org.hibernate.validator.constraints.*;

/** OrderAmendKeepPriorityResponse1 */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.12.0")
public class OrderAmendKeepPriorityResponse1 {
    public static final String SERIALIZED_NAME_TRANSACT_TIME = "transactTime";

    @SerializedName(SERIALIZED_NAME_TRANSACT_TIME)
    @jakarta.annotation.Nullable
    private Long transactTime;

    public static final String SERIALIZED_NAME_EXECUTION_ID = "executionId";

    @SerializedName(SERIALIZED_NAME_EXECUTION_ID)
    @jakarta.annotation.Nullable
    private Long executionId;

    public static final String SERIALIZED_NAME_AMENDED_ORDER = "amendedOrder";

    @SerializedName(SERIALIZED_NAME_AMENDED_ORDER)
    @jakarta.annotation.Nullable
    private OrderAmendKeepPriorityResponse1AmendedOrder amendedOrder;

    public OrderAmendKeepPriorityResponse1() {}

    public OrderAmendKeepPriorityResponse1 transactTime(
            @jakarta.annotation.Nullable Long transactTime) {
        this.transactTime = transactTime;
        return this;
    }

    /**
     * Get transactTime
     *
     * @return transactTime
     */
    @jakarta.annotation.Nullable
    public Long getTransactTime() {
        return transactTime;
    }

    public void setTransactTime(@jakarta.annotation.Nullable Long transactTime) {
        this.transactTime = transactTime;
    }

    public OrderAmendKeepPriorityResponse1 executionId(
            @jakarta.annotation.Nullable Long executionId) {
        this.executionId = executionId;
        return this;
    }

    /**
     * Get executionId
     *
     * @return executionId
     */
    @jakarta.annotation.Nullable
    public Long getExecutionId() {
        return executionId;
    }

    public void setExecutionId(@jakarta.annotation.Nullable Long executionId) {
        this.executionId = executionId;
    }

    public OrderAmendKeepPriorityResponse1 amendedOrder(
            @jakarta.annotation.Nullable OrderAmendKeepPriorityResponse1AmendedOrder amendedOrder) {
        this.amendedOrder = amendedOrder;
        return this;
    }

    /**
     * Get amendedOrder
     *
     * @return amendedOrder
     */
    @jakarta.annotation.Nullable
    @Valid
    public OrderAmendKeepPriorityResponse1AmendedOrder getAmendedOrder() {
        return amendedOrder;
    }

    public void setAmendedOrder(
            @jakarta.annotation.Nullable OrderAmendKeepPriorityResponse1AmendedOrder amendedOrder) {
        this.amendedOrder = amendedOrder;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        OrderAmendKeepPriorityResponse1 orderAmendKeepPriorityResponse1 =
                (OrderAmendKeepPriorityResponse1) o;
        return Objects.equals(this.transactTime, orderAmendKeepPriorityResponse1.transactTime)
                && Objects.equals(this.executionId, orderAmendKeepPriorityResponse1.executionId)
                && Objects.equals(this.amendedOrder, orderAmendKeepPriorityResponse1.amendedOrder);
    }

    @Override
    public int hashCode() {
        return Objects.hash(transactTime, executionId, amendedOrder);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class OrderAmendKeepPriorityResponse1 {\n");
        sb.append("		transactTime: ").append(toIndentedString(transactTime)).append("\n");
        sb.append("		executionId: ").append(toIndentedString(executionId)).append("\n");
        sb.append("		amendedOrder: ").append(toIndentedString(amendedOrder)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    public String toUrlQueryString() {
        StringBuilder sb = new StringBuilder();

        Object transactTimeValue = getTransactTime();
        String transactTimeValueAsString = "";
        transactTimeValueAsString = transactTimeValue.toString();
        sb.append("transactTime=").append(urlEncode(transactTimeValueAsString)).append("");
        Object executionIdValue = getExecutionId();
        String executionIdValueAsString = "";
        executionIdValueAsString = executionIdValue.toString();
        sb.append("executionId=").append(urlEncode(executionIdValueAsString)).append("");
        Object amendedOrderValue = getAmendedOrder();
        String amendedOrderValueAsString = "";
        amendedOrderValueAsString = amendedOrderValue.toString();
        sb.append("amendedOrder=").append(urlEncode(amendedOrderValueAsString)).append("");
        return sb.toString();
    }

    public static String urlEncode(String s) {
        try {
            return URLEncoder.encode(s, StandardCharsets.UTF_8.name());
        } catch (UnsupportedEncodingException e) {
            throw new RuntimeException(StandardCharsets.UTF_8.name() + " is unsupported", e);
        }
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces (except the first
     * line).
     */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n		");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("transactTime");
        openapiFields.add("executionId");
        openapiFields.add("amendedOrder");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to
     *     OrderAmendKeepPriorityResponse1
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!OrderAmendKeepPriorityResponse1.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                "The required field(s) %s in OrderAmendKeepPriorityResponse1 is not"
                                        + " found in the empty JSON string",
                                OrderAmendKeepPriorityResponse1.openapiRequiredFields.toString()));
            }
        }

        Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
        // check to see if the JSON string contains additional fields
        for (Map.Entry<String, JsonElement> entry : entries) {
            if (!OrderAmendKeepPriorityResponse1.openapiFields.contains(entry.getKey())) {
                throw new IllegalArgumentException(
                        String.format(
                                "The field `%s` in the JSON string is not defined in the"
                                        + " `OrderAmendKeepPriorityResponse1` properties. JSON: %s",
                                entry.getKey(), jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        // validate the optional field `amendedOrder`
        if (jsonObj.get("amendedOrder") != null && !jsonObj.get("amendedOrder").isJsonNull()) {
            OrderAmendKeepPriorityResponse1AmendedOrder.validateJsonElement(
                    jsonObj.get("amendedOrder"));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!OrderAmendKeepPriorityResponse1.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'OrderAmendKeepPriorityResponse1' and its
                // subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<OrderAmendKeepPriorityResponse1> thisAdapter =
                    gson.getDelegateAdapter(
                            this, TypeToken.get(OrderAmendKeepPriorityResponse1.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<OrderAmendKeepPriorityResponse1>() {
                        @Override
                        public void write(JsonWriter out, OrderAmendKeepPriorityResponse1 value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public OrderAmendKeepPriorityResponse1 read(JsonReader in)
                                throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            // validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of OrderAmendKeepPriorityResponse1 given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of OrderAmendKeepPriorityResponse1
     * @throws IOException if the JSON string is invalid with respect to
     *     OrderAmendKeepPriorityResponse1
     */
    public static OrderAmendKeepPriorityResponse1 fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, OrderAmendKeepPriorityResponse1.class);
    }

    /**
     * Convert an instance of OrderAmendKeepPriorityResponse1 to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
