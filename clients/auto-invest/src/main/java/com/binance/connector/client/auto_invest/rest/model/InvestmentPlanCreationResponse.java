/*
 * Binance Auto Invest REST API
 * OpenAPI Specification for the Binance Auto Invest REST API
 *
 * The version of the OpenAPI document: 1.0.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package com.binance.connector.client.auto_invest.rest.model;

import com.binance.connector.client.auto_invest.rest.JSON;
import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import jakarta.validation.constraints.*;
import java.io.IOException;
import java.io.UnsupportedEncodingException;
import java.net.URLEncoder;
import java.nio.charset.StandardCharsets;
import java.util.HashSet;
import java.util.Map;
import java.util.Objects;
import java.util.Set;
import org.hibernate.validator.constraints.*;

/** InvestmentPlanCreationResponse */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.12.0")
public class InvestmentPlanCreationResponse {
    public static final String SERIALIZED_NAME_PLAN_ID = "planId";

    @SerializedName(SERIALIZED_NAME_PLAN_ID)
    @jakarta.annotation.Nullable
    private Long planId;

    public static final String SERIALIZED_NAME_NEXT_EXECUTION_DATE_TIME = "nextExecutionDateTime";

    @SerializedName(SERIALIZED_NAME_NEXT_EXECUTION_DATE_TIME)
    @jakarta.annotation.Nullable
    private Long nextExecutionDateTime;

    public InvestmentPlanCreationResponse() {}

    public InvestmentPlanCreationResponse planId(@jakarta.annotation.Nullable Long planId) {
        this.planId = planId;
        return this;
    }

    /**
     * Get planId
     *
     * @return planId
     */
    @jakarta.annotation.Nullable
    public Long getPlanId() {
        return planId;
    }

    public void setPlanId(@jakarta.annotation.Nullable Long planId) {
        this.planId = planId;
    }

    public InvestmentPlanCreationResponse nextExecutionDateTime(
            @jakarta.annotation.Nullable Long nextExecutionDateTime) {
        this.nextExecutionDateTime = nextExecutionDateTime;
        return this;
    }

    /**
     * Get nextExecutionDateTime
     *
     * @return nextExecutionDateTime
     */
    @jakarta.annotation.Nullable
    public Long getNextExecutionDateTime() {
        return nextExecutionDateTime;
    }

    public void setNextExecutionDateTime(@jakarta.annotation.Nullable Long nextExecutionDateTime) {
        this.nextExecutionDateTime = nextExecutionDateTime;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        InvestmentPlanCreationResponse investmentPlanCreationResponse =
                (InvestmentPlanCreationResponse) o;
        return Objects.equals(this.planId, investmentPlanCreationResponse.planId)
                && Objects.equals(
                        this.nextExecutionDateTime,
                        investmentPlanCreationResponse.nextExecutionDateTime);
    }

    @Override
    public int hashCode() {
        return Objects.hash(planId, nextExecutionDateTime);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class InvestmentPlanCreationResponse {\n");
        sb.append("		planId: ").append(toIndentedString(planId)).append("\n");
        sb.append("		nextExecutionDateTime: ")
                .append(toIndentedString(nextExecutionDateTime))
                .append("\n");
        sb.append("}");
        return sb.toString();
    }

    public String toUrlQueryString() {
        StringBuilder sb = new StringBuilder();

        Object planIdValue = getPlanId();
        String planIdValueAsString = "";
        planIdValueAsString = planIdValue.toString();
        sb.append("planId=").append(urlEncode(planIdValueAsString)).append("");
        Object nextExecutionDateTimeValue = getNextExecutionDateTime();
        String nextExecutionDateTimeValueAsString = "";
        nextExecutionDateTimeValueAsString = nextExecutionDateTimeValue.toString();
        sb.append("nextExecutionDateTime=")
                .append(urlEncode(nextExecutionDateTimeValueAsString))
                .append("");
        return sb.toString();
    }

    public static String urlEncode(String s) {
        try {
            return URLEncoder.encode(s, StandardCharsets.UTF_8.name());
        } catch (UnsupportedEncodingException e) {
            throw new RuntimeException(StandardCharsets.UTF_8.name() + " is unsupported", e);
        }
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces (except the first
     * line).
     */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n		");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("planId");
        openapiFields.add("nextExecutionDateTime");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to
     *     InvestmentPlanCreationResponse
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!InvestmentPlanCreationResponse.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                "The required field(s) %s in InvestmentPlanCreationResponse is not"
                                        + " found in the empty JSON string",
                                InvestmentPlanCreationResponse.openapiRequiredFields.toString()));
            }
        }

        Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
        // check to see if the JSON string contains additional fields
        for (Map.Entry<String, JsonElement> entry : entries) {
            if (!InvestmentPlanCreationResponse.openapiFields.contains(entry.getKey())) {
                throw new IllegalArgumentException(
                        String.format(
                                "The field `%s` in the JSON string is not defined in the"
                                        + " `InvestmentPlanCreationResponse` properties. JSON: %s",
                                entry.getKey(), jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!InvestmentPlanCreationResponse.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'InvestmentPlanCreationResponse' and its
                // subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<InvestmentPlanCreationResponse> thisAdapter =
                    gson.getDelegateAdapter(
                            this, TypeToken.get(InvestmentPlanCreationResponse.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<InvestmentPlanCreationResponse>() {
                        @Override
                        public void write(JsonWriter out, InvestmentPlanCreationResponse value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public InvestmentPlanCreationResponse read(JsonReader in)
                                throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            // validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of InvestmentPlanCreationResponse given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of InvestmentPlanCreationResponse
     * @throws IOException if the JSON string is invalid with respect to
     *     InvestmentPlanCreationResponse
     */
    public static InvestmentPlanCreationResponse fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, InvestmentPlanCreationResponse.class);
    }

    /**
     * Convert an instance of InvestmentPlanCreationResponse to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
