/*
 * Binance Wallet REST API
 * OpenAPI Specification for the Binance Wallet REST API
 *
 * The version of the OpenAPI document: 1.0.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package com.binance.connector.client.wallet.rest.model;

import com.binance.connector.client.wallet.rest.JSON;
import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import jakarta.validation.constraints.*;
import java.io.IOException;
import java.io.UnsupportedEncodingException;
import java.net.URLEncoder;
import java.nio.charset.StandardCharsets;
import java.util.HashSet;
import java.util.Map;
import java.util.Objects;
import java.util.Set;
import org.hibernate.validator.constraints.*;

/** FetchWithdrawQuotaResponse */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.12.0")
public class FetchWithdrawQuotaResponse {
    public static final String SERIALIZED_NAME_WD_QUOTA = "wdQuota";

    @SerializedName(SERIALIZED_NAME_WD_QUOTA)
    @jakarta.annotation.Nullable
    private String wdQuota;

    public static final String SERIALIZED_NAME_USED_WD_QUOTA = "usedWdQuota";

    @SerializedName(SERIALIZED_NAME_USED_WD_QUOTA)
    @jakarta.annotation.Nullable
    private String usedWdQuota;

    public FetchWithdrawQuotaResponse() {}

    public FetchWithdrawQuotaResponse wdQuota(@jakarta.annotation.Nullable String wdQuota) {
        this.wdQuota = wdQuota;
        return this;
    }

    /**
     * Get wdQuota
     *
     * @return wdQuota
     */
    @jakarta.annotation.Nullable
    public String getWdQuota() {
        return wdQuota;
    }

    public void setWdQuota(@jakarta.annotation.Nullable String wdQuota) {
        this.wdQuota = wdQuota;
    }

    public FetchWithdrawQuotaResponse usedWdQuota(@jakarta.annotation.Nullable String usedWdQuota) {
        this.usedWdQuota = usedWdQuota;
        return this;
    }

    /**
     * Get usedWdQuota
     *
     * @return usedWdQuota
     */
    @jakarta.annotation.Nullable
    public String getUsedWdQuota() {
        return usedWdQuota;
    }

    public void setUsedWdQuota(@jakarta.annotation.Nullable String usedWdQuota) {
        this.usedWdQuota = usedWdQuota;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        FetchWithdrawQuotaResponse fetchWithdrawQuotaResponse = (FetchWithdrawQuotaResponse) o;
        return Objects.equals(this.wdQuota, fetchWithdrawQuotaResponse.wdQuota)
                && Objects.equals(this.usedWdQuota, fetchWithdrawQuotaResponse.usedWdQuota);
    }

    @Override
    public int hashCode() {
        return Objects.hash(wdQuota, usedWdQuota);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class FetchWithdrawQuotaResponse {\n");
        sb.append("		wdQuota: ").append(toIndentedString(wdQuota)).append("\n");
        sb.append("		usedWdQuota: ").append(toIndentedString(usedWdQuota)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    public String toUrlQueryString() {
        StringBuilder sb = new StringBuilder();

        Object wdQuotaValue = getWdQuota();
        String wdQuotaValueAsString = "";
        wdQuotaValueAsString = wdQuotaValue.toString();
        sb.append("wdQuota=").append(urlEncode(wdQuotaValueAsString)).append("");
        Object usedWdQuotaValue = getUsedWdQuota();
        String usedWdQuotaValueAsString = "";
        usedWdQuotaValueAsString = usedWdQuotaValue.toString();
        sb.append("usedWdQuota=").append(urlEncode(usedWdQuotaValueAsString)).append("");
        return sb.toString();
    }

    public static String urlEncode(String s) {
        try {
            return URLEncoder.encode(s, StandardCharsets.UTF_8.name());
        } catch (UnsupportedEncodingException e) {
            throw new RuntimeException(StandardCharsets.UTF_8.name() + " is unsupported", e);
        }
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces (except the first
     * line).
     */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n		");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("wdQuota");
        openapiFields.add("usedWdQuota");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to FetchWithdrawQuotaResponse
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!FetchWithdrawQuotaResponse.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                "The required field(s) %s in FetchWithdrawQuotaResponse is not"
                                        + " found in the empty JSON string",
                                FetchWithdrawQuotaResponse.openapiRequiredFields.toString()));
            }
        }

        Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
        // check to see if the JSON string contains additional fields
        for (Map.Entry<String, JsonElement> entry : entries) {
            if (!FetchWithdrawQuotaResponse.openapiFields.contains(entry.getKey())) {
                throw new IllegalArgumentException(
                        String.format(
                                "The field `%s` in the JSON string is not defined in the"
                                        + " `FetchWithdrawQuotaResponse` properties. JSON: %s",
                                entry.getKey(), jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if ((jsonObj.get("wdQuota") != null && !jsonObj.get("wdQuota").isJsonNull())
                && !jsonObj.get("wdQuota").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            "Expected the field `wdQuota` to be a primitive type in the JSON string"
                                    + " but got `%s`",
                            jsonObj.get("wdQuota").toString()));
        }
        if ((jsonObj.get("usedWdQuota") != null && !jsonObj.get("usedWdQuota").isJsonNull())
                && !jsonObj.get("usedWdQuota").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            "Expected the field `usedWdQuota` to be a primitive type in the JSON"
                                    + " string but got `%s`",
                            jsonObj.get("usedWdQuota").toString()));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!FetchWithdrawQuotaResponse.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'FetchWithdrawQuotaResponse' and its
                // subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<FetchWithdrawQuotaResponse> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(FetchWithdrawQuotaResponse.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<FetchWithdrawQuotaResponse>() {
                        @Override
                        public void write(JsonWriter out, FetchWithdrawQuotaResponse value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public FetchWithdrawQuotaResponse read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            // validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of FetchWithdrawQuotaResponse given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of FetchWithdrawQuotaResponse
     * @throws IOException if the JSON string is invalid with respect to FetchWithdrawQuotaResponse
     */
    public static FetchWithdrawQuotaResponse fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, FetchWithdrawQuotaResponse.class);
    }

    /**
     * Convert an instance of FetchWithdrawQuotaResponse to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
