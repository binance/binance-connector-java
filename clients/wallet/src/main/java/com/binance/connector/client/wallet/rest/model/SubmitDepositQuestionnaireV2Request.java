/*
 * Binance Wallet REST API
 * OpenAPI Specification for the Binance Wallet REST API
 *
 * The version of the OpenAPI document: 1.0.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package com.binance.connector.client.wallet.rest.model;

import com.binance.connector.client.wallet.rest.JSON;
import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import jakarta.validation.constraints.*;
import java.io.IOException;
import java.io.UnsupportedEncodingException;
import java.net.URLEncoder;
import java.nio.charset.StandardCharsets;
import java.util.HashSet;
import java.util.Objects;
import org.hibernate.validator.constraints.*;

/** SubmitDepositQuestionnaireV2Request */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.12.0")
public class SubmitDepositQuestionnaireV2Request {
    public static final String SERIALIZED_NAME_DEPOSIT_ID = "depositId";

    @SerializedName(SERIALIZED_NAME_DEPOSIT_ID)
    @jakarta.annotation.Nonnull
    private Long depositId;

    public static final String SERIALIZED_NAME_QUESTIONNAIRE = "questionnaire";

    @SerializedName(SERIALIZED_NAME_QUESTIONNAIRE)
    @jakarta.annotation.Nonnull
    private String questionnaire;

    public SubmitDepositQuestionnaireV2Request() {}

    public SubmitDepositQuestionnaireV2Request depositId(
            @jakarta.annotation.Nonnull Long depositId) {
        this.depositId = depositId;
        return this;
    }

    /**
     * Get depositId
     *
     * @return depositId
     */
    @jakarta.annotation.Nonnull
    @NotNull
    public Long getDepositId() {
        return depositId;
    }

    public void setDepositId(@jakarta.annotation.Nonnull Long depositId) {
        this.depositId = depositId;
    }

    public SubmitDepositQuestionnaireV2Request questionnaire(
            @jakarta.annotation.Nonnull String questionnaire) {
        this.questionnaire = questionnaire;
        return this;
    }

    /**
     * Get questionnaire
     *
     * @return questionnaire
     */
    @jakarta.annotation.Nonnull
    @NotNull
    public String getQuestionnaire() {
        return questionnaire;
    }

    public void setQuestionnaire(@jakarta.annotation.Nonnull String questionnaire) {
        this.questionnaire = questionnaire;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        SubmitDepositQuestionnaireV2Request submitDepositQuestionnaireV2Request =
                (SubmitDepositQuestionnaireV2Request) o;
        return Objects.equals(this.depositId, submitDepositQuestionnaireV2Request.depositId)
                && Objects.equals(
                        this.questionnaire, submitDepositQuestionnaireV2Request.questionnaire);
    }

    @Override
    public int hashCode() {
        return Objects.hash(depositId, questionnaire);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class SubmitDepositQuestionnaireV2Request {\n");
        sb.append("		depositId: ").append(toIndentedString(depositId)).append("\n");
        sb.append("		questionnaire: ").append(toIndentedString(questionnaire)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    public String toUrlQueryString() {
        StringBuilder sb = new StringBuilder();

        Object depositIdValue = getDepositId();
        String depositIdValueAsString = "";
        depositIdValueAsString = depositIdValue.toString();
        sb.append("depositId=").append(urlEncode(depositIdValueAsString)).append("");
        Object questionnaireValue = getQuestionnaire();
        String questionnaireValueAsString = "";
        questionnaireValueAsString = questionnaireValue.toString();
        sb.append("questionnaire=").append(urlEncode(questionnaireValueAsString)).append("");
        return sb.toString();
    }

    public static String urlEncode(String s) {
        try {
            return URLEncoder.encode(s, StandardCharsets.UTF_8.name());
        } catch (UnsupportedEncodingException e) {
            throw new RuntimeException(StandardCharsets.UTF_8.name() + " is unsupported", e);
        }
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces (except the first
     * line).
     */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n		");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("depositId");
        openapiFields.add("questionnaire");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
        openapiRequiredFields.add("depositId");
        openapiRequiredFields.add("questionnaire");
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to
     *     SubmitDepositQuestionnaireV2Request
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!SubmitDepositQuestionnaireV2Request.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                "The required field(s) %s in SubmitDepositQuestionnaireV2Request is"
                                        + " not found in the empty JSON string",
                                SubmitDepositQuestionnaireV2Request.openapiRequiredFields
                                        .toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : SubmitDepositQuestionnaireV2Request.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(
                        String.format(
                                "The required field `%s` is not found in the JSON string: %s",
                                requiredField, jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if (!jsonObj.get("questionnaire").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            "Expected the field `questionnaire` to be a primitive type in the JSON"
                                    + " string but got `%s`",
                            jsonObj.get("questionnaire").toString()));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!SubmitDepositQuestionnaireV2Request.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'SubmitDepositQuestionnaireV2Request' and
                // its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<SubmitDepositQuestionnaireV2Request> thisAdapter =
                    gson.getDelegateAdapter(
                            this, TypeToken.get(SubmitDepositQuestionnaireV2Request.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<SubmitDepositQuestionnaireV2Request>() {
                        @Override
                        public void write(JsonWriter out, SubmitDepositQuestionnaireV2Request value)
                                throws IOException {
                            JsonElement obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public SubmitDepositQuestionnaireV2Request read(JsonReader in)
                                throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            // validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of SubmitDepositQuestionnaireV2Request given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of SubmitDepositQuestionnaireV2Request
     * @throws IOException if the JSON string is invalid with respect to
     *     SubmitDepositQuestionnaireV2Request
     */
    public static SubmitDepositQuestionnaireV2Request fromJson(String jsonString)
            throws IOException {
        return JSON.getGson().fromJson(jsonString, SubmitDepositQuestionnaireV2Request.class);
    }

    /**
     * Convert an instance of SubmitDepositQuestionnaireV2Request to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
