/*
 * Binance Wallet REST API
 * OpenAPI Specification for the Binance Wallet REST API
 *
 * The version of the OpenAPI document: 1.0.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package com.binance.connector.client.wallet.rest.model;

import com.binance.connector.client.wallet.rest.JSON;
import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import jakarta.validation.constraints.*;
import java.io.IOException;
import java.io.UnsupportedEncodingException;
import java.net.URLEncoder;
import java.nio.charset.StandardCharsets;
import java.util.HashSet;
import java.util.Map;
import java.util.Objects;
import java.util.Set;
import org.hibernate.validator.constraints.*;

/** ToggleBnbBurnOnSpotTradeAndMarginInterestResponse */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.12.0")
public class ToggleBnbBurnOnSpotTradeAndMarginInterestResponse {
    public static final String SERIALIZED_NAME_SPOT_B_N_B_BURN = "spotBNBBurn";

    @SerializedName(SERIALIZED_NAME_SPOT_B_N_B_BURN)
    @jakarta.annotation.Nullable
    private Boolean spotBNBBurn;

    public static final String SERIALIZED_NAME_INTEREST_B_N_B_BURN = "interestBNBBurn";

    @SerializedName(SERIALIZED_NAME_INTEREST_B_N_B_BURN)
    @jakarta.annotation.Nullable
    private Boolean interestBNBBurn;

    public ToggleBnbBurnOnSpotTradeAndMarginInterestResponse() {}

    public ToggleBnbBurnOnSpotTradeAndMarginInterestResponse spotBNBBurn(
            @jakarta.annotation.Nullable Boolean spotBNBBurn) {
        this.spotBNBBurn = spotBNBBurn;
        return this;
    }

    /**
     * Get spotBNBBurn
     *
     * @return spotBNBBurn
     */
    @jakarta.annotation.Nullable
    public Boolean getSpotBNBBurn() {
        return spotBNBBurn;
    }

    public void setSpotBNBBurn(@jakarta.annotation.Nullable Boolean spotBNBBurn) {
        this.spotBNBBurn = spotBNBBurn;
    }

    public ToggleBnbBurnOnSpotTradeAndMarginInterestResponse interestBNBBurn(
            @jakarta.annotation.Nullable Boolean interestBNBBurn) {
        this.interestBNBBurn = interestBNBBurn;
        return this;
    }

    /**
     * Get interestBNBBurn
     *
     * @return interestBNBBurn
     */
    @jakarta.annotation.Nullable
    public Boolean getInterestBNBBurn() {
        return interestBNBBurn;
    }

    public void setInterestBNBBurn(@jakarta.annotation.Nullable Boolean interestBNBBurn) {
        this.interestBNBBurn = interestBNBBurn;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        ToggleBnbBurnOnSpotTradeAndMarginInterestResponse
                toggleBnbBurnOnSpotTradeAndMarginInterestResponse =
                        (ToggleBnbBurnOnSpotTradeAndMarginInterestResponse) o;
        return Objects.equals(
                        this.spotBNBBurn,
                        toggleBnbBurnOnSpotTradeAndMarginInterestResponse.spotBNBBurn)
                && Objects.equals(
                        this.interestBNBBurn,
                        toggleBnbBurnOnSpotTradeAndMarginInterestResponse.interestBNBBurn);
    }

    @Override
    public int hashCode() {
        return Objects.hash(spotBNBBurn, interestBNBBurn);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class ToggleBnbBurnOnSpotTradeAndMarginInterestResponse {\n");
        sb.append("		spotBNBBurn: ").append(toIndentedString(spotBNBBurn)).append("\n");
        sb.append("		interestBNBBurn: ").append(toIndentedString(interestBNBBurn)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    public String toUrlQueryString() {
        StringBuilder sb = new StringBuilder();

        Object spotBNBBurnValue = getSpotBNBBurn();
        String spotBNBBurnValueAsString = "";
        spotBNBBurnValueAsString = spotBNBBurnValue.toString();
        sb.append("spotBNBBurn=").append(urlEncode(spotBNBBurnValueAsString)).append("");
        Object interestBNBBurnValue = getInterestBNBBurn();
        String interestBNBBurnValueAsString = "";
        interestBNBBurnValueAsString = interestBNBBurnValue.toString();
        sb.append("interestBNBBurn=").append(urlEncode(interestBNBBurnValueAsString)).append("");
        return sb.toString();
    }

    public static String urlEncode(String s) {
        try {
            return URLEncoder.encode(s, StandardCharsets.UTF_8.name());
        } catch (UnsupportedEncodingException e) {
            throw new RuntimeException(StandardCharsets.UTF_8.name() + " is unsupported", e);
        }
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces (except the first
     * line).
     */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n		");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("spotBNBBurn");
        openapiFields.add("interestBNBBurn");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to
     *     ToggleBnbBurnOnSpotTradeAndMarginInterestResponse
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!ToggleBnbBurnOnSpotTradeAndMarginInterestResponse.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                "The required field(s) %s in"
                                    + " ToggleBnbBurnOnSpotTradeAndMarginInterestResponse is not"
                                    + " found in the empty JSON string",
                                ToggleBnbBurnOnSpotTradeAndMarginInterestResponse
                                        .openapiRequiredFields
                                        .toString()));
            }
        }

        Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
        // check to see if the JSON string contains additional fields
        for (Map.Entry<String, JsonElement> entry : entries) {
            if (!ToggleBnbBurnOnSpotTradeAndMarginInterestResponse.openapiFields.contains(
                    entry.getKey())) {
                throw new IllegalArgumentException(
                        String.format(
                                "The field `%s` in the JSON string is not defined in the"
                                        + " `ToggleBnbBurnOnSpotTradeAndMarginInterestResponse`"
                                        + " properties. JSON: %s",
                                entry.getKey(), jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!ToggleBnbBurnOnSpotTradeAndMarginInterestResponse.class.isAssignableFrom(
                    type.getRawType())) {
                return null; // this class only serializes
                // 'ToggleBnbBurnOnSpotTradeAndMarginInterestResponse' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<ToggleBnbBurnOnSpotTradeAndMarginInterestResponse> thisAdapter =
                    gson.getDelegateAdapter(
                            this,
                            TypeToken.get(ToggleBnbBurnOnSpotTradeAndMarginInterestResponse.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<ToggleBnbBurnOnSpotTradeAndMarginInterestResponse>() {
                        @Override
                        public void write(
                                JsonWriter out,
                                ToggleBnbBurnOnSpotTradeAndMarginInterestResponse value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public ToggleBnbBurnOnSpotTradeAndMarginInterestResponse read(JsonReader in)
                                throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            // validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of ToggleBnbBurnOnSpotTradeAndMarginInterestResponse given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of ToggleBnbBurnOnSpotTradeAndMarginInterestResponse
     * @throws IOException if the JSON string is invalid with respect to
     *     ToggleBnbBurnOnSpotTradeAndMarginInterestResponse
     */
    public static ToggleBnbBurnOnSpotTradeAndMarginInterestResponse fromJson(String jsonString)
            throws IOException {
        return JSON.getGson()
                .fromJson(jsonString, ToggleBnbBurnOnSpotTradeAndMarginInterestResponse.class);
    }

    /**
     * Convert an instance of ToggleBnbBurnOnSpotTradeAndMarginInterestResponse to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
