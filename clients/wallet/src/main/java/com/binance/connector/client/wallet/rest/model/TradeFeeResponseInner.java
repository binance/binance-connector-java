/*
 * Binance Wallet REST API
 * OpenAPI Specification for the Binance Wallet REST API
 *
 * The version of the OpenAPI document: 1.0.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package com.binance.connector.client.wallet.rest.model;

import com.binance.connector.client.wallet.rest.JSON;
import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import jakarta.validation.constraints.*;
import java.io.IOException;
import java.io.UnsupportedEncodingException;
import java.net.URLEncoder;
import java.nio.charset.StandardCharsets;
import java.util.HashSet;
import java.util.Objects;
import org.hibernate.validator.constraints.*;

/** TradeFeeResponseInner */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.12.0")
public class TradeFeeResponseInner {
    public static final String SERIALIZED_NAME_SYMBOL = "symbol";

    @SerializedName(SERIALIZED_NAME_SYMBOL)
    @jakarta.annotation.Nullable
    private String symbol;

    public static final String SERIALIZED_NAME_MAKER_COMMISSION = "makerCommission";

    @SerializedName(SERIALIZED_NAME_MAKER_COMMISSION)
    @jakarta.annotation.Nullable
    private String makerCommission;

    public static final String SERIALIZED_NAME_TAKER_COMMISSION = "takerCommission";

    @SerializedName(SERIALIZED_NAME_TAKER_COMMISSION)
    @jakarta.annotation.Nullable
    private String takerCommission;

    public TradeFeeResponseInner() {}

    public TradeFeeResponseInner symbol(@jakarta.annotation.Nullable String symbol) {
        this.symbol = symbol;
        return this;
    }

    /**
     * Get symbol
     *
     * @return symbol
     */
    @jakarta.annotation.Nullable
    public String getSymbol() {
        return symbol;
    }

    public void setSymbol(@jakarta.annotation.Nullable String symbol) {
        this.symbol = symbol;
    }

    public TradeFeeResponseInner makerCommission(
            @jakarta.annotation.Nullable String makerCommission) {
        this.makerCommission = makerCommission;
        return this;
    }

    /**
     * Get makerCommission
     *
     * @return makerCommission
     */
    @jakarta.annotation.Nullable
    public String getMakerCommission() {
        return makerCommission;
    }

    public void setMakerCommission(@jakarta.annotation.Nullable String makerCommission) {
        this.makerCommission = makerCommission;
    }

    public TradeFeeResponseInner takerCommission(
            @jakarta.annotation.Nullable String takerCommission) {
        this.takerCommission = takerCommission;
        return this;
    }

    /**
     * Get takerCommission
     *
     * @return takerCommission
     */
    @jakarta.annotation.Nullable
    public String getTakerCommission() {
        return takerCommission;
    }

    public void setTakerCommission(@jakarta.annotation.Nullable String takerCommission) {
        this.takerCommission = takerCommission;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        TradeFeeResponseInner tradeFeeResponseInner = (TradeFeeResponseInner) o;
        return Objects.equals(this.symbol, tradeFeeResponseInner.symbol)
                && Objects.equals(this.makerCommission, tradeFeeResponseInner.makerCommission)
                && Objects.equals(this.takerCommission, tradeFeeResponseInner.takerCommission);
    }

    @Override
    public int hashCode() {
        return Objects.hash(symbol, makerCommission, takerCommission);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class TradeFeeResponseInner {\n");
        sb.append("		symbol: ").append(toIndentedString(symbol)).append("\n");
        sb.append("		makerCommission: ").append(toIndentedString(makerCommission)).append("\n");
        sb.append("		takerCommission: ").append(toIndentedString(takerCommission)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    public String toUrlQueryString() {
        StringBuilder sb = new StringBuilder();

        Object symbolValue = getSymbol();
        String symbolValueAsString = "";
        symbolValueAsString = symbolValue.toString();
        sb.append("symbol=").append(urlEncode(symbolValueAsString)).append("");
        Object makerCommissionValue = getMakerCommission();
        String makerCommissionValueAsString = "";
        makerCommissionValueAsString = makerCommissionValue.toString();
        sb.append("makerCommission=").append(urlEncode(makerCommissionValueAsString)).append("");
        Object takerCommissionValue = getTakerCommission();
        String takerCommissionValueAsString = "";
        takerCommissionValueAsString = takerCommissionValue.toString();
        sb.append("takerCommission=").append(urlEncode(takerCommissionValueAsString)).append("");
        return sb.toString();
    }

    public static String urlEncode(String s) {
        try {
            return URLEncoder.encode(s, StandardCharsets.UTF_8.name());
        } catch (UnsupportedEncodingException e) {
            throw new RuntimeException(StandardCharsets.UTF_8.name() + " is unsupported", e);
        }
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces (except the first
     * line).
     */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n		");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("symbol");
        openapiFields.add("makerCommission");
        openapiFields.add("takerCommission");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to TradeFeeResponseInner
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!TradeFeeResponseInner.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                "The required field(s) %s in TradeFeeResponseInner is not found in"
                                        + " the empty JSON string",
                                TradeFeeResponseInner.openapiRequiredFields.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if ((jsonObj.get("symbol") != null && !jsonObj.get("symbol").isJsonNull())
                && !jsonObj.get("symbol").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            "Expected the field `symbol` to be a primitive type in the JSON string"
                                    + " but got `%s`",
                            jsonObj.get("symbol").toString()));
        }
        if ((jsonObj.get("makerCommission") != null && !jsonObj.get("makerCommission").isJsonNull())
                && !jsonObj.get("makerCommission").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            "Expected the field `makerCommission` to be a primitive type in the"
                                    + " JSON string but got `%s`",
                            jsonObj.get("makerCommission").toString()));
        }
        if ((jsonObj.get("takerCommission") != null && !jsonObj.get("takerCommission").isJsonNull())
                && !jsonObj.get("takerCommission").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            "Expected the field `takerCommission` to be a primitive type in the"
                                    + " JSON string but got `%s`",
                            jsonObj.get("takerCommission").toString()));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!TradeFeeResponseInner.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'TradeFeeResponseInner' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<TradeFeeResponseInner> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(TradeFeeResponseInner.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<TradeFeeResponseInner>() {
                        @Override
                        public void write(JsonWriter out, TradeFeeResponseInner value)
                                throws IOException {
                            JsonElement obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public TradeFeeResponseInner read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            // validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of TradeFeeResponseInner given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of TradeFeeResponseInner
     * @throws IOException if the JSON string is invalid with respect to TradeFeeResponseInner
     */
    public static TradeFeeResponseInner fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, TradeFeeResponseInner.class);
    }

    /**
     * Convert an instance of TradeFeeResponseInner to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
