/*
 * Alpha WebSocket Market Streams
 * Access Alpha market streams over WebSocket.
 *
 * The version of the OpenAPI document: 1.0.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package com.binance.connector.client.alpha.websocket.stream.api;

import com.binance.connector.client.alpha.websocket.stream.JSON;
import com.binance.connector.client.alpha.websocket.stream.model.AggregateTradeStreamRequest;
import com.binance.connector.client.alpha.websocket.stream.model.AggregateTradeStreamResponse;
import com.binance.connector.client.alpha.websocket.stream.model.AllBookTickerStreamRequest;
import com.binance.connector.client.alpha.websocket.stream.model.AllBookTickerStreamResponse;
import com.binance.connector.client.alpha.websocket.stream.model.AllMiniTickerStreamRequest;
import com.binance.connector.client.alpha.websocket.stream.model.AllMiniTickerStreamResponse;
import com.binance.connector.client.alpha.websocket.stream.model.AllTickerStreamRequest;
import com.binance.connector.client.alpha.websocket.stream.model.AllTickerStreamResponse;
import com.binance.connector.client.alpha.websocket.stream.model.AllTokens24hTickerStreamRequest;
import com.binance.connector.client.alpha.websocket.stream.model.AllTokens24hTickerStreamResponse;
import com.binance.connector.client.alpha.websocket.stream.model.BookTickerStreamRequest;
import com.binance.connector.client.alpha.websocket.stream.model.BookTickerStreamResponse;
import com.binance.connector.client.alpha.websocket.stream.model.ContractKlineStreamRequest;
import com.binance.connector.client.alpha.websocket.stream.model.ContractKlineStreamResponse;
import com.binance.connector.client.alpha.websocket.stream.model.FullDepthStreamRequest;
import com.binance.connector.client.alpha.websocket.stream.model.FullDepthStreamResponse;
import com.binance.connector.client.alpha.websocket.stream.model.KlineStreamRequest;
import com.binance.connector.client.alpha.websocket.stream.model.KlineStreamResponse;
import com.binance.connector.client.alpha.websocket.stream.model.MiniTickerStreamRequest;
import com.binance.connector.client.alpha.websocket.stream.model.MiniTickerStreamResponse;
import com.binance.connector.client.alpha.websocket.stream.model.PartialDepthStreamRequest;
import com.binance.connector.client.alpha.websocket.stream.model.PartialDepthStreamResponse;
import com.binance.connector.client.alpha.websocket.stream.model.TickerStreamRequest;
import com.binance.connector.client.alpha.websocket.stream.model.TickerStreamResponse;
import com.binance.connector.client.alpha.websocket.stream.model.TradeStreamRequest;
import com.binance.connector.client.alpha.websocket.stream.model.TradeStreamResponse;
import com.binance.connector.client.common.ApiException;
import com.binance.connector.client.common.SystemUtil;
import com.binance.connector.client.common.exception.ConstraintViolationException;
import com.binance.connector.client.common.websocket.adapter.stream.StreamConnectionInterface;
import com.binance.connector.client.common.websocket.adapter.stream.StreamConnectionPoolWrapper;
import com.binance.connector.client.common.websocket.adapter.stream.StreamConnectionWrapper;
import com.binance.connector.client.common.websocket.configuration.WebSocketClientConfiguration;
import com.binance.connector.client.common.websocket.dtos.RequestWrapperDTO;
import com.binance.connector.client.common.websocket.service.StreamBlockingQueue;
import com.binance.connector.client.common.websocket.service.StreamBlockingQueueWrapper;
import com.google.gson.reflect.TypeToken;
import jakarta.validation.ConstraintViolation;
import jakarta.validation.Validation;
import jakarta.validation.Validator;
import jakarta.validation.constraints.*;
import java.util.Collections;
import java.util.Map;
import java.util.Random;
import java.util.Set;
import org.hibernate.validator.messageinterpolation.ParameterMessageInterpolator;

public class DefaultApi {
    private static final String USER_AGENT =
            String.format(
                    "binance-alpha/2.0.0 (Java/%s; %s; %s)",
                    SystemUtil.getJavaVersion(), SystemUtil.getOs(), SystemUtil.getArch());

    private StreamConnectionInterface connection;

    public DefaultApi() {}

    public DefaultApi(WebSocketClientConfiguration configuration) {
        this(
                configuration.getUsePool()
                        ? new StreamConnectionPoolWrapper(configuration, JSON.getGson())
                        : new StreamConnectionWrapper(configuration, JSON.getGson()));
    }

    public DefaultApi(StreamConnectionInterface connection) {
        connection.setUserAgent(USER_AGENT);
        if (!connection.isConnected()) {
            connection.connect();
        }
        this.connection = connection;
    }

    public StreamConnectionInterface getConnection() {
        return connection;
    }

    /**
     * Aggregate Trade Stream Pushes aggregate trade updates for a symbol.
     *
     * @param aggregateTradeStreamRequest (required)
     * @return AggregateTradeStreamResponse
     * @throws ApiException If fail to call the API, e.g. server error or cannot deserialize the
     *     response body
     * @http.response.details
     *     <table border="1">
     * <caption>Response Details</caption>
     * <tr><td> Status Code </td><td> Description </td><td> Response Headers </td></tr>
     * <tr><td> 200 </td><td> Aggregate Trade Stream </td><td>  -  </td></tr>
     * </table>
     *
     * @see <a
     *     href="https://developers.binance.com/en/docs/catalog/advanced-trading-alpha-trading/api/ws-streams/~#aggregate-trade-stream">Aggregate
     *     Trade Stream Documentation</a>
     */
    public StreamBlockingQueueWrapper<AggregateTradeStreamResponse> aggregateTradeStream(
            AggregateTradeStreamRequest aggregateTradeStreamRequest) throws ApiException {
        StreamBlockingQueue<String> queue = aggregateTradeStreamRaw(aggregateTradeStreamRequest);

        TypeToken<AggregateTradeStreamResponse> typeToken =
                new TypeToken<AggregateTradeStreamResponse>() {};

        return new StreamBlockingQueueWrapper<>(queue, typeToken);
    }

    public StreamBlockingQueue<String> aggregateTradeStreamRaw(
            AggregateTradeStreamRequest aggregateTradeStreamRequest) throws ApiException {
        aggregateTradeStreamValidateBeforeCall(aggregateTradeStreamRequest);

        String methodName =
                "/<symbol>@aggTrade"
                        .substring(1)
                        .replace(
                                "<id>",
                                aggregateTradeStreamRequest.getId() != null
                                        ? aggregateTradeStreamRequest.getId().toString()
                                        : "")
                        .replace(
                                "<symbol>",
                                aggregateTradeStreamRequest.getSymbol() != null
                                        ? aggregateTradeStreamRequest.getSymbol().toString()
                                        : "");
        if ("@".equals(methodName.substring(methodName.length() - 1))) {
            methodName = methodName.substring(0, methodName.length() - 1);
        }

        RequestWrapperDTO<Set<String>, Object> requestWrapperDTO =
                new RequestWrapperDTO.Builder<Set<String>, Object>()
                        .id(getRequestID())
                        .method("SUBSCRIBE")
                        .params(Collections.singleton(methodName))
                        .build();
        Map<String, StreamBlockingQueue<String>> queuesMap =
                connection.subscribe(requestWrapperDTO);
        return queuesMap.get(methodName);
    }

    @SuppressWarnings("rawtypes")
    private void aggregateTradeStreamValidateBeforeCall(
            AggregateTradeStreamRequest aggregateTradeStreamRequest) throws ApiException {
        try {
            Validator validator =
                    Validation.byDefaultProvider()
                            .configure()
                            .messageInterpolator(new ParameterMessageInterpolator())
                            .buildValidatorFactory()
                            .getValidator();

            Set<ConstraintViolation<AggregateTradeStreamRequest>> violations =
                    validator.validate(aggregateTradeStreamRequest);

            if (!violations.isEmpty()) {
                throw new ConstraintViolationException(violations);
            }
        } catch (SecurityException e) {
            e.printStackTrace();
            throw new ApiException(e.getMessage());
        }
    }

    /**
     * All Book Ticker Stream Pushes best bid/ask updates for all symbols.
     *
     * @param allBookTickerStreamRequest (required)
     * @return AllBookTickerStreamResponse
     * @throws ApiException If fail to call the API, e.g. server error or cannot deserialize the
     *     response body
     * @http.response.details
     *     <table border="1">
     * <caption>Response Details</caption>
     * <tr><td> Status Code </td><td> Description </td><td> Response Headers </td></tr>
     * <tr><td> 200 </td><td> All Book Ticker Stream </td><td>  -  </td></tr>
     * </table>
     *
     * @see <a
     *     href="https://developers.binance.com/en/docs/catalog/advanced-trading-alpha-trading/api/ws-streams/~#all-book-ticker-stream">All
     *     Book Ticker Stream Documentation</a>
     */
    public StreamBlockingQueueWrapper<AllBookTickerStreamResponse> allBookTickerStream(
            AllBookTickerStreamRequest allBookTickerStreamRequest) throws ApiException {
        StreamBlockingQueue<String> queue = allBookTickerStreamRaw(allBookTickerStreamRequest);

        TypeToken<AllBookTickerStreamResponse> typeToken =
                new TypeToken<AllBookTickerStreamResponse>() {};

        return new StreamBlockingQueueWrapper<>(queue, typeToken);
    }

    public StreamBlockingQueue<String> allBookTickerStreamRaw(
            AllBookTickerStreamRequest allBookTickerStreamRequest) throws ApiException {
        allBookTickerStreamValidateBeforeCall(allBookTickerStreamRequest);

        String methodName =
                "/!bookTicker"
                        .substring(1)
                        .replace(
                                "<id>",
                                allBookTickerStreamRequest.getId() != null
                                        ? allBookTickerStreamRequest.getId().toString()
                                        : "");
        if ("@".equals(methodName.substring(methodName.length() - 1))) {
            methodName = methodName.substring(0, methodName.length() - 1);
        }

        RequestWrapperDTO<Set<String>, Object> requestWrapperDTO =
                new RequestWrapperDTO.Builder<Set<String>, Object>()
                        .id(getRequestID())
                        .method("SUBSCRIBE")
                        .params(Collections.singleton(methodName))
                        .build();
        Map<String, StreamBlockingQueue<String>> queuesMap =
                connection.subscribe(requestWrapperDTO);
        return queuesMap.get(methodName);
    }

    @SuppressWarnings("rawtypes")
    private void allBookTickerStreamValidateBeforeCall(
            AllBookTickerStreamRequest allBookTickerStreamRequest) throws ApiException {
        try {
            Validator validator =
                    Validation.byDefaultProvider()
                            .configure()
                            .messageInterpolator(new ParameterMessageInterpolator())
                            .buildValidatorFactory()
                            .getValidator();

            Set<ConstraintViolation<AllBookTickerStreamRequest>> violations =
                    validator.validate(allBookTickerStreamRequest);

            if (!violations.isEmpty()) {
                throw new ConstraintViolationException(violations);
            }
        } catch (SecurityException e) {
            e.printStackTrace();
            throw new ApiException(e.getMessage());
        }
    }

    /**
     * All Mini Ticker Stream Pushes mini ticker statistics for all symbols.
     *
     * @param allMiniTickerStreamRequest (required)
     * @return AllMiniTickerStreamResponse
     * @throws ApiException If fail to call the API, e.g. server error or cannot deserialize the
     *     response body
     * @http.response.details
     *     <table border="1">
     * <caption>Response Details</caption>
     * <tr><td> Status Code </td><td> Description </td><td> Response Headers </td></tr>
     * <tr><td> 200 </td><td> All Mini Ticker Stream </td><td>  -  </td></tr>
     * </table>
     *
     * @see <a
     *     href="https://developers.binance.com/en/docs/catalog/advanced-trading-alpha-trading/api/ws-streams/~#all-mini-ticker-stream">All
     *     Mini Ticker Stream Documentation</a>
     */
    public StreamBlockingQueueWrapper<AllMiniTickerStreamResponse> allMiniTickerStream(
            AllMiniTickerStreamRequest allMiniTickerStreamRequest) throws ApiException {
        StreamBlockingQueue<String> queue = allMiniTickerStreamRaw(allMiniTickerStreamRequest);

        TypeToken<AllMiniTickerStreamResponse> typeToken =
                new TypeToken<AllMiniTickerStreamResponse>() {};

        return new StreamBlockingQueueWrapper<>(queue, typeToken);
    }

    public StreamBlockingQueue<String> allMiniTickerStreamRaw(
            AllMiniTickerStreamRequest allMiniTickerStreamRequest) throws ApiException {
        allMiniTickerStreamValidateBeforeCall(allMiniTickerStreamRequest);

        String methodName =
                "/!miniTicker@arr"
                        .substring(1)
                        .replace(
                                "<id>",
                                allMiniTickerStreamRequest.getId() != null
                                        ? allMiniTickerStreamRequest.getId().toString()
                                        : "");
        if ("@".equals(methodName.substring(methodName.length() - 1))) {
            methodName = methodName.substring(0, methodName.length() - 1);
        }

        RequestWrapperDTO<Set<String>, Object> requestWrapperDTO =
                new RequestWrapperDTO.Builder<Set<String>, Object>()
                        .id(getRequestID())
                        .method("SUBSCRIBE")
                        .params(Collections.singleton(methodName))
                        .build();
        Map<String, StreamBlockingQueue<String>> queuesMap =
                connection.subscribe(requestWrapperDTO);
        return queuesMap.get(methodName);
    }

    @SuppressWarnings("rawtypes")
    private void allMiniTickerStreamValidateBeforeCall(
            AllMiniTickerStreamRequest allMiniTickerStreamRequest) throws ApiException {
        try {
            Validator validator =
                    Validation.byDefaultProvider()
                            .configure()
                            .messageInterpolator(new ParameterMessageInterpolator())
                            .buildValidatorFactory()
                            .getValidator();

            Set<ConstraintViolation<AllMiniTickerStreamRequest>> violations =
                    validator.validate(allMiniTickerStreamRequest);

            if (!violations.isEmpty()) {
                throw new ConstraintViolationException(violations);
            }
        } catch (SecurityException e) {
            e.printStackTrace();
            throw new ApiException(e.getMessage());
        }
    }

    /**
     * All Ticker Stream Pushes full ticker statistics for all symbols.
     *
     * @param allTickerStreamRequest (required)
     * @return AllTickerStreamResponse
     * @throws ApiException If fail to call the API, e.g. server error or cannot deserialize the
     *     response body
     * @http.response.details
     *     <table border="1">
     * <caption>Response Details</caption>
     * <tr><td> Status Code </td><td> Description </td><td> Response Headers </td></tr>
     * <tr><td> 200 </td><td> All Ticker Stream </td><td>  -  </td></tr>
     * </table>
     *
     * @see <a
     *     href="https://developers.binance.com/en/docs/catalog/advanced-trading-alpha-trading/api/ws-streams/~#all-ticker-stream">All
     *     Ticker Stream Documentation</a>
     */
    public StreamBlockingQueueWrapper<AllTickerStreamResponse> allTickerStream(
            AllTickerStreamRequest allTickerStreamRequest) throws ApiException {
        StreamBlockingQueue<String> queue = allTickerStreamRaw(allTickerStreamRequest);

        TypeToken<AllTickerStreamResponse> typeToken = new TypeToken<AllTickerStreamResponse>() {};

        return new StreamBlockingQueueWrapper<>(queue, typeToken);
    }

    public StreamBlockingQueue<String> allTickerStreamRaw(
            AllTickerStreamRequest allTickerStreamRequest) throws ApiException {
        allTickerStreamValidateBeforeCall(allTickerStreamRequest);

        String methodName =
                "/!ticker@arr"
                        .substring(1)
                        .replace(
                                "<id>",
                                allTickerStreamRequest.getId() != null
                                        ? allTickerStreamRequest.getId().toString()
                                        : "");
        if ("@".equals(methodName.substring(methodName.length() - 1))) {
            methodName = methodName.substring(0, methodName.length() - 1);
        }

        RequestWrapperDTO<Set<String>, Object> requestWrapperDTO =
                new RequestWrapperDTO.Builder<Set<String>, Object>()
                        .id(getRequestID())
                        .method("SUBSCRIBE")
                        .params(Collections.singleton(methodName))
                        .build();
        Map<String, StreamBlockingQueue<String>> queuesMap =
                connection.subscribe(requestWrapperDTO);
        return queuesMap.get(methodName);
    }

    @SuppressWarnings("rawtypes")
    private void allTickerStreamValidateBeforeCall(AllTickerStreamRequest allTickerStreamRequest)
            throws ApiException {
        try {
            Validator validator =
                    Validation.byDefaultProvider()
                            .configure()
                            .messageInterpolator(new ParameterMessageInterpolator())
                            .buildValidatorFactory()
                            .getValidator();

            Set<ConstraintViolation<AllTickerStreamRequest>> violations =
                    validator.validate(allTickerStreamRequest);

            if (!violations.isEmpty()) {
                throw new ConstraintViolationException(violations);
            }
        } catch (SecurityException e) {
            e.printStackTrace();
            throw new ApiException(e.getMessage());
        }
    }

    /**
     * All Tokens 24h Ticker Stream Pushes 24h ticker-like metrics for all tokens.
     *
     * @param allTokens24hTickerStreamRequest (required)
     * @return AllTokens24hTickerStreamResponse
     * @throws ApiException If fail to call the API, e.g. server error or cannot deserialize the
     *     response body
     * @http.response.details
     *     <table border="1">
     * <caption>Response Details</caption>
     * <tr><td> Status Code </td><td> Description </td><td> Response Headers </td></tr>
     * <tr><td> 200 </td><td> All Tokens 24h Ticker Stream </td><td>  -  </td></tr>
     * </table>
     *
     * @see <a
     *     href="https://developers.binance.com/en/docs/catalog/advanced-trading-alpha-trading/api/ws-streams/~#all-tokens24h-ticker-stream">All
     *     Tokens 24h Ticker Stream Documentation</a>
     */
    public StreamBlockingQueueWrapper<AllTokens24hTickerStreamResponse> allTokens24hTickerStream(
            AllTokens24hTickerStreamRequest allTokens24hTickerStreamRequest) throws ApiException {
        StreamBlockingQueue<String> queue =
                allTokens24hTickerStreamRaw(allTokens24hTickerStreamRequest);

        TypeToken<AllTokens24hTickerStreamResponse> typeToken =
                new TypeToken<AllTokens24hTickerStreamResponse>() {};

        return new StreamBlockingQueueWrapper<>(queue, typeToken);
    }

    public StreamBlockingQueue<String> allTokens24hTickerStreamRaw(
            AllTokens24hTickerStreamRequest allTokens24hTickerStreamRequest) throws ApiException {
        allTokens24hTickerStreamValidateBeforeCall(allTokens24hTickerStreamRequest);

        String methodName =
                "/came@allTokens@ticker24"
                        .substring(1)
                        .replace(
                                "<id>",
                                allTokens24hTickerStreamRequest.getId() != null
                                        ? allTokens24hTickerStreamRequest.getId().toString()
                                        : "");
        if ("@".equals(methodName.substring(methodName.length() - 1))) {
            methodName = methodName.substring(0, methodName.length() - 1);
        }

        RequestWrapperDTO<Set<String>, Object> requestWrapperDTO =
                new RequestWrapperDTO.Builder<Set<String>, Object>()
                        .id(getRequestID())
                        .method("SUBSCRIBE")
                        .params(Collections.singleton(methodName))
                        .build();
        Map<String, StreamBlockingQueue<String>> queuesMap =
                connection.subscribe(requestWrapperDTO);
        return queuesMap.get(methodName);
    }

    @SuppressWarnings("rawtypes")
    private void allTokens24hTickerStreamValidateBeforeCall(
            AllTokens24hTickerStreamRequest allTokens24hTickerStreamRequest) throws ApiException {
        try {
            Validator validator =
                    Validation.byDefaultProvider()
                            .configure()
                            .messageInterpolator(new ParameterMessageInterpolator())
                            .buildValidatorFactory()
                            .getValidator();

            Set<ConstraintViolation<AllTokens24hTickerStreamRequest>> violations =
                    validator.validate(allTokens24hTickerStreamRequest);

            if (!violations.isEmpty()) {
                throw new ConstraintViolationException(violations);
            }
        } catch (SecurityException e) {
            e.printStackTrace();
            throw new ApiException(e.getMessage());
        }
    }

    /**
     * Book Ticker Stream Pushes best bid/ask updates for a symbol.
     *
     * @param bookTickerStreamRequest (required)
     * @return BookTickerStreamResponse
     * @throws ApiException If fail to call the API, e.g. server error or cannot deserialize the
     *     response body
     * @http.response.details
     *     <table border="1">
     * <caption>Response Details</caption>
     * <tr><td> Status Code </td><td> Description </td><td> Response Headers </td></tr>
     * <tr><td> 200 </td><td> Book Ticker Stream </td><td>  -  </td></tr>
     * </table>
     *
     * @see <a
     *     href="https://developers.binance.com/en/docs/catalog/advanced-trading-alpha-trading/api/ws-streams/~#book-ticker-stream">Book
     *     Ticker Stream Documentation</a>
     */
    public StreamBlockingQueueWrapper<BookTickerStreamResponse> bookTickerStream(
            BookTickerStreamRequest bookTickerStreamRequest) throws ApiException {
        StreamBlockingQueue<String> queue = bookTickerStreamRaw(bookTickerStreamRequest);

        TypeToken<BookTickerStreamResponse> typeToken =
                new TypeToken<BookTickerStreamResponse>() {};

        return new StreamBlockingQueueWrapper<>(queue, typeToken);
    }

    public StreamBlockingQueue<String> bookTickerStreamRaw(
            BookTickerStreamRequest bookTickerStreamRequest) throws ApiException {
        bookTickerStreamValidateBeforeCall(bookTickerStreamRequest);

        String methodName =
                "/<symbol>@bookTicker"
                        .substring(1)
                        .replace(
                                "<id>",
                                bookTickerStreamRequest.getId() != null
                                        ? bookTickerStreamRequest.getId().toString()
                                        : "")
                        .replace(
                                "<symbol>",
                                bookTickerStreamRequest.getSymbol() != null
                                        ? bookTickerStreamRequest.getSymbol().toString()
                                        : "");
        if ("@".equals(methodName.substring(methodName.length() - 1))) {
            methodName = methodName.substring(0, methodName.length() - 1);
        }

        RequestWrapperDTO<Set<String>, Object> requestWrapperDTO =
                new RequestWrapperDTO.Builder<Set<String>, Object>()
                        .id(getRequestID())
                        .method("SUBSCRIBE")
                        .params(Collections.singleton(methodName))
                        .build();
        Map<String, StreamBlockingQueue<String>> queuesMap =
                connection.subscribe(requestWrapperDTO);
        return queuesMap.get(methodName);
    }

    @SuppressWarnings("rawtypes")
    private void bookTickerStreamValidateBeforeCall(BookTickerStreamRequest bookTickerStreamRequest)
            throws ApiException {
        try {
            Validator validator =
                    Validation.byDefaultProvider()
                            .configure()
                            .messageInterpolator(new ParameterMessageInterpolator())
                            .buildValidatorFactory()
                            .getValidator();

            Set<ConstraintViolation<BookTickerStreamRequest>> violations =
                    validator.validate(bookTickerStreamRequest);

            if (!violations.isEmpty()) {
                throw new ConstraintViolationException(violations);
            }
        } catch (SecurityException e) {
            e.printStackTrace();
            throw new ApiException(e.getMessage());
        }
    }

    /**
     * Contract Kline Stream Pushes kline updates by contractAddress@chainId.
     *
     * @param contractKlineStreamRequest (required)
     * @return ContractKlineStreamResponse
     * @throws ApiException If fail to call the API, e.g. server error or cannot deserialize the
     *     response body
     * @http.response.details
     *     <table border="1">
     * <caption>Response Details</caption>
     * <tr><td> Status Code </td><td> Description </td><td> Response Headers </td></tr>
     * <tr><td> 200 </td><td> Contract Kline Stream </td><td>  -  </td></tr>
     * </table>
     *
     * @see <a
     *     href="https://developers.binance.com/en/docs/catalog/advanced-trading-alpha-trading/api/ws-streams/~#contract-kline-stream">Contract
     *     Kline Stream Documentation</a>
     */
    public StreamBlockingQueueWrapper<ContractKlineStreamResponse> contractKlineStream(
            ContractKlineStreamRequest contractKlineStreamRequest) throws ApiException {
        StreamBlockingQueue<String> queue = contractKlineStreamRaw(contractKlineStreamRequest);

        TypeToken<ContractKlineStreamResponse> typeToken =
                new TypeToken<ContractKlineStreamResponse>() {};

        return new StreamBlockingQueueWrapper<>(queue, typeToken);
    }

    public StreamBlockingQueue<String> contractKlineStreamRaw(
            ContractKlineStreamRequest contractKlineStreamRequest) throws ApiException {
        contractKlineStreamValidateBeforeCall(contractKlineStreamRequest);

        String methodName =
                "/came@<contractAddress>@<chainId>@kline_<interval>"
                        .substring(1)
                        .replace(
                                "<id>",
                                contractKlineStreamRequest.getId() != null
                                        ? contractKlineStreamRequest.getId().toString()
                                        : "")
                        .replace(
                                "<contractAddress>",
                                contractKlineStreamRequest.getContractAddress() != null
                                        ? contractKlineStreamRequest.getContractAddress().toString()
                                        : "")
                        .replace(
                                "<chainId>",
                                contractKlineStreamRequest.getChainId() != null
                                        ? contractKlineStreamRequest.getChainId().toString()
                                        : "")
                        .replace(
                                "<interval>",
                                contractKlineStreamRequest.getInterval() != null
                                        ? contractKlineStreamRequest.getInterval().toString()
                                        : "");
        if ("@".equals(methodName.substring(methodName.length() - 1))) {
            methodName = methodName.substring(0, methodName.length() - 1);
        }

        RequestWrapperDTO<Set<String>, Object> requestWrapperDTO =
                new RequestWrapperDTO.Builder<Set<String>, Object>()
                        .id(getRequestID())
                        .method("SUBSCRIBE")
                        .params(Collections.singleton(methodName))
                        .build();
        Map<String, StreamBlockingQueue<String>> queuesMap =
                connection.subscribe(requestWrapperDTO);
        return queuesMap.get(methodName);
    }

    @SuppressWarnings("rawtypes")
    private void contractKlineStreamValidateBeforeCall(
            ContractKlineStreamRequest contractKlineStreamRequest) throws ApiException {
        try {
            Validator validator =
                    Validation.byDefaultProvider()
                            .configure()
                            .messageInterpolator(new ParameterMessageInterpolator())
                            .buildValidatorFactory()
                            .getValidator();

            Set<ConstraintViolation<ContractKlineStreamRequest>> violations =
                    validator.validate(contractKlineStreamRequest);

            if (!violations.isEmpty()) {
                throw new ConstraintViolationException(violations);
            }
        } catch (SecurityException e) {
            e.printStackTrace();
            throw new ApiException(e.getMessage());
        }
    }

    /**
     * Full Depth Stream Returns all available depth, including UI and API orders.
     *
     * @param fullDepthStreamRequest (required)
     * @return FullDepthStreamResponse
     * @throws ApiException If fail to call the API, e.g. server error or cannot deserialize the
     *     response body
     * @http.response.details
     *     <table border="1">
     * <caption>Response Details</caption>
     * <tr><td> Status Code </td><td> Description </td><td> Response Headers </td></tr>
     * <tr><td> 200 </td><td> Full Depth Stream </td><td>  -  </td></tr>
     * </table>
     *
     * @see <a
     *     href="https://developers.binance.com/en/docs/catalog/advanced-trading-alpha-trading/api/ws-streams/~#full-depth-stream">Full
     *     Depth Stream Documentation</a>
     */
    public StreamBlockingQueueWrapper<FullDepthStreamResponse> fullDepthStream(
            FullDepthStreamRequest fullDepthStreamRequest) throws ApiException {
        StreamBlockingQueue<String> queue = fullDepthStreamRaw(fullDepthStreamRequest);

        TypeToken<FullDepthStreamResponse> typeToken = new TypeToken<FullDepthStreamResponse>() {};

        return new StreamBlockingQueueWrapper<>(queue, typeToken);
    }

    public StreamBlockingQueue<String> fullDepthStreamRaw(
            FullDepthStreamRequest fullDepthStreamRequest) throws ApiException {
        fullDepthStreamValidateBeforeCall(fullDepthStreamRequest);

        String methodName =
                "/<symbol>@fulldepth@<interval>"
                        .substring(1)
                        .replace(
                                "<id>",
                                fullDepthStreamRequest.getId() != null
                                        ? fullDepthStreamRequest.getId().toString()
                                        : "")
                        .replace(
                                "<symbol>",
                                fullDepthStreamRequest.getSymbol() != null
                                        ? fullDepthStreamRequest.getSymbol().toString()
                                        : "")
                        .replace(
                                "<interval>",
                                fullDepthStreamRequest.getInterval() != null
                                        ? fullDepthStreamRequest.getInterval().toString()
                                        : "");
        if ("@".equals(methodName.substring(methodName.length() - 1))) {
            methodName = methodName.substring(0, methodName.length() - 1);
        }

        RequestWrapperDTO<Set<String>, Object> requestWrapperDTO =
                new RequestWrapperDTO.Builder<Set<String>, Object>()
                        .id(getRequestID())
                        .method("SUBSCRIBE")
                        .params(Collections.singleton(methodName))
                        .build();
        Map<String, StreamBlockingQueue<String>> queuesMap =
                connection.subscribe(requestWrapperDTO);
        return queuesMap.get(methodName);
    }

    @SuppressWarnings("rawtypes")
    private void fullDepthStreamValidateBeforeCall(FullDepthStreamRequest fullDepthStreamRequest)
            throws ApiException {
        try {
            Validator validator =
                    Validation.byDefaultProvider()
                            .configure()
                            .messageInterpolator(new ParameterMessageInterpolator())
                            .buildValidatorFactory()
                            .getValidator();

            Set<ConstraintViolation<FullDepthStreamRequest>> violations =
                    validator.validate(fullDepthStreamRequest);

            if (!violations.isEmpty()) {
                throw new ConstraintViolationException(violations);
            }
        } catch (SecurityException e) {
            e.printStackTrace();
            throw new ApiException(e.getMessage());
        }
    }

    /**
     * Kline Stream Pushes kline updates for a symbol.
     *
     * @param klineStreamRequest (required)
     * @return KlineStreamResponse
     * @throws ApiException If fail to call the API, e.g. server error or cannot deserialize the
     *     response body
     * @http.response.details
     *     <table border="1">
     * <caption>Response Details</caption>
     * <tr><td> Status Code </td><td> Description </td><td> Response Headers </td></tr>
     * <tr><td> 200 </td><td> Kline Stream </td><td>  -  </td></tr>
     * </table>
     *
     * @see <a
     *     href="https://developers.binance.com/en/docs/catalog/advanced-trading-alpha-trading/api/ws-streams/~#kline-stream">Kline
     *     Stream Documentation</a>
     */
    public StreamBlockingQueueWrapper<KlineStreamResponse> klineStream(
            KlineStreamRequest klineStreamRequest) throws ApiException {
        StreamBlockingQueue<String> queue = klineStreamRaw(klineStreamRequest);

        TypeToken<KlineStreamResponse> typeToken = new TypeToken<KlineStreamResponse>() {};

        return new StreamBlockingQueueWrapper<>(queue, typeToken);
    }

    public StreamBlockingQueue<String> klineStreamRaw(KlineStreamRequest klineStreamRequest)
            throws ApiException {
        klineStreamValidateBeforeCall(klineStreamRequest);

        String methodName =
                "/<symbol>@kline_<interval>"
                        .substring(1)
                        .replace(
                                "<id>",
                                klineStreamRequest.getId() != null
                                        ? klineStreamRequest.getId().toString()
                                        : "")
                        .replace(
                                "<symbol>",
                                klineStreamRequest.getSymbol() != null
                                        ? klineStreamRequest.getSymbol().toString()
                                        : "")
                        .replace(
                                "<interval>",
                                klineStreamRequest.getInterval() != null
                                        ? klineStreamRequest.getInterval().toString()
                                        : "");
        if ("@".equals(methodName.substring(methodName.length() - 1))) {
            methodName = methodName.substring(0, methodName.length() - 1);
        }

        RequestWrapperDTO<Set<String>, Object> requestWrapperDTO =
                new RequestWrapperDTO.Builder<Set<String>, Object>()
                        .id(getRequestID())
                        .method("SUBSCRIBE")
                        .params(Collections.singleton(methodName))
                        .build();
        Map<String, StreamBlockingQueue<String>> queuesMap =
                connection.subscribe(requestWrapperDTO);
        return queuesMap.get(methodName);
    }

    @SuppressWarnings("rawtypes")
    private void klineStreamValidateBeforeCall(KlineStreamRequest klineStreamRequest)
            throws ApiException {
        try {
            Validator validator =
                    Validation.byDefaultProvider()
                            .configure()
                            .messageInterpolator(new ParameterMessageInterpolator())
                            .buildValidatorFactory()
                            .getValidator();

            Set<ConstraintViolation<KlineStreamRequest>> violations =
                    validator.validate(klineStreamRequest);

            if (!violations.isEmpty()) {
                throw new ConstraintViolationException(violations);
            }
        } catch (SecurityException e) {
            e.printStackTrace();
            throw new ApiException(e.getMessage());
        }
    }

    /**
     * Mini Ticker Stream Pushes 24h rolling mini ticker statistics.
     *
     * @param miniTickerStreamRequest (required)
     * @return MiniTickerStreamResponse
     * @throws ApiException If fail to call the API, e.g. server error or cannot deserialize the
     *     response body
     * @http.response.details
     *     <table border="1">
     * <caption>Response Details</caption>
     * <tr><td> Status Code </td><td> Description </td><td> Response Headers </td></tr>
     * <tr><td> 200 </td><td> Mini Ticker Stream </td><td>  -  </td></tr>
     * </table>
     *
     * @see <a
     *     href="https://developers.binance.com/en/docs/catalog/advanced-trading-alpha-trading/api/ws-streams/~#mini-ticker-stream">Mini
     *     Ticker Stream Documentation</a>
     */
    public StreamBlockingQueueWrapper<MiniTickerStreamResponse> miniTickerStream(
            MiniTickerStreamRequest miniTickerStreamRequest) throws ApiException {
        StreamBlockingQueue<String> queue = miniTickerStreamRaw(miniTickerStreamRequest);

        TypeToken<MiniTickerStreamResponse> typeToken =
                new TypeToken<MiniTickerStreamResponse>() {};

        return new StreamBlockingQueueWrapper<>(queue, typeToken);
    }

    public StreamBlockingQueue<String> miniTickerStreamRaw(
            MiniTickerStreamRequest miniTickerStreamRequest) throws ApiException {
        miniTickerStreamValidateBeforeCall(miniTickerStreamRequest);

        String methodName =
                "/<symbol>@miniTicker"
                        .substring(1)
                        .replace(
                                "<id>",
                                miniTickerStreamRequest.getId() != null
                                        ? miniTickerStreamRequest.getId().toString()
                                        : "")
                        .replace(
                                "<symbol>",
                                miniTickerStreamRequest.getSymbol() != null
                                        ? miniTickerStreamRequest.getSymbol().toString()
                                        : "");
        if ("@".equals(methodName.substring(methodName.length() - 1))) {
            methodName = methodName.substring(0, methodName.length() - 1);
        }

        RequestWrapperDTO<Set<String>, Object> requestWrapperDTO =
                new RequestWrapperDTO.Builder<Set<String>, Object>()
                        .id(getRequestID())
                        .method("SUBSCRIBE")
                        .params(Collections.singleton(methodName))
                        .build();
        Map<String, StreamBlockingQueue<String>> queuesMap =
                connection.subscribe(requestWrapperDTO);
        return queuesMap.get(methodName);
    }

    @SuppressWarnings("rawtypes")
    private void miniTickerStreamValidateBeforeCall(MiniTickerStreamRequest miniTickerStreamRequest)
            throws ApiException {
        try {
            Validator validator =
                    Validation.byDefaultProvider()
                            .configure()
                            .messageInterpolator(new ParameterMessageInterpolator())
                            .buildValidatorFactory()
                            .getValidator();

            Set<ConstraintViolation<MiniTickerStreamRequest>> violations =
                    validator.validate(miniTickerStreamRequest);

            if (!violations.isEmpty()) {
                throw new ConstraintViolationException(violations);
            }
        } catch (SecurityException e) {
            e.printStackTrace();
            throw new ApiException(e.getMessage());
        }
    }

    /**
     * Partial Depth Stream Pushes partial depth updates (UI orders only).
     *
     * @param partialDepthStreamRequest (required)
     * @return PartialDepthStreamResponse
     * @throws ApiException If fail to call the API, e.g. server error or cannot deserialize the
     *     response body
     * @http.response.details
     *     <table border="1">
     * <caption>Response Details</caption>
     * <tr><td> Status Code </td><td> Description </td><td> Response Headers </td></tr>
     * <tr><td> 200 </td><td> Partial Depth Stream </td><td>  -  </td></tr>
     * </table>
     *
     * @see <a
     *     href="https://developers.binance.com/en/docs/catalog/advanced-trading-alpha-trading/api/ws-streams/~#partial-depth-stream">Partial
     *     Depth Stream Documentation</a>
     */
    public StreamBlockingQueueWrapper<PartialDepthStreamResponse> partialDepthStream(
            PartialDepthStreamRequest partialDepthStreamRequest) throws ApiException {
        StreamBlockingQueue<String> queue = partialDepthStreamRaw(partialDepthStreamRequest);

        TypeToken<PartialDepthStreamResponse> typeToken =
                new TypeToken<PartialDepthStreamResponse>() {};

        return new StreamBlockingQueueWrapper<>(queue, typeToken);
    }

    public StreamBlockingQueue<String> partialDepthStreamRaw(
            PartialDepthStreamRequest partialDepthStreamRequest) throws ApiException {
        partialDepthStreamValidateBeforeCall(partialDepthStreamRequest);

        String methodName =
                "/<symbol>@depth<levels>@<interval>"
                        .substring(1)
                        .replace(
                                "<id>",
                                partialDepthStreamRequest.getId() != null
                                        ? partialDepthStreamRequest.getId().toString()
                                        : "")
                        .replace(
                                "<symbol>",
                                partialDepthStreamRequest.getSymbol() != null
                                        ? partialDepthStreamRequest.getSymbol().toString()
                                        : "")
                        .replace(
                                "<levels>",
                                partialDepthStreamRequest.getLevels() != null
                                        ? partialDepthStreamRequest.getLevels().toString()
                                        : "")
                        .replace(
                                "<interval>",
                                partialDepthStreamRequest.getInterval() != null
                                        ? partialDepthStreamRequest.getInterval().toString()
                                        : "");
        if ("@".equals(methodName.substring(methodName.length() - 1))) {
            methodName = methodName.substring(0, methodName.length() - 1);
        }

        RequestWrapperDTO<Set<String>, Object> requestWrapperDTO =
                new RequestWrapperDTO.Builder<Set<String>, Object>()
                        .id(getRequestID())
                        .method("SUBSCRIBE")
                        .params(Collections.singleton(methodName))
                        .build();
        Map<String, StreamBlockingQueue<String>> queuesMap =
                connection.subscribe(requestWrapperDTO);
        return queuesMap.get(methodName);
    }

    @SuppressWarnings("rawtypes")
    private void partialDepthStreamValidateBeforeCall(
            PartialDepthStreamRequest partialDepthStreamRequest) throws ApiException {
        try {
            Validator validator =
                    Validation.byDefaultProvider()
                            .configure()
                            .messageInterpolator(new ParameterMessageInterpolator())
                            .buildValidatorFactory()
                            .getValidator();

            Set<ConstraintViolation<PartialDepthStreamRequest>> violations =
                    validator.validate(partialDepthStreamRequest);

            if (!violations.isEmpty()) {
                throw new ConstraintViolationException(violations);
            }
        } catch (SecurityException e) {
            e.printStackTrace();
            throw new ApiException(e.getMessage());
        }
    }

    /**
     * Ticker Stream Pushes full 24h rolling ticker statistics.
     *
     * @param tickerStreamRequest (required)
     * @return TickerStreamResponse
     * @throws ApiException If fail to call the API, e.g. server error or cannot deserialize the
     *     response body
     * @http.response.details
     *     <table border="1">
     * <caption>Response Details</caption>
     * <tr><td> Status Code </td><td> Description </td><td> Response Headers </td></tr>
     * <tr><td> 200 </td><td> Ticker Stream </td><td>  -  </td></tr>
     * </table>
     *
     * @see <a
     *     href="https://developers.binance.com/en/docs/catalog/advanced-trading-alpha-trading/api/ws-streams/~#ticker-stream">Ticker
     *     Stream Documentation</a>
     */
    public StreamBlockingQueueWrapper<TickerStreamResponse> tickerStream(
            TickerStreamRequest tickerStreamRequest) throws ApiException {
        StreamBlockingQueue<String> queue = tickerStreamRaw(tickerStreamRequest);

        TypeToken<TickerStreamResponse> typeToken = new TypeToken<TickerStreamResponse>() {};

        return new StreamBlockingQueueWrapper<>(queue, typeToken);
    }

    public StreamBlockingQueue<String> tickerStreamRaw(TickerStreamRequest tickerStreamRequest)
            throws ApiException {
        tickerStreamValidateBeforeCall(tickerStreamRequest);

        String methodName =
                "/<symbol>@ticker"
                        .substring(1)
                        .replace(
                                "<id>",
                                tickerStreamRequest.getId() != null
                                        ? tickerStreamRequest.getId().toString()
                                        : "")
                        .replace(
                                "<symbol>",
                                tickerStreamRequest.getSymbol() != null
                                        ? tickerStreamRequest.getSymbol().toString()
                                        : "");
        if ("@".equals(methodName.substring(methodName.length() - 1))) {
            methodName = methodName.substring(0, methodName.length() - 1);
        }

        RequestWrapperDTO<Set<String>, Object> requestWrapperDTO =
                new RequestWrapperDTO.Builder<Set<String>, Object>()
                        .id(getRequestID())
                        .method("SUBSCRIBE")
                        .params(Collections.singleton(methodName))
                        .build();
        Map<String, StreamBlockingQueue<String>> queuesMap =
                connection.subscribe(requestWrapperDTO);
        return queuesMap.get(methodName);
    }

    @SuppressWarnings("rawtypes")
    private void tickerStreamValidateBeforeCall(TickerStreamRequest tickerStreamRequest)
            throws ApiException {
        try {
            Validator validator =
                    Validation.byDefaultProvider()
                            .configure()
                            .messageInterpolator(new ParameterMessageInterpolator())
                            .buildValidatorFactory()
                            .getValidator();

            Set<ConstraintViolation<TickerStreamRequest>> violations =
                    validator.validate(tickerStreamRequest);

            if (!violations.isEmpty()) {
                throw new ConstraintViolationException(violations);
            }
        } catch (SecurityException e) {
            e.printStackTrace();
            throw new ApiException(e.getMessage());
        }
    }

    /**
     * Trade Stream Pushes raw trade updates for a symbol.
     *
     * @param tradeStreamRequest (required)
     * @return TradeStreamResponse
     * @throws ApiException If fail to call the API, e.g. server error or cannot deserialize the
     *     response body
     * @http.response.details
     *     <table border="1">
     * <caption>Response Details</caption>
     * <tr><td> Status Code </td><td> Description </td><td> Response Headers </td></tr>
     * <tr><td> 200 </td><td> Trade Stream </td><td>  -  </td></tr>
     * </table>
     *
     * @see <a
     *     href="https://developers.binance.com/en/docs/catalog/advanced-trading-alpha-trading/api/ws-streams/~#trade-stream">Trade
     *     Stream Documentation</a>
     */
    public StreamBlockingQueueWrapper<TradeStreamResponse> tradeStream(
            TradeStreamRequest tradeStreamRequest) throws ApiException {
        StreamBlockingQueue<String> queue = tradeStreamRaw(tradeStreamRequest);

        TypeToken<TradeStreamResponse> typeToken = new TypeToken<TradeStreamResponse>() {};

        return new StreamBlockingQueueWrapper<>(queue, typeToken);
    }

    public StreamBlockingQueue<String> tradeStreamRaw(TradeStreamRequest tradeStreamRequest)
            throws ApiException {
        tradeStreamValidateBeforeCall(tradeStreamRequest);

        String methodName =
                "/<symbol>@trade"
                        .substring(1)
                        .replace(
                                "<id>",
                                tradeStreamRequest.getId() != null
                                        ? tradeStreamRequest.getId().toString()
                                        : "")
                        .replace(
                                "<symbol>",
                                tradeStreamRequest.getSymbol() != null
                                        ? tradeStreamRequest.getSymbol().toString()
                                        : "");
        if ("@".equals(methodName.substring(methodName.length() - 1))) {
            methodName = methodName.substring(0, methodName.length() - 1);
        }

        RequestWrapperDTO<Set<String>, Object> requestWrapperDTO =
                new RequestWrapperDTO.Builder<Set<String>, Object>()
                        .id(getRequestID())
                        .method("SUBSCRIBE")
                        .params(Collections.singleton(methodName))
                        .build();
        Map<String, StreamBlockingQueue<String>> queuesMap =
                connection.subscribe(requestWrapperDTO);
        return queuesMap.get(methodName);
    }

    @SuppressWarnings("rawtypes")
    private void tradeStreamValidateBeforeCall(TradeStreamRequest tradeStreamRequest)
            throws ApiException {
        try {
            Validator validator =
                    Validation.byDefaultProvider()
                            .configure()
                            .messageInterpolator(new ParameterMessageInterpolator())
                            .buildValidatorFactory()
                            .getValidator();

            Set<ConstraintViolation<TradeStreamRequest>> violations =
                    validator.validate(tradeStreamRequest);

            if (!violations.isEmpty()) {
                throw new ConstraintViolationException(violations);
            }
        } catch (SecurityException e) {
            e.printStackTrace();
            throw new ApiException(e.getMessage());
        }
    }

    public String getRequestID() {
        Random rand = new Random();
        return Integer.toString(Math.abs(rand.nextInt()));
    }
}
