/*
 * Alpha WebSocket Market Streams
 * Access Alpha market streams over WebSocket.
 *
 * The version of the OpenAPI document: 1.0.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package com.binance.connector.client.alpha.websocket.stream.model;

import com.binance.connector.client.alpha.websocket.stream.JSON;
import com.binance.connector.client.common.websocket.dtos.BaseDTO;
import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import jakarta.validation.Valid;
import jakarta.validation.constraints.*;
import java.io.IOException;
import java.nio.charset.StandardCharsets;
import java.util.HashSet;
import java.util.Map;
import java.util.Objects;
import java.util.Set;
import java.util.TreeMap;
import java.util.stream.Collectors;
import org.hibernate.validator.constraints.*;

/** ContractKlineStreamResponse */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class ContractKlineStreamResponse extends BaseDTO {
    public static final String SERIALIZED_NAME_CA = "ca";

    @SerializedName(SERIALIZED_NAME_CA)
    @jakarta.annotation.Nullable
    private String ca;

    public static final String SERIALIZED_NAME_E_LOWER_CASE = "e";

    @SerializedName(SERIALIZED_NAME_E_LOWER_CASE)
    @jakarta.annotation.Nullable
    private String eLowerCase;

    public static final String SERIALIZED_NAME_K_LOWER_CASE = "k";

    @SerializedName(SERIALIZED_NAME_K_LOWER_CASE)
    @jakarta.annotation.Nullable
    private ContractKlineStreamResponseK kLowerCase;

    public ContractKlineStreamResponse() {}

    public ContractKlineStreamResponse ca(@jakarta.annotation.Nullable String ca) {
        this.ca = ca;
        return this;
    }

    /**
     * Contract address@Chain ID
     *
     * @return ca
     */
    @jakarta.annotation.Nullable
    public String getCa() {
        return ca;
    }

    public void setCa(@jakarta.annotation.Nullable String ca) {
        this.ca = ca;
    }

    public ContractKlineStreamResponse eLowerCase(@jakarta.annotation.Nullable String eLowerCase) {
        this.eLowerCase = eLowerCase;
        return this;
    }

    /**
     * Event type
     *
     * @return eLowerCase
     */
    @jakarta.annotation.Nullable
    public String geteLowerCase() {
        return eLowerCase;
    }

    public void seteLowerCase(@jakarta.annotation.Nullable String eLowerCase) {
        this.eLowerCase = eLowerCase;
    }

    public ContractKlineStreamResponse kLowerCase(
            @jakarta.annotation.Nullable ContractKlineStreamResponseK kLowerCase) {
        this.kLowerCase = kLowerCase;
        return this;
    }

    /**
     * Get kLowerCase
     *
     * @return kLowerCase
     */
    @jakarta.annotation.Nullable
    @Valid
    public ContractKlineStreamResponseK getkLowerCase() {
        return kLowerCase;
    }

    public void setkLowerCase(
            @jakarta.annotation.Nullable ContractKlineStreamResponseK kLowerCase) {
        this.kLowerCase = kLowerCase;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        ContractKlineStreamResponse contractKlineStreamResponse = (ContractKlineStreamResponse) o;
        return Objects.equals(this.ca, contractKlineStreamResponse.ca)
                && Objects.equals(this.eLowerCase, contractKlineStreamResponse.eLowerCase)
                && Objects.equals(this.kLowerCase, contractKlineStreamResponse.kLowerCase);
    }

    @Override
    public int hashCode() {
        return Objects.hash(ca, eLowerCase, kLowerCase);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class ContractKlineStreamResponse {\n");
        sb.append("		ca: ").append(toIndentedString(ca)).append("\n");
        sb.append("		eLowerCase: ").append(toIndentedString(eLowerCase)).append("\n");
        sb.append("		kLowerCase: ").append(toIndentedString(kLowerCase)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    public String toUrlQueryString() {
        StringBuilder sb = new StringBuilder();
        Map<String, String> valMap = new TreeMap<String, String>();
        valMap.put("apiKey", getApiKey());
        String caValue = getCa();
        if (caValue != null) {
            String caValueAsString = caValue.toString();
            valMap.put("ca", caValueAsString);
        }
        String eLowerCaseValue = geteLowerCase();
        if (eLowerCaseValue != null) {
            String eLowerCaseValueAsString = eLowerCaseValue.toString();
            valMap.put("eLowerCase", eLowerCaseValueAsString);
        }
        ContractKlineStreamResponseK kLowerCaseValue = getkLowerCase();
        if (kLowerCaseValue != null) {
            String kLowerCaseValueAsString = JSON.getGson().toJson(kLowerCaseValue);
            valMap.put("kLowerCase", kLowerCaseValueAsString);
        }

        valMap.put("timestamp", getTimestamp());
        return asciiEncode(
                valMap.keySet().stream()
                        .map(key -> key + "=" + valMap.get(key))
                        .collect(Collectors.joining("&")));
    }

    public Map<String, Object> toMap() {
        Map<String, Object> valMap = new TreeMap<String, Object>();
        valMap.put("apiKey", getApiKey());
        Object caValue = getCa();
        if (caValue != null) {
            valMap.put("ca", caValue);
        }
        Object eLowerCaseValue = geteLowerCase();
        if (eLowerCaseValue != null) {
            valMap.put("eLowerCase", eLowerCaseValue);
        }
        Object kLowerCaseValue = getkLowerCase();
        if (kLowerCaseValue != null) {
            valMap.put("kLowerCase", kLowerCaseValue);
        }

        valMap.put("timestamp", getTimestamp());
        return valMap;
    }

    public static String asciiEncode(String s) {
        return new String(s.getBytes(), StandardCharsets.US_ASCII);
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces (except the first
     * line).
     */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n		");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("ca");
        openapiFields.add("e");
        openapiFields.add("k");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to
     *     ContractKlineStreamResponse
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!ContractKlineStreamResponse.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                "The required field(s) %s in ContractKlineStreamResponse is not"
                                        + " found in the empty JSON string",
                                ContractKlineStreamResponse.openapiRequiredFields.toString()));
            }
        }

        Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
        // check to see if the JSON string contains additional fields
        for (Map.Entry<String, JsonElement> entry : entries) {
            if (!ContractKlineStreamResponse.openapiFields.contains(entry.getKey())) {
                throw new IllegalArgumentException(
                        String.format(
                                "The field `%s` in the JSON string is not defined in the"
                                        + " `ContractKlineStreamResponse` properties. JSON: %s",
                                entry.getKey(), jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if ((jsonObj.get("ca") != null && !jsonObj.get("ca").isJsonNull())
                && !jsonObj.get("ca").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            "Expected the field `ca` to be a primitive type in the JSON string but"
                                    + " got `%s`",
                            jsonObj.get("ca").toString()));
        }
        if ((jsonObj.get("e") != null && !jsonObj.get("e").isJsonNull())
                && !jsonObj.get("e").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            "Expected the field `e` to be a primitive type in the JSON string but"
                                    + " got `%s`",
                            jsonObj.get("e").toString()));
        }
        // validate the optional field `k`
        if (jsonObj.get("k") != null && !jsonObj.get("k").isJsonNull()) {
            ContractKlineStreamResponseK.validateJsonElement(jsonObj.get("k"));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!ContractKlineStreamResponse.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'ContractKlineStreamResponse' and its
                // subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<ContractKlineStreamResponse> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(ContractKlineStreamResponse.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<ContractKlineStreamResponse>() {
                        @Override
                        public void write(JsonWriter out, ContractKlineStreamResponse value)
                                throws IOException {
                            JsonElement obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public ContractKlineStreamResponse read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            // validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of ContractKlineStreamResponse given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of ContractKlineStreamResponse
     * @throws IOException if the JSON string is invalid with respect to ContractKlineStreamResponse
     */
    public static ContractKlineStreamResponse fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, ContractKlineStreamResponse.class);
    }

    /**
     * Convert an instance of ContractKlineStreamResponse to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
