/*
 * Staking REST API
 * Subscribe to staking products, track positions, and query rewards via the Binance Staking API.
 *
 * The version of the OpenAPI document: 1.0.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package com.binance.connector.client.staking.rest.api;

import static org.junit.Assert.assertEquals;

import com.binance.connector.client.common.ApiClient;
import com.binance.connector.client.common.ApiException;
import com.binance.connector.client.common.ApiResponse;
import com.binance.connector.client.common.auth.BinanceAuthenticationFactory;
import com.binance.connector.client.common.auth.SignatureAuthentication;
import com.binance.connector.client.common.configuration.ClientConfiguration;
import com.binance.connector.client.common.configuration.SignatureConfiguration;
import com.binance.connector.client.common.sign.HmacSignatureGenerator;
import com.binance.connector.client.common.sign.SignatureGenerator;
import com.binance.connector.client.staking.rest.model.GetOnChainYieldsLockedPersonalLeftQuotaResponse;
import com.binance.connector.client.staking.rest.model.GetOnChainYieldsLockedProductListResponse;
import com.binance.connector.client.staking.rest.model.GetOnChainYieldsLockedProductPositionResponse;
import com.binance.connector.client.staking.rest.model.GetOnChainYieldsLockedRedemptionRecordResponse;
import com.binance.connector.client.staking.rest.model.GetOnChainYieldsLockedRewardsHistoryResponse;
import com.binance.connector.client.staking.rest.model.GetOnChainYieldsLockedSubscriptionPreviewResponse;
import com.binance.connector.client.staking.rest.model.GetOnChainYieldsLockedSubscriptionRecordResponse;
import com.binance.connector.client.staking.rest.model.OnChainYieldsAccountResponse;
import com.binance.connector.client.staking.rest.model.RedeemOnChainYieldsLockedProductRequest;
import com.binance.connector.client.staking.rest.model.RedeemOnChainYieldsLockedProductResponse;
import com.binance.connector.client.staking.rest.model.RedeemTo;
import com.binance.connector.client.staking.rest.model.SetOnChainYieldsLockedAutoSubscribeRequest;
import com.binance.connector.client.staking.rest.model.SetOnChainYieldsLockedAutoSubscribeResponse;
import com.binance.connector.client.staking.rest.model.SetOnChainYieldsLockedProductRedeemOptionRequest;
import com.binance.connector.client.staking.rest.model.SetOnChainYieldsLockedProductRedeemOptionResponse;
import com.binance.connector.client.staking.rest.model.SubscribeOnChainYieldsLockedProductRequest;
import com.binance.connector.client.staking.rest.model.SubscribeOnChainYieldsLockedProductResponse;
import jakarta.validation.constraints.*;
import java.io.IOException;
import okhttp3.Call;
import okhttp3.Request;
import org.bouncycastle.crypto.CryptoException;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.mockito.ArgumentCaptor;
import org.mockito.Mockito;

/** API tests for OnChainYieldsApi */
public class OnChainYieldsApiTest {

    private OnChainYieldsApi api;
    private ApiClient apiClientSpy;
    private SignatureGenerator signatureGeneratorSpy;

    @BeforeEach
    public void initApiClient() throws ApiException {
        ClientConfiguration clientConfiguration = new ClientConfiguration();
        clientConfiguration.setUrl("http://localhost:8080");
        SignatureConfiguration signatureConfiguration = new SignatureConfiguration();
        signatureConfiguration.setApiKey("apiKey");
        signatureConfiguration.setSecretKey("secretKey");
        clientConfiguration.setSignatureConfiguration(signatureConfiguration);

        HmacSignatureGenerator signatureGenerator =
                new HmacSignatureGenerator(signatureConfiguration.getSecretKey());
        signatureGeneratorSpy = Mockito.spy(signatureGenerator);
        SignatureAuthentication signatureAuthentication =
                new SignatureAuthentication(
                        signatureConfiguration.getApiKey(), signatureGeneratorSpy);
        SignatureAuthentication authenticationSpy = Mockito.spy(signatureAuthentication);
        Mockito.doReturn("1736393892000").when(authenticationSpy).buildTimestamp();

        BinanceAuthenticationFactory factoryMock = Mockito.mock(BinanceAuthenticationFactory.class);
        Mockito.doReturn(authenticationSpy)
                .when(factoryMock)
                .getAuthentication(signatureConfiguration);

        ApiClient apiClient = new ApiClient(clientConfiguration, factoryMock);

        apiClientSpy = Mockito.spy(apiClient);
        Mockito.doReturn(new ApiResponse<>(200, null))
                .when(apiClientSpy)
                .execute(Mockito.any(), Mockito.any(java.lang.reflect.Type.class));
        Mockito.doReturn(new ApiResponse<>(200, null)).when(apiClientSpy).execute(Mockito.any());
        Mockito.doReturn("1736393892000").when(apiClientSpy).buildTimestamp();

        api = new OnChainYieldsApi(apiClientSpy);
    }

    /**
     * Get On-chain Yields Locked Personal Left Quota (USER_DATA)
     *
     * <p>Get On-chain Yields Locked Personal Left Quota Weight(IP): 50 Security Type: USER_DATA
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void getOnChainYieldsLockedPersonalLeftQuotaTest()
            throws ApiException, CryptoException, IOException {
        String projectId = "1";
        Long recvWindow = 5000L;
        ApiResponse<GetOnChainYieldsLockedPersonalLeftQuotaResponse> response =
                api.getOnChainYieldsLockedPersonalLeftQuota(projectId, recvWindow);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());

        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals(
                "projectId=1&recvWindow=5000&timestamp=1736393892000",
                signInputCaptor.getValue());
        assertEquals(
                "7df8536b8711da59322819086e27ee0e183d18d1f4d6a68377c9c4a87f835ab8",
                actualRequest.url().queryParameter("signature"));
        assertEquals(
                "/sapi/v1/onchain-yields/locked/personalLeftQuota",
                actualRequest.url().encodedPath());
    }

    /**
     * Get On-chain Yields Locked Product List (USER_DATA)
     *
     * <p>Get available On-chain Yields Locked product list Weight(IP): 50 Security Type: USER_DATA
     * Notes: - Get available On-chain Yields Locked product list
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void getOnChainYieldsLockedProductListTest()
            throws ApiException, CryptoException, IOException {
        String asset = "SOL";
        Long current = 1L;
        Long size = 10L;
        Long recvWindow = 5000L;
        ApiResponse<GetOnChainYieldsLockedProductListResponse> response =
                api.getOnChainYieldsLockedProductList(asset, current, size, recvWindow);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());

        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals(
                "asset=SOL&current=1&size=10&recvWindow=5000&timestamp=1736393892000", signInputCaptor.getValue());
        assertEquals(
                "0614571df2ce2c99cb13906242913c37c0beb97a8f84eb72e5203e58177eb3eb",
                actualRequest.url().queryParameter("signature"));
        assertEquals("/sapi/v1/onchain-yields/locked/list", actualRequest.url().encodedPath());
    }

    /**
     * Get On-chain Yields Locked Product Position (USER_DATA)
     *
     * <p>Get On-chain Yields Locked Product Position Weight(IP): 50 Security Type: USER_DATA
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void getOnChainYieldsLockedProductPositionTest()
            throws ApiException, CryptoException, IOException {
        String asset = "BTC";
        String positionId = "1";
        String projectId = "1";
        Long current = 1L;
        Long size = 10L;
        Long recvWindow = 5000L;
        ApiResponse<GetOnChainYieldsLockedProductPositionResponse> response =
                api.getOnChainYieldsLockedProductPosition(
                        asset, positionId, projectId, current, size, recvWindow);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());

        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals(
                "asset=BTC&positionId=1&projectId=1&current=1&size=10&recvWindow=5000&timestamp=1736393892000",
                signInputCaptor.getValue());
        assertEquals(
                "26491df822593223531140428c8ad55dd92494f711d16ef5e54d0c8fef8552bf",
                actualRequest.url().queryParameter("signature"));
        assertEquals(
                "/sapi/v1/onchain-yields/locked/position", actualRequest.url().encodedPath());
    }

    /**
     * Get On-chain Yields Locked Redemption Record (USER_DATA)
     *
     * <p>Get On-chain Yields Locked Redemption Record Weight(IP): 50 Security Type: USER_DATA
     * Notes: - The time between &#x60;startTime&#x60; and &#x60;endTime&#x60; cannot be longer than
     * 3 months. - If &#x60;startTime&#x60; and &#x60;endTime&#x60; are both not sent, then the last
     * 30 days&#39; data will be returned. - If &#x60;startTime&#x60; is sent but
     * &#x60;endTime&#x60; is not sent, the next 30 days&#39; data beginning from
     * &#x60;startTime&#x60; will be returned. - If &#x60;endTime&#x60; is sent but
     * &#x60;startTime&#x60; is not sent, the 30 days&#39; data before &#x60;endTime&#x60; will be
     * returned.
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void getOnChainYieldsLockedRedemptionRecordTest()
            throws ApiException, CryptoException, IOException {
        String positionId = "1";
        String redeemId = "1";
        String asset = "BTC";
        Long startTime = 1623319461670L;
        Long endTime = 1641782889000L;
        Long current = 1L;
        Long size = 10L;
        Long recvWindow = 5000L;
        ApiResponse<GetOnChainYieldsLockedRedemptionRecordResponse> response =
                api.getOnChainYieldsLockedRedemptionRecord(
                        positionId, redeemId, asset, startTime, endTime, current, size, recvWindow);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());

        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals(
                "positionId=1&redeemId=1&asset=BTC&startTime=1623319461670&endTime=1641782889000&current=1&size=10&recvWindow=5000&timestamp=1736393892000",
                signInputCaptor.getValue());
        assertEquals(
                "1d183a6f1e0cdfa8e69e40b818d8d7f40f5d78816b8785b35281ce987a4112ad",
                actualRequest.url().queryParameter("signature"));
        assertEquals(
                "/sapi/v1/onchain-yields/locked/history/redemptionRecord", actualRequest.url().encodedPath());
    }

    /**
     * Get On-chain Yields Locked Rewards History (USER_DATA)
     *
     * <p>Get On-chain Yields Locked Rewards History Weight(IP): 50 Security Type: USER_DATA Notes:
     * - The time between &#x60;startTime&#x60; and &#x60;endTime&#x60; cannot be longer than 3
     * months. - If &#x60;startTime&#x60; and &#x60;endTime&#x60; are both not sent, then the last
     * 30 days&#39; data will be returned. - If &#x60;startTime&#x60; is sent but
     * &#x60;endTime&#x60; is not sent, the next 30 days&#39; data beginning from
     * &#x60;startTime&#x60; will be returned. - If &#x60;endTime&#x60; is sent but
     * &#x60;startTime&#x60; is not sent, the 30 days&#39; data before &#x60;endTime&#x60; will be
     * returned.
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void getOnChainYieldsLockedRewardsHistoryTest()
            throws ApiException, CryptoException, IOException {
        String positionId = "1";
        String asset = "BTC";
        Long startTime = 1623319461670L;
        Long endTime = 1641782889000L;
        Long current = 1L;
        Long size = 10L;
        Long recvWindow = 5000L;
        ApiResponse<GetOnChainYieldsLockedRewardsHistoryResponse> response =
                api.getOnChainYieldsLockedRewardsHistory(
                        positionId, asset, startTime, endTime, current, size, recvWindow);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());

        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals(
                "positionId=1&asset=BTC&startTime=1623319461670&endTime=1641782889000&current=1&size=10&recvWindow=5000&timestamp=1736393892000", signInputCaptor.getValue());
        assertEquals(
                "409c7bf8794eb5a049caa12f3a505d43a5f87db6f5f8d74d257cd34d20f331ff",
                actualRequest.url().queryParameter("signature"));
        assertEquals(
                "/sapi/v1/onchain-yields/locked/history/rewardsRecord", actualRequest.url().encodedPath());
    }

    /**
     * Get On-chain Yields Locked Subscription Preview (USER_DATA)
     *
     * <p>Get On-chain Yields Locked Subscription Preview Weight(IP): 50 Security Type: USER_DATA
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void getOnChainYieldsLockedSubscriptionPreviewTest()
            throws ApiException, CryptoException, IOException {
        String projectId = "1";
        Double amount = 1.0d;
        Boolean autoSubscribe = true;
        Long recvWindow = 5000L;
        ApiResponse<GetOnChainYieldsLockedSubscriptionPreviewResponse> response =
                api.getOnChainYieldsLockedSubscriptionPreview(
                        projectId, amount, autoSubscribe, recvWindow);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());

        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals(
                "projectId=1&amount=1&autoSubscribe=true&recvWindow=5000&timestamp=1736393892000",
                signInputCaptor.getValue());
        assertEquals(
                "ecc2c9a2d68e05051696f604598a440ee1aeaed7b90bc546e17a9edcdc6ae43a",
                actualRequest.url().queryParameter("signature"));
        assertEquals(
                "/sapi/v1/onchain-yields/locked/subscriptionPreview",
                actualRequest.url().encodedPath());
    }

    /**
     * Get On-chain Yields Locked Subscription Record (USER_DATA)
     *
     * <p>Get On-chain Yields Locked Subscription Record Weight(IP): 50 Security Type: USER_DATA
     * Notes: - The time between &#x60;startTime&#x60; and &#x60;endTime&#x60; cannot be longer than
     * 3 months. - If &#x60;startTime&#x60; and &#x60;endTime&#x60; are both not sent, then the last
     * 30 days&#39; data will be returned. - If &#x60;startTime&#x60; is sent but
     * &#x60;endTime&#x60; is not sent, the next 30 days&#39; data beginning from
     * &#x60;startTime&#x60; will be returned. - If &#x60;endTime&#x60; is sent but
     * &#x60;startTime&#x60; is not sent, the 30 days&#39; data before &#x60;endTime&#x60; will be
     * returned.
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void getOnChainYieldsLockedSubscriptionRecordTest()
            throws ApiException, CryptoException, IOException {
        String purchaseId = "1";
        String clientId = "1";
        String asset = "BTC";
        Long startTime = 1623319461670L;
        Long endTime = 1641782889000L;
        Long current = 1L;
        Long size = 10L;
        Long recvWindow = 5000L;
        ApiResponse<GetOnChainYieldsLockedSubscriptionRecordResponse> response =
                api.getOnChainYieldsLockedSubscriptionRecord(
                        purchaseId, clientId, asset, startTime, endTime, current, size, recvWindow);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());

        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals(
                "purchaseId=1&clientId=1&asset=BTC&startTime=1623319461670&endTime=1641782889000&current=1&size=10&recvWindow=5000&timestamp=1736393892000",
                signInputCaptor.getValue());
        assertEquals(
                "1229a0f8ac2c76bec7b1a680e20a85bf6227c75f823e248f855203d8ccfc94af",
                actualRequest.url().queryParameter("signature"));
        assertEquals(
                "/sapi/v1/onchain-yields/locked/history/subscriptionRecord",
                actualRequest.url().encodedPath());
    }

    /**
     * On-chain Yields Account (USER_DATA)
     *
     * <p>On-chain Yields Account query Weight(IP): 50 Security Type: USER_DATA
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void onChainYieldsAccountTest() throws ApiException, CryptoException, IOException {
        Long recvWindow = 5000L;
        ApiResponse<OnChainYieldsAccountResponse> response = api.onChainYieldsAccount(recvWindow);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());

        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals("recvWindow=5000&timestamp=1736393892000", signInputCaptor.getValue());
        assertEquals(
                "2cdd1e484bce80021437bee6b762e6a276b1954c3a0c011a16f6f2f6a47aba75",
                actualRequest.url().queryParameter("signature"));
        assertEquals("/sapi/v1/onchain-yields/account", actualRequest.url().encodedPath());
    }

    /**
     * Redeem On-chain Yields Locked Product (TRADE)
     *
     * <p>Redeem On-chain Yields Locked Product Weight(IP): 200 Security Type: TRADE Notes: - You
     * need to open &#x60;Enable Spot &amp; Margin Trading&#x60; permission for the API Key which
     * requests this endpoint.
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void redeemOnChainYieldsLockedProductTest()
            throws ApiException, CryptoException, IOException {
        RedeemOnChainYieldsLockedProductRequest redeemOnChainYieldsLockedProductRequest =
                new RedeemOnChainYieldsLockedProductRequest();
        redeemOnChainYieldsLockedProductRequest.positionId("1");

        ApiResponse<RedeemOnChainYieldsLockedProductResponse> response =
                api.redeemOnChainYieldsLockedProduct(redeemOnChainYieldsLockedProductRequest);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());

        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals(
                "timestamp=1736393892000positionId=1", signInputCaptor.getValue());
        assertEquals(
                "05345189012e725ecf394a569c76b468c3a9d6377f5b644a1b90e1551920ce91",
                actualRequest.url().queryParameter("signature"));
        assertEquals("/sapi/v1/onchain-yields/locked/redeem", actualRequest.url().encodedPath());
    }

    /**
     * Set On-chain Yields Locked Auto Subscribe (USER_DATA)
     *
     * <p>Set On-chain Yield locked auto subscribe Weight(IP): 50 Security Type: USER_DATA
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void setOnChainYieldsLockedAutoSubscribeTest()
            throws ApiException, CryptoException, IOException {
        SetOnChainYieldsLockedAutoSubscribeRequest setOnChainYieldsLockedAutoSubscribeRequest =
                new SetOnChainYieldsLockedAutoSubscribeRequest();
        setOnChainYieldsLockedAutoSubscribeRequest.positionId("1");
        setOnChainYieldsLockedAutoSubscribeRequest.autoSubscribe(true);

        ApiResponse<SetOnChainYieldsLockedAutoSubscribeResponse> response =
                api.setOnChainYieldsLockedAutoSubscribe(setOnChainYieldsLockedAutoSubscribeRequest);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());

        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals(
                "timestamp=1736393892000positionId=1&autoSubscribe=true", signInputCaptor.getValue());
        assertEquals(
                "ccf4485e39f5a5902d6132dc3dff0cebd2ef3a8bd3920de8ebf5ab46d448213a",
                actualRequest.url().queryParameter("signature"));
        assertEquals(
                "/sapi/v1/onchain-yields/locked/setAutoSubscribe", actualRequest.url().encodedPath());
    }

    /**
     * Set On-chain Yields Locked Product Redeem Option (USER_DATA)
     *
     * <p>Set On-chain Yields redeem option for Locked product Weight(IP): 50 Security Type:
     * USER_DATA
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void setOnChainYieldsLockedProductRedeemOptionTest()
            throws ApiException, CryptoException, IOException {
        SetOnChainYieldsLockedProductRedeemOptionRequest
                setOnChainYieldsLockedProductRedeemOptionRequest =
                        new SetOnChainYieldsLockedProductRedeemOptionRequest();
        setOnChainYieldsLockedProductRedeemOptionRequest.positionId("1");
        setOnChainYieldsLockedProductRedeemOptionRequest.redeemTo(RedeemTo.SPOT);

        ApiResponse<SetOnChainYieldsLockedProductRedeemOptionResponse> response =
                api.setOnChainYieldsLockedProductRedeemOption(
                        setOnChainYieldsLockedProductRedeemOptionRequest);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());

        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals(
                "timestamp=1736393892000positionId=1&redeemTo=SPOT",
                signInputCaptor.getValue());
        assertEquals(
                "54b4c36882fe7b0b06bb851e208c082cdc9e6d0ca526a21cbb323a3659e02a2e",
                actualRequest.url().queryParameter("signature"));
        assertEquals(
                "/sapi/v1/onchain-yields/locked/setRedeemOption",
                actualRequest.url().encodedPath());
    }

    /**
     * Subscribe On-chain Yields Locked Product (TRADE)
     *
     * <p>Subscribe On-chain Yields Locked Product Weight(IP): 200 Security Type: TRADE Notes: - You
     * need to open &#x60;Enable Spot &amp; Margin Trading&#x60; permission for the API Key which
     * requests this endpoint.
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void subscribeOnChainYieldsLockedProductTest()
            throws ApiException, CryptoException, IOException {
        SubscribeOnChainYieldsLockedProductRequest subscribeOnChainYieldsLockedProductRequest =
                new SubscribeOnChainYieldsLockedProductRequest();
        subscribeOnChainYieldsLockedProductRequest.projectId("1");
        subscribeOnChainYieldsLockedProductRequest.amount(1.0d);

        ApiResponse<SubscribeOnChainYieldsLockedProductResponse> response =
                api.subscribeOnChainYieldsLockedProduct(subscribeOnChainYieldsLockedProductRequest);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());

        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals(
                "timestamp=1736393892000amount=1&sourceAccount=SPOT&redeemTo=FLEXIBLE&autoSubscribe=false&projectId=1", signInputCaptor.getValue());
        assertEquals(
                "08d0295c51d9b1c64b3d0d607b234347f113741c6e1fc995b31e680001149ee7",
                actualRequest.url().queryParameter("signature"));
        assertEquals(
                "/sapi/v1/onchain-yields/locked/subscribe", actualRequest.url().encodedPath());
    }
}
