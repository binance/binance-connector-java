/*
 * Mining REST API
 * Query mining status, earnings, and account data via the Binance Pool API.
 *
 * The version of the OpenAPI document: 1.0.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package com.binance.connector.client.mining.rest.api;

import static org.junit.Assert.assertEquals;

import com.binance.connector.client.common.ApiClient;
import com.binance.connector.client.common.ApiException;
import com.binance.connector.client.common.ApiResponse;
import com.binance.connector.client.common.auth.BinanceAuthenticationFactory;
import com.binance.connector.client.common.auth.SignatureAuthentication;
import com.binance.connector.client.common.configuration.ClientConfiguration;
import com.binance.connector.client.common.configuration.SignatureConfiguration;
import com.binance.connector.client.common.sign.HmacSignatureGenerator;
import com.binance.connector.client.common.sign.SignatureGenerator;
import com.binance.connector.client.mining.rest.model.AccountListResponse;
import com.binance.connector.client.mining.rest.model.AcquiringAlgorithmResponse;
import com.binance.connector.client.mining.rest.model.AcquiringCoinnameResponse;
import com.binance.connector.client.mining.rest.model.CancelHashrateResaleConfigurationRequest;
import com.binance.connector.client.mining.rest.model.CancelHashrateResaleConfigurationResponse;
import com.binance.connector.client.mining.rest.model.EarningsListResponse;
import com.binance.connector.client.mining.rest.model.ExtraBonusListResponse;
import com.binance.connector.client.mining.rest.model.HashrateResaleDetailResponse;
import com.binance.connector.client.mining.rest.model.HashrateResaleListResponse;
import com.binance.connector.client.mining.rest.model.HashrateResaleRequestRequest;
import com.binance.connector.client.mining.rest.model.HashrateResaleRequestResponse;
import com.binance.connector.client.mining.rest.model.MiningAccountEarningResponse;
import com.binance.connector.client.mining.rest.model.RequestForDetailMinerListResponse;
import com.binance.connector.client.mining.rest.model.RequestForMinerListResponse;
import com.binance.connector.client.mining.rest.model.StatisticListResponse;
import jakarta.validation.constraints.*;
import java.io.IOException;
import okhttp3.Call;
import okhttp3.Request;
import org.bouncycastle.crypto.CryptoException;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.mockito.ArgumentCaptor;
import org.mockito.Mockito;

/** API tests for DefaultApi */
public class DefaultApiTest {

    private DefaultApi api;
    private ApiClient apiClientSpy;
    private SignatureGenerator signatureGeneratorSpy;

    @BeforeEach
    public void initApiClient() throws ApiException {
        ClientConfiguration clientConfiguration = new ClientConfiguration();
        clientConfiguration.setUrl("http://localhost:8080");
        SignatureConfiguration signatureConfiguration = new SignatureConfiguration();
        signatureConfiguration.setApiKey("apiKey");
        signatureConfiguration.setSecretKey("secretKey");
        clientConfiguration.setSignatureConfiguration(signatureConfiguration);

        HmacSignatureGenerator signatureGenerator =
                new HmacSignatureGenerator(signatureConfiguration.getSecretKey());
        signatureGeneratorSpy = Mockito.spy(signatureGenerator);
        SignatureAuthentication signatureAuthentication =
                new SignatureAuthentication(
                        signatureConfiguration.getApiKey(), signatureGeneratorSpy);
        SignatureAuthentication authenticationSpy = Mockito.spy(signatureAuthentication);
        Mockito.doReturn("1736393892000").when(authenticationSpy).buildTimestamp();

        BinanceAuthenticationFactory factoryMock = Mockito.mock(BinanceAuthenticationFactory.class);
        Mockito.doReturn(authenticationSpy)
                .when(factoryMock)
                .getAuthentication(signatureConfiguration);

        ApiClient apiClient = new ApiClient(clientConfiguration, factoryMock);

        apiClientSpy = Mockito.spy(apiClient);
        Mockito.doReturn(new ApiResponse<>(200, null))
                .when(apiClientSpy)
                .execute(Mockito.any(), Mockito.any(java.lang.reflect.Type.class));
        Mockito.doReturn(new ApiResponse<>(200, null)).when(apiClientSpy).execute(Mockito.any());
        Mockito.doReturn("1736393892000").when(apiClientSpy).buildTimestamp();

        api = new DefaultApi(apiClientSpy);
    }

    /**
     * Account List (USER_DATA)
     *
     * <p>Query Account List Weight(IP): 1 Security Type: USER_DATA
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void accountListTest() throws ApiException, CryptoException, IOException {
        String algo = "sha256";
        String userName = "test";
        Long recvWindow = 5000L;
        ApiResponse<AccountListResponse> response = api.accountList(algo, userName, recvWindow);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());

        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals("algo=sha256&userName=test&recvWindow=5000&timestamp=1736393892000", signInputCaptor.getValue());
        assertEquals("05518147bd340521fe0a08f11f7330b0bb8b42976e9cb2e54a3f1576367a5448", actualRequest.url().queryParameter("signature"));
        assertEquals("/sapi/v1/mining/statistics/user/list", actualRequest.url().encodedPath());
    }

    /**
     * Acquiring Algorithm (MARKET_DATA)
     *
     * <p>Acquiring Algorithm Weight(IP): 1 Security Type: MARKET_DATA
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void acquiringAlgorithmTest() throws ApiException, CryptoException, IOException {
        ApiResponse<AcquiringAlgorithmResponse> response = api.acquiringAlgorithm();

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals(
                null, actualRequest.url().queryParameter("signature"));
        assertEquals("/sapi/v1/mining/pub/algoList", actualRequest.url().encodedPath());
    }

    /**
     * Acquiring CoinName (MARKET_DATA)
     *
     * <p>Acquiring CoinName Weight(IP): 1 Security Type: MARKET_DATA
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void acquiringCoinnameTest() throws ApiException, CryptoException, IOException {
        ApiResponse<AcquiringCoinnameResponse> response = api.acquiringCoinname();

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals(
                null, actualRequest.url().queryParameter("signature"));
        assertEquals("/sapi/v1/mining/pub/coinList", actualRequest.url().encodedPath());
    }

    /**
     * Cancel hashrate resale configuration (USER_DATA)
     *
     * <p>Cancel hashrate resale configuration Weight(IP): 1 Security Type: USER_DATA
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void cancelHashrateResaleConfigurationTest()
            throws ApiException, CryptoException, IOException {
        CancelHashrateResaleConfigurationRequest cancelHashrateResaleConfigurationRequest =
                new CancelHashrateResaleConfigurationRequest();
        cancelHashrateResaleConfigurationRequest.configId(168L);
        cancelHashrateResaleConfigurationRequest.userName("test");

        ApiResponse<CancelHashrateResaleConfigurationResponse> response =
                api.cancelHashrateResaleConfiguration(cancelHashrateResaleConfigurationRequest);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());

        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals(
                "timestamp=1736393892000configId=168&userName=test", signInputCaptor.getValue());
        assertEquals(
                "8ac29a5c1358d69ee45d406694a67e0c724b0110ae60bba736b1ec7ed6f57e5f",
                actualRequest.url().queryParameter("signature"));
        assertEquals("/sapi/v1/mining/hash-transfer/config/cancel", actualRequest.url().encodedPath());
    }

    /**
     * Earnings List (USER_DATA)
     *
     * <p>Query Earnings List Weight(IP): 1 Security Type: USER_DATA
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void earningsListTest() throws ApiException, CryptoException, IOException {
        String algo = "sha256";
        String userName = "test";
        String coin = "BTC";
        Long startDate = 1770736694138L;
        Long endDate = 1770736694138L;
        Long pageIndex = 1L;
        Long pageSize = 10L;
        Long recvWindow = 5000L;
        ApiResponse<EarningsListResponse> response =
                api.earningsList(
                        algo, userName, coin, startDate, endDate, pageIndex, pageSize, recvWindow);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());

        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals("algo=sha256&userName=test&coin=BTC&startDate=1770736694138&endDate=1770736694138&pageIndex=1&pageSize=10&recvWindow=5000&timestamp=1736393892000", signInputCaptor.getValue());
        assertEquals("a502ab402c8a6ee527689734d02ae815cf9c4e6e0e0a492d913626e92a53ff48", actualRequest.url().queryParameter("signature"));
        assertEquals("/sapi/v1/mining/payment/list", actualRequest.url().encodedPath());
    }

    /**
     * Extra Bonus List (USER_DATA)
     *
     * <p>Extra Bonus List Weight(IP): 1 Security Type: USER_DATA
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void extraBonusListTest() throws ApiException, CryptoException, IOException {
        String algo = "sha256";
        String userName = "test";
        String coin = "BTC";
        Long startDate = 1770736694138L;
        Long endDate = 1770736694138L;
        Long pageIndex = 1L;
        Long pageSize = 10L;
        Long recvWindow = 5000L;
        ApiResponse<ExtraBonusListResponse> response =
                api.extraBonusList(
                        algo, userName, coin, startDate, endDate, pageIndex, pageSize, recvWindow);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());

        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals("algo=sha256&userName=test&coin=BTC&startDate=1770736694138&endDate=1770736694138&pageIndex=1&pageSize=10&recvWindow=5000&timestamp=1736393892000", signInputCaptor.getValue());
        assertEquals("a502ab402c8a6ee527689734d02ae815cf9c4e6e0e0a492d913626e92a53ff48", actualRequest.url().queryParameter("signature"));
        assertEquals("/sapi/v1/mining/payment/other", actualRequest.url().encodedPath());
    }

    /**
     * Hashrate Resale Detail (USER_DATA)
     *
     * <p>Hashrate Resale Detail(USER_DATA) Weight(IP): 1 Security Type: USER_DATA
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void hashrateResaleDetailTest() throws ApiException, CryptoException, IOException {
        Long configId = 168L;
        Long pageIndex = 1L;
        Long pageSize = 10L;
        Long recvWindow = 5000L;
        ApiResponse<HashrateResaleDetailResponse> response =
                api.hashrateResaleDetail(configId, pageIndex, pageSize, recvWindow);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());

        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals("configId=168&pageIndex=1&pageSize=10&recvWindow=5000&timestamp=1736393892000", signInputCaptor.getValue());
        assertEquals(
                "cfb957f7f5f89e84c3eb96d83cd6eed901b60947c857d7387b46c6020a338ed8",
                actualRequest.url().queryParameter("signature"));
        assertEquals("/sapi/v1/mining/hash-transfer/profit/details", actualRequest.url().encodedPath());
    }

    /**
     * Hashrate Resale List (USER_DATA)
     *
     * <p>Hashrate Resale List Weight(IP): 1 Security Type: USER_DATA
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void hashrateResaleListTest() throws ApiException, CryptoException, IOException {
        Long pageIndex = 1L;
        Long pageSize = 10L;
        Long recvWindow = 5000L;
        ApiResponse<HashrateResaleListResponse> response =
                api.hashrateResaleList(pageIndex, pageSize, recvWindow);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());

        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals("pageIndex=1&pageSize=10&recvWindow=5000&timestamp=1736393892000", signInputCaptor.getValue());
        assertEquals(
                "3afaf7183f03d1042c49aa8527fffd90347a96e61867b5bfd5ae183f8fa1f746", actualRequest.url().queryParameter("signature"));
        assertEquals("/sapi/v1/mining/hash-transfer/config/details/list", actualRequest.url().encodedPath());
    }

    /**
     * Hashrate Resale Request (USER_DATA)
     *
     * <p>Hashrate Resale Request Weight(IP): 1 Security Type: USER_DATA
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void hashrateResaleRequestTest() throws ApiException, CryptoException, IOException {
        HashrateResaleRequestRequest hashrateResaleRequestRequest =
                new HashrateResaleRequestRequest();
        hashrateResaleRequestRequest.userName("test");
        hashrateResaleRequestRequest.algo("sha256");
        hashrateResaleRequestRequest.endDate(1770736694138L);
        hashrateResaleRequestRequest.startDate(1770736694138L);
        hashrateResaleRequestRequest.toPoolUser("S19pro");
        hashrateResaleRequestRequest.hashRate(100000000L);

        ApiResponse<HashrateResaleRequestResponse> response =
                api.hashrateResaleRequest(hashrateResaleRequestRequest);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());

        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals("timestamp=1736393892000endDate=1770736694138&hashRate=100000000&toPoolUser=S19pro&userName=test&algo=sha256&startDate=1770736694138", signInputCaptor.getValue());
        assertEquals(
                "a7eb79f0f158615b142438fc6fdb119ebb0a535aebb963075ab4cd6825b3c4af",
                actualRequest.url().queryParameter("signature"));
        assertEquals("/sapi/v1/mining/hash-transfer/config", actualRequest.url().encodedPath());
    }

    /**
     * Mining Account Earning (USER_DATA)
     *
     * <p>Mining Account Earning Weight(IP): 1 Security Type: USER_DATA
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void miningAccountEarningTest() throws ApiException, CryptoException, IOException {
        String algo = "sha256";
        Long startDate = 1770736694138L;
        Long endDate = 1770736694138L;
        Long pageIndex = 1L;
        Long pageSize = 10L;
        Long recvWindow = 5000L;
        ApiResponse<MiningAccountEarningResponse> response =
                api.miningAccountEarning(algo, startDate, endDate, pageIndex, pageSize, recvWindow);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());

        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals("algo=sha256&startDate=1770736694138&endDate=1770736694138&pageIndex=1&pageSize=10&recvWindow=5000&timestamp=1736393892000", signInputCaptor.getValue());
        assertEquals(
                "ae6b2077a850d3bf994d1c66a9b318a7ba0e8c5ee392c46f6934bd8752dc594b",
                actualRequest.url().queryParameter("signature"));
        assertEquals("/sapi/v1/mining/payment/uid", actualRequest.url().encodedPath());
    }

    /**
     * Request for Detail Miner List (USER_DATA)
     *
     * <p>Request for Detail Miner List Weight(IP): 1 Security Type: USER_DATA
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void requestForDetailMinerListTest() throws ApiException, CryptoException, IOException {
        String algo = "sha256";
        String userName = "test";
        String workerName = "bhdc1.16A10404B";
        Long recvWindow = 5000L;
        ApiResponse<RequestForDetailMinerListResponse> response =
                api.requestForDetailMinerList(algo, userName, workerName, recvWindow);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());

        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals("algo=sha256&userName=test&workerName=bhdc1.16A10404B&recvWindow=5000&timestamp=1736393892000", signInputCaptor.getValue());
        assertEquals(
                "bb6000cd13b802c98208a3fb719f7069a0952d49d7f77ece496c45f478c56bef",
                actualRequest.url().queryParameter("signature"));
        assertEquals("/sapi/v1/mining/worker/detail", actualRequest.url().encodedPath());
    }

    /**
     * Request for Miner List (USER_DATA)
     *
     * <p>Request for Miner List Weight(IP): 1 Security Type: USER_DATA
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void requestForMinerListTest() throws ApiException, CryptoException, IOException {
        String algo = "sha256";
        String userName = "test";
        Long pageIndex = 1L;
        Long sort = 0L;
        Long sortColumn = 1L;
        Long workerStatus = 0L;
        Long recvWindow = 5000L;
        ApiResponse<RequestForMinerListResponse> response =
                api.requestForMinerList(
                        algo, userName, pageIndex, sort, sortColumn, workerStatus, recvWindow);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());

        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals("algo=sha256&userName=test&pageIndex=1&sort=0&sortColumn=1&workerStatus=0&recvWindow=5000&timestamp=1736393892000", signInputCaptor.getValue());
        assertEquals(
                "05f15b7628289441d80d57937ac1025b7a619411ffda6cf53ea099e6433f336d", actualRequest.url().queryParameter("signature"));
        assertEquals("/sapi/v1/mining/worker/list", actualRequest.url().encodedPath());
    }

    /**
     * Statistic List (USER_DATA)
     *
     * <p>Statistic List Weight(IP): 1 Security Type: USER_DATA
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void statisticListTest() throws ApiException, CryptoException, IOException {
        String algo = "sha256";
        String userName = "test";
        Long recvWindow = 5000L;
        ApiResponse<StatisticListResponse> response = api.statisticList(algo, userName, recvWindow);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());

        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals("algo=sha256&userName=test&recvWindow=5000&timestamp=1736393892000", signInputCaptor.getValue());
        assertEquals("05518147bd340521fe0a08f11f7330b0bb8b42976e9cb2e54a3f1576367a5448", actualRequest.url().queryParameter("signature"));
        assertEquals("/sapi/v1/mining/statistics/user/status", actualRequest.url().encodedPath());
    }
}
