/*
 * Stocks Trading WebSocket Streams
 * WebSocket stream definitions for Binance Stocks Trading. Base URL: wss://nbstream.binance.com/equity
 *
 * The version of the OpenAPI document: 1.0.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package com.binance.connector.client.stocks.websocket.stream.api;

import com.binance.connector.client.common.ApiException;
import com.binance.connector.client.common.configuration.SignatureConfiguration;
import com.binance.connector.client.common.websocket.adapter.stream.StreamConnectionWrapper;
import com.binance.connector.client.common.websocket.configuration.WebSocketClientConfiguration;
import com.binance.connector.client.common.websocket.dtos.RequestWrapperDTO;
import com.binance.connector.client.common.websocket.service.StreamBlockingQueueWrapper;
import com.binance.connector.client.stocks.websocket.stream.model.CalendarStreamResponse;
import com.binance.connector.client.stocks.websocket.stream.model.KlineStreamRequest;
import com.binance.connector.client.stocks.websocket.stream.model.KlineStreamResponse;
import com.binance.connector.client.stocks.websocket.stream.model.PriceStreamResponse;
import com.binance.connector.client.stocks.websocket.stream.model.QuoteStreamRequest;
import com.binance.connector.client.stocks.websocket.stream.model.QuoteStreamResponse;
import com.binance.connector.client.stocks.websocket.stream.model.TradabilityStreamRequest;
import com.binance.connector.client.stocks.websocket.stream.model.TradabilityStreamResponse;
import com.binance.connector.client.stocks.websocket.stream.model.TradingStatusStreamRequest;
import com.binance.connector.client.stocks.websocket.stream.model.TradingStatusStreamResponse;
import jakarta.validation.constraints.*;
import java.io.File;
import java.io.IOException;
import java.net.URISyntaxException;
import java.net.URL;
import java.nio.file.Files;
import java.nio.file.Paths;
import java.util.Set;
import java.util.concurrent.CompletableFuture;
import org.eclipse.jetty.websocket.api.RemoteEndpoint;
import org.eclipse.jetty.websocket.api.Session;
import org.eclipse.jetty.websocket.client.WebSocketClient;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.mockito.ArgumentCaptor;
import org.mockito.Mockito;
import org.skyscreamer.jsonassert.JSONAssert;

/** API tests for MarketStreamsApi */
public class MarketStreamsApiTest {

    private MarketStreamsApi api;
    private StreamConnectionWrapper connectionSpy;
    private Session sessionMock;

    @BeforeEach
    public void initApiClient() throws Exception {
        URL resource = MarketStreamsApi.class.getResource("/test-ed25519-prv-key.pem");
        SignatureConfiguration signatureConfiguration = new SignatureConfiguration();
        signatureConfiguration.setApiKey("apiKey");
        File file = new File(resource.toURI());
        signatureConfiguration.setPrivateKey(file.getAbsolutePath());
        WebSocketClientConfiguration clientConfiguration = new WebSocketClientConfiguration();
        // @TODO: run tests for LOGON as well
        clientConfiguration.setAutoLogon(false);
        clientConfiguration.setSignatureConfiguration(signatureConfiguration);
        clientConfiguration.setUrl("wss://localhost:8080");

        WebSocketClient webSocketClient = Mockito.mock(WebSocketClient.class);
        CompletableFuture<Session> sessionCompletableFuture = new CompletableFuture<>();
        Mockito.doReturn(sessionCompletableFuture)
                .when(webSocketClient)
                .connect(Mockito.any(), Mockito.any(), Mockito.any());
        sessionMock = Mockito.mock(Session.class);

        RemoteEndpoint remoteEndpointMock = Mockito.mock(RemoteEndpoint.class);
        Mockito.doReturn(remoteEndpointMock).when(sessionMock).getRemote();

        sessionCompletableFuture.complete(sessionMock);
        StreamConnectionWrapper connectionWrapper =
                new StreamConnectionWrapper(clientConfiguration, webSocketClient);
        connectionSpy = Mockito.spy(connectionWrapper);
        Mockito.doReturn(1736393892000L).when(connectionSpy).getTimestamp();
        Mockito.doNothing().when(connectionSpy).setUserAgent(Mockito.anyString());
        connectionSpy.connect();
        MarketStreamsApi apiClient = new MarketStreamsApi(connectionSpy);
        api = Mockito.spy(apiClient);
        Mockito.doReturn("eaf3292c-64b6-4c04-ad4f-4ca2608b42b4").when(api).getRequestID();
    }

    /**
     * Calendar Stream
     *
     * <p>Single-stream broadcast of market-phase transitions. One message per transition; no
     * periodic heartbeat payload. Server polls every 5 seconds.
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void calendarStreamTest() throws ApiException, URISyntaxException, IOException {
        StreamBlockingQueueWrapper<CalendarStreamResponse> response = api.calendarStream();
        ArgumentCaptor<RequestWrapperDTO<Set<String>, CalendarStreamResponse>> callArgumentCaptor =
                ArgumentCaptor.forClass(RequestWrapperDTO.class);
        Mockito.verify(connectionSpy).innerSend(callArgumentCaptor.capture());
        ArgumentCaptor<String> sendArgumentCaptor = ArgumentCaptor.forClass(String.class);
        RemoteEndpoint remote = sessionMock.getRemote();
        Mockito.verify(remote).sendString(sendArgumentCaptor.capture(), Mockito.any());
        RequestWrapperDTO<Set<String>, CalendarStreamResponse> requestWrapperDTO =
                callArgumentCaptor.getValue();
        Set<String> params = requestWrapperDTO.getParams();
        // TODO: test validations
        String sentPayload = sendArgumentCaptor.getValue();

        URL resource =
                MarketStreamsApiTest.class.getResource(
                        "/expected/stream/MarketStreamsApi/calendar-test.json");

        String expectedJson = Files.readString(Paths.get(resource.toURI()));
        JSONAssert.assertEquals(expectedJson, sentPayload, true);
    }

    /**
     * Kline Stream
     *
     * <p>Per-symbol candlestick updates. One stream per (symbol, interval) combination. Supported
     * intervals: 5m, 1h, 1d, 1w, 1M.
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void klineStreamTest() throws ApiException, URISyntaxException, IOException {
        KlineStreamRequest klineStreamRequest = new KlineStreamRequest();

        StreamBlockingQueueWrapper<KlineStreamResponse> response =
                api.klineStream(klineStreamRequest);
        ArgumentCaptor<RequestWrapperDTO<Set<String>, KlineStreamResponse>> callArgumentCaptor =
                ArgumentCaptor.forClass(RequestWrapperDTO.class);
        Mockito.verify(connectionSpy).innerSend(callArgumentCaptor.capture());
        ArgumentCaptor<String> sendArgumentCaptor = ArgumentCaptor.forClass(String.class);
        RemoteEndpoint remote = sessionMock.getRemote();
        Mockito.verify(remote).sendString(sendArgumentCaptor.capture(), Mockito.any());
        RequestWrapperDTO<Set<String>, KlineStreamResponse> requestWrapperDTO =
                callArgumentCaptor.getValue();
        Set<String> params = requestWrapperDTO.getParams();
        // TODO: test validations
        String sentPayload = sendArgumentCaptor.getValue();

        URL resource =
                MarketStreamsApiTest.class.getResource(
                        "/expected/stream/MarketStreamsApi/symbol@kline_interval-test.json");

        String expectedJson = Files.readString(Paths.get(resource.toURI()));
        JSONAssert.assertEquals(expectedJson, sentPayload, true);
    }

    /**
     * Price Stream
     *
     * <p>Push-all price snapshot, polled every 3 seconds. One message carries the latest price for
     * every active US-equity symbol.
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void priceStreamTest() throws ApiException, URISyntaxException, IOException {
        StreamBlockingQueueWrapper<PriceStreamResponse> response = api.priceStream();
        ArgumentCaptor<RequestWrapperDTO<Set<String>, PriceStreamResponse>> callArgumentCaptor =
                ArgumentCaptor.forClass(RequestWrapperDTO.class);
        Mockito.verify(connectionSpy).innerSend(callArgumentCaptor.capture());
        ArgumentCaptor<String> sendArgumentCaptor = ArgumentCaptor.forClass(String.class);
        RemoteEndpoint remote = sessionMock.getRemote();
        Mockito.verify(remote).sendString(sendArgumentCaptor.capture(), Mockito.any());
        RequestWrapperDTO<Set<String>, PriceStreamResponse> requestWrapperDTO =
                callArgumentCaptor.getValue();
        Set<String> params = requestWrapperDTO.getParams();
        // TODO: test validations
        String sentPayload = sendArgumentCaptor.getValue();

        URL resource =
                MarketStreamsApiTest.class.getResource(
                        "/expected/stream/MarketStreamsApi/price-test.json");

        String expectedJson = Files.readString(Paths.get(resource.toURI()));
        JSONAssert.assertEquals(expectedJson, sentPayload, true);
    }

    /**
     * Quote Stream
     *
     * <p>Per-symbol real-time best-bid / best-ask. Each symbol has its own stream. Per-symbol
     * throttle: at most one push per symbol every 200 ms.
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void quoteStreamTest() throws ApiException, URISyntaxException, IOException {
        QuoteStreamRequest quoteStreamRequest = new QuoteStreamRequest();

        StreamBlockingQueueWrapper<QuoteStreamResponse> response =
                api.quoteStream(quoteStreamRequest);
        ArgumentCaptor<RequestWrapperDTO<Set<String>, QuoteStreamResponse>> callArgumentCaptor =
                ArgumentCaptor.forClass(RequestWrapperDTO.class);
        Mockito.verify(connectionSpy).innerSend(callArgumentCaptor.capture());
        ArgumentCaptor<String> sendArgumentCaptor = ArgumentCaptor.forClass(String.class);
        RemoteEndpoint remote = sessionMock.getRemote();
        Mockito.verify(remote).sendString(sendArgumentCaptor.capture(), Mockito.any());
        RequestWrapperDTO<Set<String>, QuoteStreamResponse> requestWrapperDTO =
                callArgumentCaptor.getValue();
        Set<String> params = requestWrapperDTO.getParams();
        // TODO: test validations
        String sentPayload = sendArgumentCaptor.getValue();

        URL resource =
                MarketStreamsApiTest.class.getResource(
                        "/expected/stream/MarketStreamsApi/symbol@quote-test.json");

        String expectedJson = Files.readString(Paths.get(resource.toURI()));
        JSONAssert.assertEquals(expectedJson, sentPayload, true);
    }

    /**
     * Tradability Stream
     *
     * <p>Per-symbol push whenever the tradable direction of a symbol changes. Pushed only when the
     * value actually changes (new value ≠ old value).
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void tradabilityStreamTest() throws ApiException, URISyntaxException, IOException {
        TradabilityStreamRequest tradabilityStreamRequest = new TradabilityStreamRequest();

        StreamBlockingQueueWrapper<TradabilityStreamResponse> response =
                api.tradabilityStream(tradabilityStreamRequest);
        ArgumentCaptor<RequestWrapperDTO<Set<String>, TradabilityStreamResponse>>
                callArgumentCaptor = ArgumentCaptor.forClass(RequestWrapperDTO.class);
        Mockito.verify(connectionSpy).innerSend(callArgumentCaptor.capture());
        ArgumentCaptor<String> sendArgumentCaptor = ArgumentCaptor.forClass(String.class);
        RemoteEndpoint remote = sessionMock.getRemote();
        Mockito.verify(remote).sendString(sendArgumentCaptor.capture(), Mockito.any());
        RequestWrapperDTO<Set<String>, TradabilityStreamResponse> requestWrapperDTO =
                callArgumentCaptor.getValue();
        Set<String> params = requestWrapperDTO.getParams();
        // TODO: test validations
        String sentPayload = sendArgumentCaptor.getValue();

        URL resource =
                MarketStreamsApiTest.class.getResource(
                        "/expected/stream/MarketStreamsApi/symbol@tradability-test.json");

        String expectedJson = Files.readString(Paths.get(resource.toURI()));
        JSONAssert.assertEquals(expectedJson, sentPayload, true);
    }

    /**
     * Trading Status Stream
     *
     * <p>Per-symbol trading-status transitions (halts, resumes, SSR, LULD pauses, etc.). Events
     * that do not match a known status/reason rule are not pushed.
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void tradingStatusStreamTest() throws ApiException, URISyntaxException, IOException {
        TradingStatusStreamRequest tradingStatusStreamRequest = new TradingStatusStreamRequest();

        StreamBlockingQueueWrapper<TradingStatusStreamResponse> response =
                api.tradingStatusStream(tradingStatusStreamRequest);
        ArgumentCaptor<RequestWrapperDTO<Set<String>, TradingStatusStreamResponse>>
                callArgumentCaptor = ArgumentCaptor.forClass(RequestWrapperDTO.class);
        Mockito.verify(connectionSpy).innerSend(callArgumentCaptor.capture());
        ArgumentCaptor<String> sendArgumentCaptor = ArgumentCaptor.forClass(String.class);
        RemoteEndpoint remote = sessionMock.getRemote();
        Mockito.verify(remote).sendString(sendArgumentCaptor.capture(), Mockito.any());
        RequestWrapperDTO<Set<String>, TradingStatusStreamResponse> requestWrapperDTO =
                callArgumentCaptor.getValue();
        Set<String> params = requestWrapperDTO.getParams();
        // TODO: test validations
        String sentPayload = sendArgumentCaptor.getValue();

        URL resource =
                MarketStreamsApiTest.class.getResource(
                        "/expected/stream/MarketStreamsApi/symbol@tradingStatus-test.json");

        String expectedJson = Files.readString(Paths.get(resource.toURI()));
        JSONAssert.assertEquals(expectedJson, sentPayload, true);
    }
}
