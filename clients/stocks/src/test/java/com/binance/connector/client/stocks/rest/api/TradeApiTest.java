/*
 * Stocks Trading REST API
 * REST APIs for Binance Stocks Trading. All endpoints under `/sapi/v1/equity/_*`.
 *
 * The version of the OpenAPI document: 1.0.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package com.binance.connector.client.stocks.rest.api;

import static org.junit.Assert.assertEquals;

import com.binance.connector.client.common.ApiClient;
import com.binance.connector.client.common.ApiException;
import com.binance.connector.client.common.ApiResponse;
import com.binance.connector.client.common.auth.BinanceAuthenticationFactory;
import com.binance.connector.client.common.auth.SignatureAuthentication;
import com.binance.connector.client.common.configuration.ClientConfiguration;
import com.binance.connector.client.common.configuration.SignatureConfiguration;
import com.binance.connector.client.common.sign.HmacSignatureGenerator;
import com.binance.connector.client.common.sign.SignatureGenerator;
import com.binance.connector.client.stocks.rest.model.CancelAllEquityOrdersResponse;
import com.binance.connector.client.stocks.rest.model.CancelEquityOrderResponse;
import com.binance.connector.client.stocks.rest.model.CurrentOpenOrdersResponse;
import com.binance.connector.client.stocks.rest.model.EquityOrderDetailResponse;
import com.binance.connector.client.stocks.rest.model.EquityOrderHistoryResponse;
import com.binance.connector.client.stocks.rest.model.EquityTradeHistoryResponse;
import com.binance.connector.client.stocks.rest.model.OrderType;
import com.binance.connector.client.stocks.rest.model.PlaceEquityOrderResponse;
import com.binance.connector.client.stocks.rest.model.Side;
import com.binance.connector.client.stocks.rest.model.TimeInForce;
import com.binance.connector.client.stocks.rest.model.TradingSession;
import com.binance.connector.client.stocks.rest.model.WalletType;
import jakarta.validation.constraints.*;
import java.io.IOException;
import okhttp3.Call;
import okhttp3.Request;
import org.bouncycastle.crypto.CryptoException;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.mockito.ArgumentCaptor;
import org.mockito.Mockito;

/** API tests for TradeApi */
public class TradeApiTest {

    private TradeApi api;
    private ApiClient apiClientSpy;
    private SignatureGenerator signatureGeneratorSpy;

    @BeforeEach
    public void initApiClient() throws ApiException {
        ClientConfiguration clientConfiguration = new ClientConfiguration();
        clientConfiguration.setUrl("http://localhost:8080");
        SignatureConfiguration signatureConfiguration = new SignatureConfiguration();
        signatureConfiguration.setApiKey("apiKey");
        signatureConfiguration.setSecretKey("secretKey");
        clientConfiguration.setSignatureConfiguration(signatureConfiguration);

        HmacSignatureGenerator signatureGenerator =
                new HmacSignatureGenerator(signatureConfiguration.getSecretKey());
        signatureGeneratorSpy = Mockito.spy(signatureGenerator);
        SignatureAuthentication signatureAuthentication =
                new SignatureAuthentication(
                        signatureConfiguration.getApiKey(), signatureGeneratorSpy);
        SignatureAuthentication authenticationSpy = Mockito.spy(signatureAuthentication);
        Mockito.doReturn("1736393892000").when(authenticationSpy).buildTimestamp();

        BinanceAuthenticationFactory factoryMock = Mockito.mock(BinanceAuthenticationFactory.class);
        Mockito.doReturn(authenticationSpy)
                .when(factoryMock)
                .getAuthentication(signatureConfiguration);

        ApiClient apiClient = new ApiClient(clientConfiguration, factoryMock);

        apiClientSpy = Mockito.spy(apiClient);
        Mockito.doReturn(new ApiResponse<>(200, null))
                .when(apiClientSpy)
                .execute(Mockito.any(), Mockito.any(java.lang.reflect.Type.class));
        Mockito.doReturn(new ApiResponse<>(200, null)).when(apiClientSpy).execute(Mockito.any());
        Mockito.doReturn("1736393892000").when(apiClientSpy).buildTimestamp();

        api = new TradeApi(apiClientSpy);
    }

    /**
     * Cancel All Equity Orders (TRADE)
     *
     * <p>Cancel all open orders for the caller. No request body is required — the scope is all of
     * the caller&#39;s open orders. The downstream service handles idempotency; to inspect
     * per-order outcomes, query &#x60;/order/history&#x60; after the call. Weight: 1 Security Type:
     * TRADE
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void cancelAllEquityOrdersTest() throws ApiException, CryptoException, IOException {
        Long recvWindow = 5000L;
        ApiResponse<CancelAllEquityOrdersResponse> response = api.cancelAllEquityOrders(recvWindow);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());

        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals("recvWindow=5000&timestamp=1736393892000", signInputCaptor.getValue());
        assertEquals(
                "2cdd1e484bce80021437bee6b762e6a276b1954c3a0c011a16f6f2f6a47aba75",
                actualRequest.url().queryParameter("signature"));
        assertEquals("/sapi/v1/equity/order/cancel-all", actualRequest.url().encodedPath());
    }

    /**
     * Cancel Equity Order (TRADE)
     *
     * <p>Cancel a single open order by its &#x60;orderId&#x60;. Weight: 1 Security Type: TRADE
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void cancelEquityOrderTest() throws ApiException, CryptoException, IOException {
        String orderId = "c3c58f49-7b0d-4b9e-a2db-1a2f9a3b8c71";
        Long recvWindow = 5000L;
        ApiResponse<CancelEquityOrderResponse> response =
                api.cancelEquityOrder(orderId, recvWindow);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());

        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals("orderId=c3c58f49-7b0d-4b9e-a2db-1a2f9a3b8c71&recvWindow=5000&timestamp=1736393892000", signInputCaptor.getValue());
        assertEquals(
                "a5be8e225fc23e0add7a7213ef24b54201490aa894495b5e9f7c4b70573cbd4a", actualRequest.url().queryParameter("signature"));
        assertEquals("/sapi/v1/equity/order/cancel", actualRequest.url().encodedPath());
    }

    /**
     * Current Open Orders (USER_DATA)
     *
     * <p>Query all unfinished (open) orders for the caller. No business parameters are needed —
     * user identity is derived from the signature. Weight: 1 Security Type: USER_DATA
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void currentOpenOrdersTest() throws ApiException, CryptoException, IOException {
        Long recvWindow = 5000L;
        ApiResponse<CurrentOpenOrdersResponse> response = api.currentOpenOrders(recvWindow);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());

        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals("recvWindow=5000&timestamp=1736393892000", signInputCaptor.getValue());
        assertEquals(
                "2cdd1e484bce80021437bee6b762e6a276b1954c3a0c011a16f6f2f6a47aba75", actualRequest.url().queryParameter("signature"));
        assertEquals("/sapi/v1/equity/order/open-orders", actualRequest.url().encodedPath());
    }

    /**
     * Equity Order Detail (USER_DATA)
     *
     * <p>Returns a single order along with its trade execution list. Callers can only read their
     * own orders — the user identity is derived from the signed request. Weight: 1 Security Type:
     * USER_DATA
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void equityOrderDetailTest() throws ApiException, CryptoException, IOException {
        String orderId = "c3c58f49-7b0d-4b9e-a2db-1a2f9a3b8c71";
        String clientOrderId = "web_2c9c92b74f1e4a7c8f3b9e1a2d3c4b5a";
        Long recvWindow = 5000L;
        ApiResponse<EquityOrderDetailResponse> response =
                api.equityOrderDetail(orderId, clientOrderId, recvWindow);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());

        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals("orderId=c3c58f49-7b0d-4b9e-a2db-1a2f9a3b8c71&clientOrderId=web_2c9c92b74f1e4a7c8f3b9e1a2d3c4b5a&recvWindow=5000&timestamp=1736393892000", signInputCaptor.getValue());
        assertEquals(
                "9591ce3dd1ad80a5cc694944c68048b40581985d1d2688524b32fba4ab0b6d39", actualRequest.url().queryParameter("signature"));
        assertEquals("/sapi/v1/equity/order/detail", actualRequest.url().encodedPath());
    }

    /**
     * Equity Order History (USER_DATA)
     *
     * <p>Paged order history for the caller. Filters by symbol, order type, side, status, and time
     * range. Weight: 1 Security Type: USER_DATA
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void equityOrderHistoryTest() throws ApiException, CryptoException, IOException {
        Long startTime = 1735800000000L;
        Long endTime = 1735900000000L;
        String symbol = "NVDA";
        OrderType orderType = OrderType.MARKET;
        Side side = Side.BUY;
        String orderStatus = "FILLED,CANCELED";
        Integer current = 1;
        Integer size = 20;
        Long recvWindow = 5000L;
        ApiResponse<EquityOrderHistoryResponse> response =
                api.equityOrderHistory(
                        startTime,
                        endTime,
                        symbol,
                        orderType,
                        side,
                        orderStatus,
                        current,
                        size,
                        recvWindow);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());

        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals("symbol=NVDA&orderType=MARKET&side=BUY&orderStatus=FILLED%2CCANCELED&startTime=1735800000000&endTime=1735900000000&current=1&size=20&recvWindow=5000&timestamp=1736393892000", signInputCaptor.getValue());
        assertEquals(
                "4c28ab66a4e77b5074f12e1880da607815c3ee151bd10444b67eefe02b1df64d", actualRequest.url().queryParameter("signature"));
        assertEquals("/sapi/v1/equity/order/history", actualRequest.url().encodedPath());
    }

    /**
     * Equity Trade History (USER_DATA)
     *
     * <p>Paged equity trade (per-fill) history for the caller. Each row is one execution, not one
     * order — a partially filled order produces multiple rows. Filters by symbol, side, specific
     * &#x60;orderId&#x60;, and time range. Weight: 1 Security Type: USER_DATA
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void equityTradeHistoryTest() throws ApiException, CryptoException, IOException {
        Long startTime = 1735800000000L;
        Long endTime = 1735900000000L;
        String symbol = "NVDA";
        Side side = Side.BUY;
        String orderId = "c3c58f49-7b0d-4b9e-a2db-1a2f9a3b8c71";
        Integer current = 1;
        Integer size = 20;
        Long recvWindow = 5000L;
        ApiResponse<EquityTradeHistoryResponse> response =
                api.equityTradeHistory(
                        startTime, endTime, symbol, side, orderId, current, size, recvWindow);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());

        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals("symbol=NVDA&side=BUY&orderId=c3c58f49-7b0d-4b9e-a2db-1a2f9a3b8c71&startTime=1735800000000&endTime=1735900000000&current=1&size=20&recvWindow=5000&timestamp=1736393892000", signInputCaptor.getValue());
        assertEquals(
                "39a69e7a5ab091453233c1823b30bcb63d2a1359f4d2dc18f5c9e26f1f42a576", actualRequest.url().queryParameter("signature"));
        assertEquals("/sapi/v1/equity/trade/history", actualRequest.url().encodedPath());
    }

    /**
     * Place Equity Order (TRADE)
     *
     * <p>Place a new equity order. Supports all combinations of &#x60;LIMIT&#x60; /
     * &#x60;MARKET&#x60; × &#x60;BUY&#x60; / &#x60;SELL&#x60;. For &#x60;LIMIT BUY&#x60; orders the
     * commission fee is automatically computed and reserved by the server at placement time —
     * callers submit &#x60;price&#x60; and &#x60;quantity&#x60; only, no &#x60;fee&#x60; field is
     * required. **Field combination matrix** | Side | OrderType | Required | Forbidden | | ---- |
     * --------- | -------- | --------- | | BUY | LIMIT | &#x60;price&#x60;, &#x60;quantity&#x60;,
     * &#x60;tradingSession&#x60; | &#x60;notional&#x60; | | BUY | MARKET | &#x60;notional&#x60; |
     * &#x60;price&#x60;, &#x60;quantity&#x60;, &#x60;tradingSession&#x60; | | SELL | LIMIT |
     * &#x60;price&#x60;, &#x60;quantity&#x60;, &#x60;tradingSession&#x60; | &#x60;notional&#x60; |
     * | SELL | MARKET | &#x60;quantity&#x60; | &#x60;price&#x60;, &#x60;notional&#x60;,
     * &#x60;tradingSession&#x60; | **Fractional shares**: when &#x60;quantity&#x60; has a decimal
     * component, or an order is placed by &#x60;notional&#x60;, it is treated as a fractional-share
     * order. A fractional-share &#x60;GTC&#x60; order must be paired with &#x60;tradingSession
     * &#x3D; EXTENDED&#x60; or &#x60;24H&#x60;. Rate limit: 200 requests / min (UID). Weight: 1
     * Security Type: TRADE
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void placeEquityOrderTest() throws ApiException, CryptoException, IOException {
        String symbol = "AAPL";
        Side side = Side.BUY;
        OrderType orderType = OrderType.MARKET;
        String quoteAsset = "USDC";
        String price = "180.50";
        String quantity = "1";
        String notional = "1000.00";
        TimeInForce timeInForce = TimeInForce.DAY;
        TradingSession tradingSession = TradingSession.RTH;
        WalletType walletType = WalletType.CARD;
        String clientOrderId = "web_2c9c92b74f1e4a7c8f3b9e1a2d3c4b5a";
        Boolean tokenize = true;
        Long recvWindow = 5000L;
        ApiResponse<PlaceEquityOrderResponse> response =
                api.placeEquityOrder(
                        symbol,
                        side,
                        orderType,
                        quoteAsset,
                        price,
                        quantity,
                        notional,
                        timeInForce,
                        tradingSession,
                        walletType,
                        clientOrderId,
                        tokenize,
                        recvWindow);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());

        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals("symbol=AAPL&quoteAsset=USDC&side=BUY&orderType=MARKET&price=180.50&quantity=1&notional=1000.00&timeInForce=DAY&tradingSession=RTH&walletType=CARD&clientOrderId=web_2c9c92b74f1e4a7c8f3b9e1a2d3c4b5a&tokenize=true&recvWindow=5000&timestamp=1736393892000", signInputCaptor.getValue());
        assertEquals(
                "0e5766dacde7a6822efc3fb17ed74d11ed96b4d15b3a53fb44a641ff0f17a478", actualRequest.url().queryParameter("signature"));
        assertEquals("/sapi/v1/equity/order/place", actualRequest.url().encodedPath());
    }
}
