/*
 * Stocks Trading WebSocket Streams
 * WebSocket stream definitions for Binance Stocks Trading. Base URL: wss://nbstream.binance.com/equity
 *
 * The version of the OpenAPI document: 1.0.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package com.binance.connector.client.stocks.websocket.stream.api;

import com.binance.connector.client.common.ApiException;
import com.binance.connector.client.common.SystemUtil;
import com.binance.connector.client.common.exception.ConstraintViolationException;
import com.binance.connector.client.common.websocket.adapter.stream.StreamConnectionInterface;
import com.binance.connector.client.common.websocket.adapter.stream.StreamConnectionPoolWrapper;
import com.binance.connector.client.common.websocket.adapter.stream.StreamConnectionWrapper;
import com.binance.connector.client.common.websocket.configuration.WebSocketClientConfiguration;
import com.binance.connector.client.common.websocket.dtos.RequestWrapperDTO;
import com.binance.connector.client.common.websocket.service.StreamBlockingQueue;
import com.binance.connector.client.common.websocket.service.StreamBlockingQueueWrapper;
import com.binance.connector.client.stocks.websocket.stream.JSON;
import com.binance.connector.client.stocks.websocket.stream.model.CalendarStreamResponse;
import com.binance.connector.client.stocks.websocket.stream.model.KlineStreamRequest;
import com.binance.connector.client.stocks.websocket.stream.model.KlineStreamResponse;
import com.binance.connector.client.stocks.websocket.stream.model.PriceStreamResponse;
import com.binance.connector.client.stocks.websocket.stream.model.QuoteStreamRequest;
import com.binance.connector.client.stocks.websocket.stream.model.QuoteStreamResponse;
import com.binance.connector.client.stocks.websocket.stream.model.TradabilityStreamRequest;
import com.binance.connector.client.stocks.websocket.stream.model.TradabilityStreamResponse;
import com.binance.connector.client.stocks.websocket.stream.model.TradingStatusStreamRequest;
import com.binance.connector.client.stocks.websocket.stream.model.TradingStatusStreamResponse;
import com.google.gson.reflect.TypeToken;
import jakarta.validation.ConstraintViolation;
import jakarta.validation.Validation;
import jakarta.validation.Validator;
import jakarta.validation.constraints.*;
import java.util.Collections;
import java.util.Map;
import java.util.Set;
import java.util.UUID;
import org.hibernate.validator.messageinterpolation.ParameterMessageInterpolator;

public class MarketStreamsApi {
    private static final String USER_AGENT =
            String.format(
                    "binance-stocks/1.0.0 (Java/%s; %s; %s)",
                    SystemUtil.getJavaVersion(), SystemUtil.getOs(), SystemUtil.getArch());

    private StreamConnectionInterface connection;

    public MarketStreamsApi() {}

    public MarketStreamsApi(WebSocketClientConfiguration configuration) {
        this(
                configuration.getUsePool()
                        ? new StreamConnectionPoolWrapper(configuration, JSON.getGson())
                        : new StreamConnectionWrapper(configuration, JSON.getGson()));
    }

    public MarketStreamsApi(StreamConnectionInterface connection) {
        connection.setUserAgent(USER_AGENT);
        if (!connection.isConnected()) {
            connection.connect();
        }
        this.connection = connection;
    }

    public StreamConnectionInterface getConnection() {
        return connection;
    }

    /**
     * Calendar Stream Single-stream broadcast of market-phase transitions. One message per
     * transition; no periodic heartbeat payload. Server polls every 5 seconds.
     *
     * @return CalendarStreamResponse
     * @throws ApiException If fail to call the API, e.g. server error or cannot deserialize the
     *     response body
     * @http.response.details
     *     <table border="1">
     * <caption>Response Details</caption>
     * <tr><td> Status Code </td><td> Description </td><td> Response Headers </td></tr>
     * <tr><td> 200 </td><td> Calendar Stream </td><td>  -  </td></tr>
     * </table>
     *
     * @see <a
     *     href="https://developers.binance.com/en/docs/catalog/advanced-trading-stocks-trading/api/ws-streams/market-streams#calendar-stream">Calendar
     *     Stream Documentation</a>
     */
    public StreamBlockingQueueWrapper<CalendarStreamResponse> calendarStream() throws ApiException {
        StreamBlockingQueue<String> queue = calendarStreamRaw();

        TypeToken<CalendarStreamResponse> typeToken = new TypeToken<CalendarStreamResponse>() {};

        return new StreamBlockingQueueWrapper<>(queue, typeToken);
    }

    public StreamBlockingQueue<String> calendarStreamRaw() throws ApiException {
        calendarStreamValidateBeforeCall();

        String methodName = "/calendar".substring(1);
        if ("@".equals(methodName.substring(methodName.length() - 1))) {
            methodName = methodName.substring(0, methodName.length() - 1);
        }

        RequestWrapperDTO<Set<String>, Object> requestWrapperDTO =
                new RequestWrapperDTO.Builder<Set<String>, Object>()
                        .id(getRequestID())
                        .method("SUBSCRIBE")
                        .params(Collections.singleton(methodName))
                        .build();
        Map<String, StreamBlockingQueue<String>> queuesMap =
                connection.subscribe(requestWrapperDTO);
        return queuesMap.get(methodName);
    }

    @SuppressWarnings("rawtypes")
    private void calendarStreamValidateBeforeCall() throws ApiException {}

    /**
     * Kline Stream Per-symbol candlestick updates. One stream per (symbol, interval) combination.
     * Supported intervals: 5m, 1h, 1d, 1w, 1M.
     *
     * @param klineStreamRequest (required)
     * @return KlineStreamResponse
     * @throws ApiException If fail to call the API, e.g. server error or cannot deserialize the
     *     response body
     * @http.response.details
     *     <table border="1">
     * <caption>Response Details</caption>
     * <tr><td> Status Code </td><td> Description </td><td> Response Headers </td></tr>
     * <tr><td> 200 </td><td> Kline Stream </td><td>  -  </td></tr>
     * </table>
     *
     * @see <a
     *     href="https://developers.binance.com/en/docs/catalog/advanced-trading-stocks-trading/api/ws-streams/market-streams#kline-stream">Kline
     *     Stream Documentation</a>
     */
    public StreamBlockingQueueWrapper<KlineStreamResponse> klineStream(
            KlineStreamRequest klineStreamRequest) throws ApiException {
        StreamBlockingQueue<String> queue = klineStreamRaw(klineStreamRequest);

        TypeToken<KlineStreamResponse> typeToken = new TypeToken<KlineStreamResponse>() {};

        return new StreamBlockingQueueWrapper<>(queue, typeToken);
    }

    public StreamBlockingQueue<String> klineStreamRaw(KlineStreamRequest klineStreamRequest)
            throws ApiException {
        klineStreamValidateBeforeCall(klineStreamRequest);

        String methodName =
                "/<symbol>@kline_<interval>"
                        .substring(1)
                        .replace(
                                "<symbol>",
                                klineStreamRequest.getSymbol() != null
                                        ? klineStreamRequest.getSymbol().toString()
                                        : "")
                        .replace(
                                "<interval>",
                                klineStreamRequest.getInterval() != null
                                        ? klineStreamRequest.getInterval().toString()
                                        : "");
        if ("@".equals(methodName.substring(methodName.length() - 1))) {
            methodName = methodName.substring(0, methodName.length() - 1);
        }

        RequestWrapperDTO<Set<String>, Object> requestWrapperDTO =
                new RequestWrapperDTO.Builder<Set<String>, Object>()
                        .id(getRequestID())
                        .method("SUBSCRIBE")
                        .params(Collections.singleton(methodName))
                        .build();
        Map<String, StreamBlockingQueue<String>> queuesMap =
                connection.subscribe(requestWrapperDTO);
        return queuesMap.get(methodName);
    }

    @SuppressWarnings("rawtypes")
    private void klineStreamValidateBeforeCall(KlineStreamRequest klineStreamRequest)
            throws ApiException {
        try {
            Validator validator =
                    Validation.byDefaultProvider()
                            .configure()
                            .messageInterpolator(new ParameterMessageInterpolator())
                            .buildValidatorFactory()
                            .getValidator();

            Set<ConstraintViolation<KlineStreamRequest>> violations =
                    validator.validate(klineStreamRequest);

            if (!violations.isEmpty()) {
                throw new ConstraintViolationException(violations);
            }
        } catch (SecurityException e) {
            e.printStackTrace();
            throw new ApiException(e.getMessage());
        }
    }

    /**
     * Price Stream Push-all price snapshot, polled every 3 seconds. One message carries the latest
     * price for every active US-equity symbol.
     *
     * @return PriceStreamResponse
     * @throws ApiException If fail to call the API, e.g. server error or cannot deserialize the
     *     response body
     * @http.response.details
     *     <table border="1">
     * <caption>Response Details</caption>
     * <tr><td> Status Code </td><td> Description </td><td> Response Headers </td></tr>
     * <tr><td> 200 </td><td> Price Stream </td><td>  -  </td></tr>
     * </table>
     *
     * @see <a
     *     href="https://developers.binance.com/en/docs/catalog/advanced-trading-stocks-trading/api/ws-streams/market-streams#price-stream">Price
     *     Stream Documentation</a>
     */
    public StreamBlockingQueueWrapper<PriceStreamResponse> priceStream() throws ApiException {
        StreamBlockingQueue<String> queue = priceStreamRaw();

        TypeToken<PriceStreamResponse> typeToken = new TypeToken<PriceStreamResponse>() {};

        return new StreamBlockingQueueWrapper<>(queue, typeToken);
    }

    public StreamBlockingQueue<String> priceStreamRaw() throws ApiException {
        priceStreamValidateBeforeCall();

        String methodName = "/price".substring(1);
        if ("@".equals(methodName.substring(methodName.length() - 1))) {
            methodName = methodName.substring(0, methodName.length() - 1);
        }

        RequestWrapperDTO<Set<String>, Object> requestWrapperDTO =
                new RequestWrapperDTO.Builder<Set<String>, Object>()
                        .id(getRequestID())
                        .method("SUBSCRIBE")
                        .params(Collections.singleton(methodName))
                        .build();
        Map<String, StreamBlockingQueue<String>> queuesMap =
                connection.subscribe(requestWrapperDTO);
        return queuesMap.get(methodName);
    }

    @SuppressWarnings("rawtypes")
    private void priceStreamValidateBeforeCall() throws ApiException {}

    /**
     * Quote Stream Per-symbol real-time best-bid / best-ask. Each symbol has its own stream.
     * Per-symbol throttle: at most one push per symbol every 200 ms.
     *
     * @param quoteStreamRequest (required)
     * @return QuoteStreamResponse
     * @throws ApiException If fail to call the API, e.g. server error or cannot deserialize the
     *     response body
     * @http.response.details
     *     <table border="1">
     * <caption>Response Details</caption>
     * <tr><td> Status Code </td><td> Description </td><td> Response Headers </td></tr>
     * <tr><td> 200 </td><td> Quote Stream </td><td>  -  </td></tr>
     * </table>
     *
     * @see <a
     *     href="https://developers.binance.com/en/docs/catalog/advanced-trading-stocks-trading/api/ws-streams/market-streams#quote-stream">Quote
     *     Stream Documentation</a>
     */
    public StreamBlockingQueueWrapper<QuoteStreamResponse> quoteStream(
            QuoteStreamRequest quoteStreamRequest) throws ApiException {
        StreamBlockingQueue<String> queue = quoteStreamRaw(quoteStreamRequest);

        TypeToken<QuoteStreamResponse> typeToken = new TypeToken<QuoteStreamResponse>() {};

        return new StreamBlockingQueueWrapper<>(queue, typeToken);
    }

    public StreamBlockingQueue<String> quoteStreamRaw(QuoteStreamRequest quoteStreamRequest)
            throws ApiException {
        quoteStreamValidateBeforeCall(quoteStreamRequest);

        String methodName =
                "/<symbol>@quote"
                        .substring(1)
                        .replace(
                                "<symbol>",
                                quoteStreamRequest.getSymbol() != null
                                        ? quoteStreamRequest.getSymbol().toString()
                                        : "");
        if ("@".equals(methodName.substring(methodName.length() - 1))) {
            methodName = methodName.substring(0, methodName.length() - 1);
        }

        RequestWrapperDTO<Set<String>, Object> requestWrapperDTO =
                new RequestWrapperDTO.Builder<Set<String>, Object>()
                        .id(getRequestID())
                        .method("SUBSCRIBE")
                        .params(Collections.singleton(methodName))
                        .build();
        Map<String, StreamBlockingQueue<String>> queuesMap =
                connection.subscribe(requestWrapperDTO);
        return queuesMap.get(methodName);
    }

    @SuppressWarnings("rawtypes")
    private void quoteStreamValidateBeforeCall(QuoteStreamRequest quoteStreamRequest)
            throws ApiException {
        try {
            Validator validator =
                    Validation.byDefaultProvider()
                            .configure()
                            .messageInterpolator(new ParameterMessageInterpolator())
                            .buildValidatorFactory()
                            .getValidator();

            Set<ConstraintViolation<QuoteStreamRequest>> violations =
                    validator.validate(quoteStreamRequest);

            if (!violations.isEmpty()) {
                throw new ConstraintViolationException(violations);
            }
        } catch (SecurityException e) {
            e.printStackTrace();
            throw new ApiException(e.getMessage());
        }
    }

    /**
     * Tradability Stream Per-symbol push whenever the tradable direction of a symbol changes.
     * Pushed only when the value actually changes (new value ≠ old value).
     *
     * @param tradabilityStreamRequest (required)
     * @return TradabilityStreamResponse
     * @throws ApiException If fail to call the API, e.g. server error or cannot deserialize the
     *     response body
     * @http.response.details
     *     <table border="1">
     * <caption>Response Details</caption>
     * <tr><td> Status Code </td><td> Description </td><td> Response Headers </td></tr>
     * <tr><td> 200 </td><td> Tradability Stream </td><td>  -  </td></tr>
     * </table>
     *
     * @see <a
     *     href="https://developers.binance.com/en/docs/catalog/advanced-trading-stocks-trading/api/ws-streams/market-streams#tradability-stream">Tradability
     *     Stream Documentation</a>
     */
    public StreamBlockingQueueWrapper<TradabilityStreamResponse> tradabilityStream(
            TradabilityStreamRequest tradabilityStreamRequest) throws ApiException {
        StreamBlockingQueue<String> queue = tradabilityStreamRaw(tradabilityStreamRequest);

        TypeToken<TradabilityStreamResponse> typeToken =
                new TypeToken<TradabilityStreamResponse>() {};

        return new StreamBlockingQueueWrapper<>(queue, typeToken);
    }

    public StreamBlockingQueue<String> tradabilityStreamRaw(
            TradabilityStreamRequest tradabilityStreamRequest) throws ApiException {
        tradabilityStreamValidateBeforeCall(tradabilityStreamRequest);

        String methodName =
                "/<symbol>@tradability"
                        .substring(1)
                        .replace(
                                "<symbol>",
                                tradabilityStreamRequest.getSymbol() != null
                                        ? tradabilityStreamRequest.getSymbol().toString()
                                        : "");
        if ("@".equals(methodName.substring(methodName.length() - 1))) {
            methodName = methodName.substring(0, methodName.length() - 1);
        }

        RequestWrapperDTO<Set<String>, Object> requestWrapperDTO =
                new RequestWrapperDTO.Builder<Set<String>, Object>()
                        .id(getRequestID())
                        .method("SUBSCRIBE")
                        .params(Collections.singleton(methodName))
                        .build();
        Map<String, StreamBlockingQueue<String>> queuesMap =
                connection.subscribe(requestWrapperDTO);
        return queuesMap.get(methodName);
    }

    @SuppressWarnings("rawtypes")
    private void tradabilityStreamValidateBeforeCall(
            TradabilityStreamRequest tradabilityStreamRequest) throws ApiException {
        try {
            Validator validator =
                    Validation.byDefaultProvider()
                            .configure()
                            .messageInterpolator(new ParameterMessageInterpolator())
                            .buildValidatorFactory()
                            .getValidator();

            Set<ConstraintViolation<TradabilityStreamRequest>> violations =
                    validator.validate(tradabilityStreamRequest);

            if (!violations.isEmpty()) {
                throw new ConstraintViolationException(violations);
            }
        } catch (SecurityException e) {
            e.printStackTrace();
            throw new ApiException(e.getMessage());
        }
    }

    /**
     * Trading Status Stream Per-symbol trading-status transitions (halts, resumes, SSR, LULD
     * pauses, etc.). Events that do not match a known status/reason rule are not pushed.
     *
     * @param tradingStatusStreamRequest (required)
     * @return TradingStatusStreamResponse
     * @throws ApiException If fail to call the API, e.g. server error or cannot deserialize the
     *     response body
     * @http.response.details
     *     <table border="1">
     * <caption>Response Details</caption>
     * <tr><td> Status Code </td><td> Description </td><td> Response Headers </td></tr>
     * <tr><td> 200 </td><td> Trading Status Stream </td><td>  -  </td></tr>
     * </table>
     *
     * @see <a
     *     href="https://developers.binance.com/en/docs/catalog/advanced-trading-stocks-trading/api/ws-streams/market-streams#trading-status-stream">Trading
     *     Status Stream Documentation</a>
     */
    public StreamBlockingQueueWrapper<TradingStatusStreamResponse> tradingStatusStream(
            TradingStatusStreamRequest tradingStatusStreamRequest) throws ApiException {
        StreamBlockingQueue<String> queue = tradingStatusStreamRaw(tradingStatusStreamRequest);

        TypeToken<TradingStatusStreamResponse> typeToken =
                new TypeToken<TradingStatusStreamResponse>() {};

        return new StreamBlockingQueueWrapper<>(queue, typeToken);
    }

    public StreamBlockingQueue<String> tradingStatusStreamRaw(
            TradingStatusStreamRequest tradingStatusStreamRequest) throws ApiException {
        tradingStatusStreamValidateBeforeCall(tradingStatusStreamRequest);

        String methodName =
                "/<symbol>@tradingStatus"
                        .substring(1)
                        .replace(
                                "<symbol>",
                                tradingStatusStreamRequest.getSymbol() != null
                                        ? tradingStatusStreamRequest.getSymbol().toString()
                                        : "");
        if ("@".equals(methodName.substring(methodName.length() - 1))) {
            methodName = methodName.substring(0, methodName.length() - 1);
        }

        RequestWrapperDTO<Set<String>, Object> requestWrapperDTO =
                new RequestWrapperDTO.Builder<Set<String>, Object>()
                        .id(getRequestID())
                        .method("SUBSCRIBE")
                        .params(Collections.singleton(methodName))
                        .build();
        Map<String, StreamBlockingQueue<String>> queuesMap =
                connection.subscribe(requestWrapperDTO);
        return queuesMap.get(methodName);
    }

    @SuppressWarnings("rawtypes")
    private void tradingStatusStreamValidateBeforeCall(
            TradingStatusStreamRequest tradingStatusStreamRequest) throws ApiException {
        try {
            Validator validator =
                    Validation.byDefaultProvider()
                            .configure()
                            .messageInterpolator(new ParameterMessageInterpolator())
                            .buildValidatorFactory()
                            .getValidator();

            Set<ConstraintViolation<TradingStatusStreamRequest>> violations =
                    validator.validate(tradingStatusStreamRequest);

            if (!violations.isEmpty()) {
                throw new ConstraintViolationException(violations);
            }
        } catch (SecurityException e) {
            e.printStackTrace();
            throw new ApiException(e.getMessage());
        }
    }

    public String getRequestID() {
        return UUID.randomUUID().toString();
    }
}
