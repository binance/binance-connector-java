/*
 * Stocks Trading WebSocket Streams
 * WebSocket stream definitions for Binance Stocks Trading. Base URL: wss://nbstream.binance.com/equity
 *
 * The version of the OpenAPI document: 1.0.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package com.binance.connector.client.stocks.websocket.stream.model;

import com.binance.connector.client.common.websocket.dtos.BaseDTO;
import com.binance.connector.client.stocks.websocket.stream.JSON;
import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import jakarta.validation.constraints.*;
import java.io.IOException;
import java.nio.charset.StandardCharsets;
import java.util.HashSet;
import java.util.Map;
import java.util.Objects;
import java.util.Set;
import java.util.TreeMap;
import java.util.stream.Collectors;
import org.hibernate.validator.constraints.*;

/** CalendarStreamResponse */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class CalendarStreamResponse extends BaseDTO {
    public static final String SERIALIZED_NAME_E_LOWER_CASE = "e";

    @SerializedName(SERIALIZED_NAME_E_LOWER_CASE)
    @jakarta.annotation.Nullable
    private String ELowerCase;

    public static final String SERIALIZED_NAME_FROM = "from";

    @SerializedName(SERIALIZED_NAME_FROM)
    @jakarta.annotation.Nullable
    private String from;

    public static final String SERIALIZED_NAME_TO = "to";

    @SerializedName(SERIALIZED_NAME_TO)
    @jakarta.annotation.Nullable
    private String to;

    public static final String SERIALIZED_NAME_TS = "ts";

    @SerializedName(SERIALIZED_NAME_TS)
    @jakarta.annotation.Nullable
    private Long ts;

    public CalendarStreamResponse() {}

    public CalendarStreamResponse ELowerCase(@jakarta.annotation.Nullable String ELowerCase) {
        this.ELowerCase = ELowerCase;
        return this;
    }

    /**
     * Event type, always &#x60;\&quot;calendar\&quot;&#x60;.
     *
     * @return ELowerCase
     */
    @jakarta.annotation.Nullable
    public String getELowerCase() {
        return ELowerCase;
    }

    public void setELowerCase(@jakarta.annotation.Nullable String ELowerCase) {
        this.ELowerCase = ELowerCase;
    }

    public CalendarStreamResponse from(@jakarta.annotation.Nullable String from) {
        this.from = from;
        return this;
    }

    /**
     * Previous market phase.
     *
     * @return from
     */
    @jakarta.annotation.Nullable
    public String getFrom() {
        return from;
    }

    public void setFrom(@jakarta.annotation.Nullable String from) {
        this.from = from;
    }

    public CalendarStreamResponse to(@jakarta.annotation.Nullable String to) {
        this.to = to;
        return this;
    }

    /**
     * New market phase.
     *
     * @return to
     */
    @jakarta.annotation.Nullable
    public String getTo() {
        return to;
    }

    public void setTo(@jakarta.annotation.Nullable String to) {
        this.to = to;
    }

    public CalendarStreamResponse ts(@jakarta.annotation.Nullable Long ts) {
        this.ts = ts;
        return this;
    }

    /**
     * Event timestamp (epoch milliseconds UTC) when the transition was detected.
     *
     * @return ts
     */
    @jakarta.annotation.Nullable
    public Long getTs() {
        return ts;
    }

    public void setTs(@jakarta.annotation.Nullable Long ts) {
        this.ts = ts;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        CalendarStreamResponse calendarStreamResponse = (CalendarStreamResponse) o;
        return Objects.equals(this.ELowerCase, calendarStreamResponse.ELowerCase)
                && Objects.equals(this.from, calendarStreamResponse.from)
                && Objects.equals(this.to, calendarStreamResponse.to)
                && Objects.equals(this.ts, calendarStreamResponse.ts);
    }

    @Override
    public int hashCode() {
        return Objects.hash(ELowerCase, from, to, ts);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class CalendarStreamResponse {\n");
        sb.append("		ELowerCase: ").append(toIndentedString(ELowerCase)).append("\n");
        sb.append("		from: ").append(toIndentedString(from)).append("\n");
        sb.append("		to: ").append(toIndentedString(to)).append("\n");
        sb.append("		ts: ").append(toIndentedString(ts)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    public String toUrlQueryString() {
        StringBuilder sb = new StringBuilder();
        Map<String, String> valMap = new TreeMap<String, String>();
        valMap.put("apiKey", getApiKey());
        String ELowerCaseValue = getELowerCase();
        if (ELowerCaseValue != null) {
            String ELowerCaseValueAsString = ELowerCaseValue.toString();
            valMap.put("ELowerCase", ELowerCaseValueAsString);
        }
        String fromValue = getFrom();
        if (fromValue != null) {
            String fromValueAsString = fromValue.toString();
            valMap.put("from", fromValueAsString);
        }
        String toValue = getTo();
        if (toValue != null) {
            String toValueAsString = toValue.toString();
            valMap.put("to", toValueAsString);
        }
        Long tsValue = getTs();
        if (tsValue != null) {
            String tsValueAsString = tsValue.toString();
            valMap.put("ts", tsValueAsString);
        }

        valMap.put("timestamp", getTimestamp());
        return asciiEncode(
                valMap.keySet().stream()
                        .map(key -> key + "=" + valMap.get(key))
                        .collect(Collectors.joining("&")));
    }

    public Map<String, Object> toMap() {
        Map<String, Object> valMap = new TreeMap<String, Object>();
        valMap.put("apiKey", getApiKey());
        Object ELowerCaseValue = getELowerCase();
        if (ELowerCaseValue != null) {
            valMap.put("ELowerCase", ELowerCaseValue);
        }
        Object fromValue = getFrom();
        if (fromValue != null) {
            valMap.put("from", fromValue);
        }
        Object toValue = getTo();
        if (toValue != null) {
            valMap.put("to", toValue);
        }
        Object tsValue = getTs();
        if (tsValue != null) {
            valMap.put("ts", tsValue);
        }

        valMap.put("timestamp", getTimestamp());
        return valMap;
    }

    public static String asciiEncode(String s) {
        return new String(s.getBytes(), StandardCharsets.US_ASCII);
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces (except the first
     * line).
     */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n		");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("e");
        openapiFields.add("from");
        openapiFields.add("to");
        openapiFields.add("ts");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to CalendarStreamResponse
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!CalendarStreamResponse.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                "The required field(s) %s in CalendarStreamResponse is not found in"
                                        + " the empty JSON string",
                                CalendarStreamResponse.openapiRequiredFields.toString()));
            }
        }

        Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
        // check to see if the JSON string contains additional fields
        for (Map.Entry<String, JsonElement> entry : entries) {
            if (!CalendarStreamResponse.openapiFields.contains(entry.getKey())) {
                throw new IllegalArgumentException(
                        String.format(
                                "The field `%s` in the JSON string is not defined in the"
                                        + " `CalendarStreamResponse` properties. JSON: %s",
                                entry.getKey(), jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if ((jsonObj.get("e") != null && !jsonObj.get("e").isJsonNull())
                && !jsonObj.get("e").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            "Expected the field `e` to be a primitive type in the JSON string but"
                                    + " got `%s`",
                            jsonObj.get("e").toString()));
        }
        if ((jsonObj.get("from") != null && !jsonObj.get("from").isJsonNull())
                && !jsonObj.get("from").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            "Expected the field `from` to be a primitive type in the JSON string"
                                    + " but got `%s`",
                            jsonObj.get("from").toString()));
        }
        if ((jsonObj.get("to") != null && !jsonObj.get("to").isJsonNull())
                && !jsonObj.get("to").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            "Expected the field `to` to be a primitive type in the JSON string but"
                                    + " got `%s`",
                            jsonObj.get("to").toString()));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!CalendarStreamResponse.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'CalendarStreamResponse' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<CalendarStreamResponse> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(CalendarStreamResponse.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<CalendarStreamResponse>() {
                        @Override
                        public void write(JsonWriter out, CalendarStreamResponse value)
                                throws IOException {
                            JsonElement obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public CalendarStreamResponse read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            // validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of CalendarStreamResponse given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of CalendarStreamResponse
     * @throws IOException if the JSON string is invalid with respect to CalendarStreamResponse
     */
    public static CalendarStreamResponse fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, CalendarStreamResponse.class);
    }

    /**
     * Convert an instance of CalendarStreamResponse to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
