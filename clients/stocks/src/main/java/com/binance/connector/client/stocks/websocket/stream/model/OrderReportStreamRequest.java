/*
 * Stocks Trading WebSocket Streams
 * WebSocket stream definitions for Binance Stocks Trading. Base URL: wss://nbstream.binance.com/equity
 *
 * The version of the OpenAPI document: 1.0.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package com.binance.connector.client.stocks.websocket.stream.model;

import com.binance.connector.client.common.websocket.dtos.BaseDTO;
import com.binance.connector.client.stocks.websocket.stream.JSON;
import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import jakarta.validation.constraints.*;
import java.io.IOException;
import java.nio.charset.StandardCharsets;
import java.util.HashSet;
import java.util.Map;
import java.util.Objects;
import java.util.Set;
import java.util.TreeMap;
import java.util.stream.Collectors;
import org.hibernate.validator.constraints.*;

/** OrderReportStreamRequest */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class OrderReportStreamRequest extends BaseDTO {
    public static final String SERIALIZED_NAME_LISTEN_KEY = "listenKey";

    @SerializedName(SERIALIZED_NAME_LISTEN_KEY)
    @jakarta.annotation.Nullable
    private String listenKey;

    public OrderReportStreamRequest() {}

    public OrderReportStreamRequest listenKey(@jakarta.annotation.Nullable String listenKey) {
        this.listenKey = listenKey;
        return this;
    }

    /**
     * User data listen key obtained from the Listen Key endpoint.
     *
     * @return listenKey
     */
    @jakarta.annotation.Nullable
    public String getListenKey() {
        return listenKey;
    }

    public void setListenKey(@jakarta.annotation.Nullable String listenKey) {
        this.listenKey = listenKey;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        OrderReportStreamRequest orderReportStreamRequest = (OrderReportStreamRequest) o;
        return Objects.equals(this.listenKey, orderReportStreamRequest.listenKey);
    }

    @Override
    public int hashCode() {
        return Objects.hash(listenKey);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class OrderReportStreamRequest {\n");
        sb.append("		listenKey: ").append(toIndentedString(listenKey)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    public String toUrlQueryString() {
        StringBuilder sb = new StringBuilder();
        Map<String, String> valMap = new TreeMap<String, String>();
        valMap.put("apiKey", getApiKey());
        String listenKeyValue = getListenKey();
        if (listenKeyValue != null) {
            String listenKeyValueAsString = listenKeyValue.toString();
            valMap.put("listenKey", listenKeyValueAsString);
        }

        valMap.put("timestamp", getTimestamp());
        return asciiEncode(
                valMap.keySet().stream()
                        .map(key -> key + "=" + valMap.get(key))
                        .collect(Collectors.joining("&")));
    }

    public Map<String, Object> toMap() {
        Map<String, Object> valMap = new TreeMap<String, Object>();
        valMap.put("apiKey", getApiKey());
        Object listenKeyValue = getListenKey();
        if (listenKeyValue != null) {
            valMap.put("listenKey", listenKeyValue);
        }

        valMap.put("timestamp", getTimestamp());
        return valMap;
    }

    public static String asciiEncode(String s) {
        return new String(s.getBytes(), StandardCharsets.US_ASCII);
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces (except the first
     * line).
     */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n		");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("listenKey");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to OrderReportStreamRequest
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!OrderReportStreamRequest.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                "The required field(s) %s in OrderReportStreamRequest is not found"
                                        + " in the empty JSON string",
                                OrderReportStreamRequest.openapiRequiredFields.toString()));
            }
        }

        Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
        // check to see if the JSON string contains additional fields
        for (Map.Entry<String, JsonElement> entry : entries) {
            if (!OrderReportStreamRequest.openapiFields.contains(entry.getKey())) {
                throw new IllegalArgumentException(
                        String.format(
                                "The field `%s` in the JSON string is not defined in the"
                                        + " `OrderReportStreamRequest` properties. JSON: %s",
                                entry.getKey(), jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if ((jsonObj.get("listenKey") != null && !jsonObj.get("listenKey").isJsonNull())
                && !jsonObj.get("listenKey").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            "Expected the field `listenKey` to be a primitive type in the JSON"
                                    + " string but got `%s`",
                            jsonObj.get("listenKey").toString()));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!OrderReportStreamRequest.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'OrderReportStreamRequest' and its
                // subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<OrderReportStreamRequest> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(OrderReportStreamRequest.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<OrderReportStreamRequest>() {
                        @Override
                        public void write(JsonWriter out, OrderReportStreamRequest value)
                                throws IOException {
                            JsonElement obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public OrderReportStreamRequest read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            // validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of OrderReportStreamRequest given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of OrderReportStreamRequest
     * @throws IOException if the JSON string is invalid with respect to OrderReportStreamRequest
     */
    public static OrderReportStreamRequest fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, OrderReportStreamRequest.class);
    }

    /**
     * Convert an instance of OrderReportStreamRequest to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
