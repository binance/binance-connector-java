/*
 * Stocks Trading REST API
 * REST APIs for Binance Stocks Trading. All endpoints under `/sapi/v1/equity/_*`.
 *
 * The version of the OpenAPI document: 1.0.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package com.binance.connector.client.stocks.rest.model;

import com.binance.connector.client.stocks.rest.JSON;
import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import jakarta.validation.constraints.*;
import java.io.IOException;
import java.io.UnsupportedEncodingException;
import java.net.URLEncoder;
import java.nio.charset.StandardCharsets;
import java.util.HashSet;
import java.util.Objects;
import org.hibernate.validator.constraints.*;

/** TokenizedMintResponse */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class TokenizedMintResponse {
    public static final String SERIALIZED_NAME_ISSUER_REQUEST_ID = "issuerRequestId";

    @SerializedName(SERIALIZED_NAME_ISSUER_REQUEST_ID)
    @jakarta.annotation.Nullable
    private String issuerRequestId;

    public static final String SERIALIZED_NAME_STATUS = "status";

    @SerializedName(SERIALIZED_NAME_STATUS)
    @jakarta.annotation.Nullable
    private String status;

    public TokenizedMintResponse() {}

    public TokenizedMintResponse issuerRequestId(
            @jakarta.annotation.Nullable String issuerRequestId) {
        this.issuerRequestId = issuerRequestId;
        return this;
    }

    /**
     * Server-assigned convert request id. Use it to poll &#x60;/tokenized/convert-status&#x60;.
     *
     * @return issuerRequestId
     */
    @jakarta.annotation.Nullable
    public String getIssuerRequestId() {
        return issuerRequestId;
    }

    public void setIssuerRequestId(@jakarta.annotation.Nullable String issuerRequestId) {
        this.issuerRequestId = issuerRequestId;
    }

    public TokenizedMintResponse status(@jakarta.annotation.Nullable String status) {
        this.status = status;
        return this;
    }

    /**
     * Convert status code: &#x60;P&#x60; &#x3D; processing, &#x60;S&#x60; &#x3D; success,
     * &#x60;F&#x60; &#x3D; failed.
     *
     * @return status
     */
    @jakarta.annotation.Nullable
    public String getStatus() {
        return status;
    }

    public void setStatus(@jakarta.annotation.Nullable String status) {
        this.status = status;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        TokenizedMintResponse tokenizedMintResponse = (TokenizedMintResponse) o;
        return Objects.equals(this.issuerRequestId, tokenizedMintResponse.issuerRequestId)
                && Objects.equals(this.status, tokenizedMintResponse.status);
    }

    @Override
    public int hashCode() {
        return Objects.hash(issuerRequestId, status);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class TokenizedMintResponse {\n");
        sb.append("		issuerRequestId: ").append(toIndentedString(issuerRequestId)).append("\n");
        sb.append("		status: ").append(toIndentedString(status)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    public String toUrlQueryString() {
        StringBuilder sb = new StringBuilder();

        Object issuerRequestIdValue = getIssuerRequestId();
        String issuerRequestIdValueAsString = "";
        issuerRequestIdValueAsString = issuerRequestIdValue.toString();
        sb.append("issuerRequestId=").append(urlEncode(issuerRequestIdValueAsString)).append("");
        Object statusValue = getStatus();
        String statusValueAsString = "";
        statusValueAsString = statusValue.toString();
        sb.append("status=").append(urlEncode(statusValueAsString)).append("");
        return sb.toString();
    }

    public static String urlEncode(String s) {
        try {
            return URLEncoder.encode(s, StandardCharsets.UTF_8.name());
        } catch (UnsupportedEncodingException e) {
            throw new RuntimeException(StandardCharsets.UTF_8.name() + " is unsupported", e);
        }
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces (except the first
     * line).
     */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n		");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("issuerRequestId");
        openapiFields.add("status");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to TokenizedMintResponse
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!TokenizedMintResponse.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                "The required field(s) %s in TokenizedMintResponse is not found in"
                                        + " the empty JSON string",
                                TokenizedMintResponse.openapiRequiredFields.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if ((jsonObj.get("issuerRequestId") != null && !jsonObj.get("issuerRequestId").isJsonNull())
                && !jsonObj.get("issuerRequestId").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            "Expected the field `issuerRequestId` to be a primitive type in the"
                                    + " JSON string but got `%s`",
                            jsonObj.get("issuerRequestId").toString()));
        }
        if ((jsonObj.get("status") != null && !jsonObj.get("status").isJsonNull())
                && !jsonObj.get("status").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            "Expected the field `status` to be a primitive type in the JSON string"
                                    + " but got `%s`",
                            jsonObj.get("status").toString()));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!TokenizedMintResponse.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'TokenizedMintResponse' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<TokenizedMintResponse> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(TokenizedMintResponse.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<TokenizedMintResponse>() {
                        @Override
                        public void write(JsonWriter out, TokenizedMintResponse value)
                                throws IOException {
                            JsonElement obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public TokenizedMintResponse read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            // validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of TokenizedMintResponse given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of TokenizedMintResponse
     * @throws IOException if the JSON string is invalid with respect to TokenizedMintResponse
     */
    public static TokenizedMintResponse fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, TokenizedMintResponse.class);
    }

    /**
     * Convert an instance of TokenizedMintResponse to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
