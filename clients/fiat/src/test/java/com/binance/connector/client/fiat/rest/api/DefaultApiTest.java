/*
 * Fiat REST API
 * Query Binance fiat deposit and withdrawal history.
 *
 * The version of the OpenAPI document: 1.0.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package com.binance.connector.client.fiat.rest.api;

import static org.junit.Assert.assertEquals;

import com.binance.connector.client.common.ApiClient;
import com.binance.connector.client.common.ApiException;
import com.binance.connector.client.common.ApiResponse;
import com.binance.connector.client.common.auth.BinanceAuthenticationFactory;
import com.binance.connector.client.common.auth.SignatureAuthentication;
import com.binance.connector.client.common.configuration.ClientConfiguration;
import com.binance.connector.client.common.configuration.SignatureConfiguration;
import com.binance.connector.client.common.sign.HmacSignatureGenerator;
import com.binance.connector.client.common.sign.SignatureGenerator;
import com.binance.connector.client.fiat.rest.model.ApiPaymentMethod;
import com.binance.connector.client.fiat.rest.model.DepositRequest;
import com.binance.connector.client.fiat.rest.model.DepositResponse;
import com.binance.connector.client.fiat.rest.model.FiatWithdrawRequest;
import com.binance.connector.client.fiat.rest.model.FiatWithdrawRequestAccountInfo;
import com.binance.connector.client.fiat.rest.model.FiatWithdrawResponse;
import com.binance.connector.client.fiat.rest.model.GetFiatDepositWithdrawHistoryResponse;
import com.binance.connector.client.fiat.rest.model.GetFiatPaymentsHistoryResponse;
import com.binance.connector.client.fiat.rest.model.GetOrderDetailResponse;
import jakarta.validation.constraints.*;
import java.io.IOException;
import okhttp3.Call;
import okhttp3.Request;
import org.bouncycastle.crypto.CryptoException;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.mockito.ArgumentCaptor;
import org.mockito.Mockito;

/** API tests for DefaultApi */
public class DefaultApiTest {

    private DefaultApi api;
    private ApiClient apiClientSpy;
    private SignatureGenerator signatureGeneratorSpy;

    @BeforeEach
    public void initApiClient() throws ApiException {
        ClientConfiguration clientConfiguration = new ClientConfiguration();
        clientConfiguration.setUrl("http://localhost:8080");
        SignatureConfiguration signatureConfiguration = new SignatureConfiguration();
        signatureConfiguration.setApiKey("apiKey");
        signatureConfiguration.setSecretKey("secretKey");
        clientConfiguration.setSignatureConfiguration(signatureConfiguration);

        HmacSignatureGenerator signatureGenerator =
                new HmacSignatureGenerator(signatureConfiguration.getSecretKey());
        signatureGeneratorSpy = Mockito.spy(signatureGenerator);
        SignatureAuthentication signatureAuthentication =
                new SignatureAuthentication(
                        signatureConfiguration.getApiKey(), signatureGeneratorSpy);
        SignatureAuthentication authenticationSpy = Mockito.spy(signatureAuthentication);
        Mockito.doReturn("1736393892000").when(authenticationSpy).buildTimestamp();

        BinanceAuthenticationFactory factoryMock = Mockito.mock(BinanceAuthenticationFactory.class);
        Mockito.doReturn(authenticationSpy)
                .when(factoryMock)
                .getAuthentication(signatureConfiguration);

        ApiClient apiClient = new ApiClient(clientConfiguration, factoryMock);

        apiClientSpy = Mockito.spy(apiClient);
        Mockito.doReturn(new ApiResponse<>(200, null))
                .when(apiClientSpy)
                .execute(Mockito.any(), Mockito.any(java.lang.reflect.Type.class));
        Mockito.doReturn(new ApiResponse<>(200, null)).when(apiClientSpy).execute(Mockito.any());
        Mockito.doReturn("1736393892000").when(apiClientSpy).buildTimestamp();

        api = new DefaultApi(apiClientSpy);
    }

    /**
     * Deposit (TRADE)
     *
     * <p>Submit deposit request, in this version, we only support BRL deposit via pix. For BRL
     * deposit via pix, you need to place an order before making a transfer from your bank. Before
     * calling this api, please make sure you have already completed your KYC or KYB, and already
     * activated your fiat service on our website. Weight(UID): 45000 Security Type: TRADE Notes: -
     * &#x60;timestamp&#x60;, &#x60;signature&#x60; and &#x60;recvWindow&#x60; are sent as
     * query-string parameters, while the business fields (&#x60;currency&#x60;,
     * &#x60;apiPaymentMethod&#x60;, &#x60;amount&#x60;, &#x60;ext&#x60;) are sent in the JSON
     * request body with &#x60;Content-Type: application/json&#x60;.
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void depositTest() throws ApiException, CryptoException, IOException {
        DepositRequest depositRequest = new DepositRequest();
        depositRequest.currency("BRL");
        depositRequest.apiPaymentMethod(ApiPaymentMethod.bank_transfer);
        depositRequest.amount("1");

        Long recvWindow = 5000L;
        ApiResponse<DepositResponse> response = api.deposit(depositRequest, recvWindow);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());

        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals("recvWindow=5000&timestamp=1736393892000amount=1&apiPaymentMethod=bank_transfer&currency=BRL", signInputCaptor.getValue());
        assertEquals("22e9fedcd109c0299fcc60715c276f5c819c23f04493cbc08065a3e662cb81dd", actualRequest.url().queryParameter("signature"));
        assertEquals("/sapi/v1/fiat/deposit", actualRequest.url().encodedPath());
    }

    /**
     * Fiat Withdraw (TRADE)
     *
     * <p>Submit withdraw request, in this version, we support BRL,ARS,MXN withdrawal via
     * bank_transfer. You need to call this api first, and call query order detail api in a loop to
     * get the status of the order until this order is successful. Before calling this api, please
     * make sure you have already completed your KYC or KYB, and already activated your fiat service
     * on our website. Weight(UID): 45000 Security Type: TRADE
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void fiatWithdrawTest() throws ApiException, CryptoException, IOException {
        FiatWithdrawRequest fiatWithdrawRequest = new FiatWithdrawRequest();
        fiatWithdrawRequest.currency("BRL");
        fiatWithdrawRequest.apiPaymentMethod(ApiPaymentMethod.bank_transfer);
        fiatWithdrawRequest.amount(10L);
        FiatWithdrawRequestAccountInfo fiatWithdrawRequestAccountInfo = new FiatWithdrawRequestAccountInfo();
        fiatWithdrawRequestAccountInfo.setAccountNumber("123");
        fiatWithdrawRequest.accountInfo(fiatWithdrawRequestAccountInfo);

        Long recvWindow = 5000L;
        ApiResponse<FiatWithdrawResponse> response =
                api.fiatWithdraw(fiatWithdrawRequest, recvWindow);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());

        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals("recvWindow=5000&timestamp=1736393892000accountInfo=%7B%22accountNumber%22%3A%22123%22%7D&amount=10&apiPaymentMethod=bank_transfer&currency=BRL", signInputCaptor.getValue());
        assertEquals("fa25f54ecdcc0a201380276831386e0ddafc8e866752d21f663b785613030131", actualRequest.url().queryParameter("signature"));
        assertEquals("/sapi/v2/fiat/withdraw", actualRequest.url().encodedPath());
    }

    /**
     * Get Fiat Deposit/Withdraw History (USER_DATA)
     *
     * <p>Get Fiat Deposit/Withdraw History Weight(UID): 45000 Security Type: USER_DATA Notes: - If
     * &#x60;beginTime&#x60; and &#x60;endTime&#x60; are not sent, recent 30-day data is returned.
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void getFiatDepositWithdrawHistoryTest()
            throws ApiException, CryptoException, IOException {
        String transactionType = "0";
        Long beginTime = 1641782889000L;
        Long endTime = 1641782889000L;
        Long page = 1L;
        Long rows = 100L;
        Long recvWindow = 5000L;
        ApiResponse<GetFiatDepositWithdrawHistoryResponse> response =
                api.getFiatDepositWithdrawHistory(
                        transactionType, beginTime, endTime, page, rows, recvWindow);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());

        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals("transactionType=0&beginTime=1641782889000&endTime=1641782889000&page=1&rows=100&recvWindow=5000&timestamp=1736393892000", signInputCaptor.getValue());
        assertEquals(
                "148a587213e93618829291b697cd5ba7f0e3f715ae346ba4cd107e396a748bf8",
                actualRequest.url().queryParameter("signature"));
        assertEquals("/sapi/v1/fiat/orders", actualRequest.url().encodedPath());
    }

    /**
     * Get Fiat Payments History (USER_DATA)
     *
     * <p>Get Fiat Payments History Weight(IP): 1 Security Type: USER_DATA Notes: - If
     * &#x60;beginTime&#x60; and &#x60;endTime&#x60; are not sent, recent 30-day data is returned. -
     * &#x60;paymentMethod&#x60; is returned only when querying buy history
     * (&#x60;transactionType&#x3D;0&#x60;). - Supported payment methods: &#x60;Cash Balance&#x60;,
     * &#x60;Credit Card&#x60;, &#x60;Online Banking&#x60;, &#x60;Bank Transfer&#x60;.
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void getFiatPaymentsHistoryTest() throws ApiException, CryptoException, IOException {
        String transactionType = "0";
        Long beginTime = 1641782889000L;
        Long endTime = 1641782889000L;
        Long page = 1L;
        Long rows = 100L;
        Long recvWindow = 5000L;
        ApiResponse<GetFiatPaymentsHistoryResponse> response =
                api.getFiatPaymentsHistory(
                        transactionType, beginTime, endTime, page, rows, recvWindow);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());

        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals("transactionType=0&beginTime=1641782889000&endTime=1641782889000&page=1&rows=100&recvWindow=5000&timestamp=1736393892000", signInputCaptor.getValue());
        assertEquals(
                "148a587213e93618829291b697cd5ba7f0e3f715ae346ba4cd107e396a748bf8",
                actualRequest.url().queryParameter("signature"));
        assertEquals("/sapi/v1/fiat/payments", actualRequest.url().encodedPath());
    }

    /**
     * Get Order Detail (USER_DATA)
     *
     * <p>Get Order Detail Before calling this api, please make sure you have already completed your
     * KYC or KYB, and already activated your fiat service on our website. Weight(IP): 1 Security
     * Type: USER_DATA
     *
     * @throws ApiException if the Api call fails
     */
    @Test
    public void getOrderDetailTest() throws ApiException, CryptoException, IOException {
        String orderNo = "036752*678";
        Long recvWindow = 5000L;
        ApiResponse<GetOrderDetailResponse> response = api.getOrderDetail(orderNo, recvWindow);

        ArgumentCaptor<Call> callArgumentCaptor = ArgumentCaptor.forClass(Call.class);
        Mockito.verify(apiClientSpy)
                .execute(callArgumentCaptor.capture(), Mockito.any(java.lang.reflect.Type.class));

        ArgumentCaptor<String> signInputCaptor = ArgumentCaptor.forClass(String.class);
        Mockito.verify(signatureGeneratorSpy).signAsString(signInputCaptor.capture());

        Call captorValue = callArgumentCaptor.getValue();
        Request actualRequest = captorValue.request();

        assertEquals("orderNo=036752*678&recvWindow=5000&timestamp=1736393892000", signInputCaptor.getValue());
        assertEquals("437ad4588868a30680ddcf87d14eb1bf11e2f4960c5ecdbd2d2c67c36e004b3c", actualRequest.url().queryParameter("signature"));
        assertEquals("/sapi/v1/fiat/get-order-detail", actualRequest.url().encodedPath());
    }
}
