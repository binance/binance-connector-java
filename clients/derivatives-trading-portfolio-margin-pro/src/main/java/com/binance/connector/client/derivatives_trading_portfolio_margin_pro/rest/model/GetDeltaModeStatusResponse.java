/*
 * Binance Derivatives Trading Portfolio Margin Pro REST API
 * OpenAPI Specification for the Binance Derivatives Trading Portfolio Margin Pro REST API
 *
 * The version of the OpenAPI document: 1.0.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package com.binance.connector.client.derivatives_trading_portfolio_margin_pro.rest.model;

import com.binance.connector.client.derivatives_trading_portfolio_margin_pro.rest.JSON;
import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import jakarta.validation.constraints.*;
import java.io.IOException;
import java.io.UnsupportedEncodingException;
import java.net.URLEncoder;
import java.nio.charset.StandardCharsets;
import java.util.HashSet;
import java.util.Objects;
import org.hibernate.validator.constraints.*;

/** GetDeltaModeStatusResponse */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.12.0")
public class GetDeltaModeStatusResponse {
    public static final String SERIALIZED_NAME_DELTA_ENABLED = "deltaEnabled";

    @SerializedName(SERIALIZED_NAME_DELTA_ENABLED)
    @jakarta.annotation.Nullable
    private Boolean deltaEnabled;

    public GetDeltaModeStatusResponse() {}

    public GetDeltaModeStatusResponse deltaEnabled(
            @jakarta.annotation.Nullable Boolean deltaEnabled) {
        this.deltaEnabled = deltaEnabled;
        return this;
    }

    /**
     * Get deltaEnabled
     *
     * @return deltaEnabled
     */
    @jakarta.annotation.Nullable
    public Boolean getDeltaEnabled() {
        return deltaEnabled;
    }

    public void setDeltaEnabled(@jakarta.annotation.Nullable Boolean deltaEnabled) {
        this.deltaEnabled = deltaEnabled;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        GetDeltaModeStatusResponse getDeltaModeStatusResponse = (GetDeltaModeStatusResponse) o;
        return Objects.equals(this.deltaEnabled, getDeltaModeStatusResponse.deltaEnabled);
    }

    @Override
    public int hashCode() {
        return Objects.hash(deltaEnabled);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class GetDeltaModeStatusResponse {\n");
        sb.append("		deltaEnabled: ").append(toIndentedString(deltaEnabled)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    public String toUrlQueryString() {
        StringBuilder sb = new StringBuilder();

        Object deltaEnabledValue = getDeltaEnabled();
        String deltaEnabledValueAsString = "";
        deltaEnabledValueAsString = deltaEnabledValue.toString();
        sb.append("deltaEnabled=").append(urlEncode(deltaEnabledValueAsString)).append("");
        return sb.toString();
    }

    public static String urlEncode(String s) {
        try {
            return URLEncoder.encode(s, StandardCharsets.UTF_8.name());
        } catch (UnsupportedEncodingException e) {
            throw new RuntimeException(StandardCharsets.UTF_8.name() + " is unsupported", e);
        }
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces (except the first
     * line).
     */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n		");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("deltaEnabled");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to GetDeltaModeStatusResponse
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!GetDeltaModeStatusResponse.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                "The required field(s) %s in GetDeltaModeStatusResponse is not"
                                        + " found in the empty JSON string",
                                GetDeltaModeStatusResponse.openapiRequiredFields.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!GetDeltaModeStatusResponse.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'GetDeltaModeStatusResponse' and its
                // subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<GetDeltaModeStatusResponse> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(GetDeltaModeStatusResponse.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<GetDeltaModeStatusResponse>() {
                        @Override
                        public void write(JsonWriter out, GetDeltaModeStatusResponse value)
                                throws IOException {
                            JsonElement obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public GetDeltaModeStatusResponse read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            // validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of GetDeltaModeStatusResponse given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of GetDeltaModeStatusResponse
     * @throws IOException if the JSON string is invalid with respect to GetDeltaModeStatusResponse
     */
    public static GetDeltaModeStatusResponse fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, GetDeltaModeStatusResponse.class);
    }

    /**
     * Convert an instance of GetDeltaModeStatusResponse to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
